export const meta = {
  name: 'road-1.0-wave-q',
  description: 'Wave Q (after sweep r26): the sole-blocked rows of lean, rocq, verus, framac and spark, and the closure-predicate family in verus, lean and fstar; sonnet builders in worktrees, independent sonnet checkers',
  phases: [ { title: 'Build', model: 'sonnet' }, { title: 'Check', model: 'sonnet' } ],
}
const S = '<scratch>'
const BASE = '05c5b8d'
const RULES = `You are in a git worktree of the tup repository; run pwd and git log -1 and report both (base ${BASE} or later; if older, say so and stop). The t project is t/; tasks are .t files under t/tasks; the lifted corpus is t/out/lifted-tasks (326 .json task records, gitignored, so read it from $HOME/tup/t/out/lifted-tasks read-only; a sweep row name is the record's file name lowercased with spaces and dots turned into underscores); the DafnyBench sources are $HOME/tup/t-corpora/DafnyBench/DafnyBench/dataset/ground_truth/ (t/corpora.py resolves them); the lifter's own outcome files are under $HOME/tup/t/out/lift (read-only). t/AGREEMENT.md is the 34-task matrix (30 of 34 in all seven) on the reordered twin ladder (SPEC.md "The twins": behavioral rungs first, invariant-drop last; a REFUTED twin is a program whose output differs from the real at a concrete input the kernel confirmed through a certificate); t/CONFORMANCE.md is the conformance suite (457 of 462, every FAIL in framac); t/COVERAGE-lifted-785.md is sweep r26 of the lifted corpus after wave P (182 of 326 rows in all seven, 57 of the 164 MBPP-DFY rows), its "Sole blockers" section names the rows where one kernel alone blocks all-seven; t/COVERAGE-mbpp-dfy-lifter.md joins the lexical census, the lifter and the sweep over the MBPP-DFY 164; internal/ROADMAP-LOG.md's 16.2 paragraphs dated 2026-09-14 and 2026-09-15 (waves N, O, P, the rocq item, sweeps r24 to r26) say what moved and what is named, with the exact tool messages. Rules: export PATH=$HOME/.cargo/bin:$HOME/.opam/default/bin:$HOME/.elan/bin:$HOME/.local/fstar/fstar/bin:$HOME/.local/gnatprove/gnatprove-x86_64-linux-16.1.0-1/bin:$HOME/.local/verus/verus-x86-linux:$PATH; never pgrep, pkill, or ps piped to grep; no em-dashes anywhere (U+2014; use commas or colons); never write a colleague's name or this machine's hostname into any file (say "the lab workstation"); no sudo, no pip, Python 3.12 standard library only; do not commit; touch only the files named as yours; two items may share a lowering file this wave, so add new helpers with your key as a prefix, edit only the functions your rows reach, and list every function you changed in your report (patches are applied in order and a conflicting hunk costs the later item its patch); dated notes (today's UTC date) in the module docstring with the command and what it printed; never claim a number you did not measure here; a gap you cannot close is named with the tool's exact message. Honesty rules for verdicts: never turn a prover timeout into unproved or refuted by relabeling; a proof obligation is proved by the kernel, never assumed (no assume, admit, sorry, Admitted, assume_val, pragma Assume, axiom, or trusted lemma in emitted code for anything the task states); the ensures and requires the kernel checks are the task's own, never weakened; a twin reads REFUTED only when the kernel accepts a certificate that the ensures fails at the harness's concrete witness (or its own verdict is a refutation with that witness); no budget constant is raised; a lifted task's clauses mean exactly what the Dafny source means. Grading: python3 t/grade.py --tasks <dir> --kernels <k1>,<k2> --flake 3 --jobs 8 --out <dir>-grade (two kernels at least, dafny as the control column). Regression bar for a kernel you change: the 34 committed tasks in that kernel at flake 3 against t/AGREEMENT.md's column cell for cell (a cell may only move from unproved or timeout to refuted on the twin side; no real may move), that kernel's conformance column before and after (conformance.py's probe_manifest()/run_items() restricted to one column) with no PASS lost, and that kernel's cells on the dafny_synthesis rows of t/COVERAGE-lifted-785.md that read verified / refuted (report any that move and why). When done: git add -N ., git diff > ${S}/r27-<yourkey>.patch, and report worktree, patch, files, functions changed, before/after numbers, commands with results, what is open by name.`
const SCHEMA = { type:'object', properties:{ key:{type:'string'}, worktree:{type:'string'}, patch:{type:'string'}, files_changed:{type:'array', items:{type:'string'}}, functions_changed:{type:'array', items:{type:'string'}}, before:{type:'string'}, after:{type:'string'}, measured:{type:'string'}, commands:{type:'array', items:{type:'string'}}, open:{type:'string'} }, required:['key','worktree','patch','files_changed','functions_changed','before','after','measured','commands','open'] }
const CHECK_SCHEMA = { type:'object', properties:{ key:{type:'string'}, holds:{type:'boolean'}, regressions:{type:'array', items:{type:'string'}}, measured:{type:'string'}, notes:{type:'string'} }, required:['key','holds','regressions','measured','notes'] }
const ITEMS = [
 { key:'lean-sole', kind:'kernel', title:'lean: the nine sole-blocked rows', task:`t/COVERAGE-lifted-785.md (sweep r26), Sole blockers: lean alone blocks nine rows: dafny_exercise_tmp_tmpouftptir_prac3_ex2__getEven, seng2011_tmp_tmpgk5jq85q_ass1_ex8__getEven, dafny_exercises_tmp_tmpjm75muf__session4exercises_exercisefirstzero__mfirstCero, dafny_programs_tmp_tmpcwodh6qh_src_factorial__factorial, dafny_synthesis_task_id_106__appendArrayToSeq (wave O: the loop-preservation leaf, a bare grind there measured flaky), dafny_synthesis_task_id_578__interleave (grind heartbeats), mfes_2021_tmp_tmpuljn8zd9_fcul_exercises_10_find__find, prog_fun_solutions_tmp_tmp7_gmnz5f_extra_pow__pow, workshop_tmp_tmp0cu11bdq_workshop_answers_question6__arrayUpToN. Reproduce each alone in lean and dafny at flake 3 with lean's own message (set_option trace.grind.ematch true on the emitted file in a scratch run when heartbeats are the cause), fix what the lowering can fix (a cited equation, a deterministic unfold, a stated witness, a loop-preservation lemma), never raise maxHeartbeats, measure; regression bar for lean. Files yours: t/lower_lean.py, only the loop, recursion and goal-closing functions your nine rows reach (the certificate functions certificate, _cert_value, _cert_loop, _cert_undefined, _cert_undefined_loop, _cert_value_loop, _reground are frozen; sfun_quantified and _grind_base belong to lean-closure this wave), t/test_lower_lean_loop_cert.py (add cases), a dated docstring note.` },
 { key:'lean-closure', kind:'kernel', title:'lean: the closure-predicate family', task:`Sweep r26: dafny_synthesis_task_id_412__removeOddNumbers, 426__filterOddNumbers, 554__findOddNumbers, 629__findEvenNumbers read lean timeout / timeout and 436__findNegativeNumbers timeout / refuted (each calls a spec_fun predicate in executable position inside its loop under a forall invariant that quantifies over the output and the input; wave P's lean item cited the spec_fun's equation, which moved 436's twin, and named the residual: the preservation goal's own instantiation over two sequences, "(deterministic) timeout at 'whnf', maximum number of heartbeats"). Read the goal that times out; the invariant over the appended output needs the new element's own witness index carried explicitly across the append (the same gap verus names as "precondition not satisfied" at its loop helper's tail call): state that witness as a lemma or a have at the loop step, cite it, and let grind close the rest; never raise maxHeartbeats. Then dafny_synthesis_task_id_594__firstEvenOddDifference (lean timeout / timeout) and 775__isOddAtIndexOdd (verified / timeout on the twin) if the same mechanism reaches them. Measure the seven in lean and dafny at flake 3; regression bar for lean. Files yours: t/lower_lean.py, only sfun_quantified, _grind_base, _close's base line and new helpers prefixed lean_closure_ (the certificate functions are frozen; the loop and recursion functions belong to lean-sole this wave), t/test_lower_lean_closure_predicate.py (add cases), a dated docstring note.` },
 { key:'rocq-sole', kind:'kernel', title:'rocq: the eight sole-blocked rows and the recursive-recurrence lemma', task:`t/COVERAGE-lifted-785.md (sweep r26), Sole blockers: rocq alone blocks eight rows: dafny_synthesis_task_id_472__containsConsecutiveNumbers, 598__isArmstrong, 808__containsK, dafny_verify_tmp_tmphq7j0row_dataset_error_data_real_error_iseven_success_1__is_even, dafnyprograms_tmp_tmp74_f9k_c_invertarray__invertArray, prog_fun_solutions_tmp_tmp7_gmnz5f_extra_sum__sum, seng2011_tmp_tmpgk5jq85q_flex_ex2__max, software_building_and_verification_projects_tmp_tmp5tm1srrn_cvs_projeto_aula2__mystery1. Wave P's rocq item (internal/ROADMAP-LOG.md, 2026-09-15, "wave P's eighth item") measured the families: the recursive-recurrence family (sum, max, invertArray with search, computeFib, problem5, sumOfCommonDivisors) needs a per-spec_fun bound or nonnegativity lemma proved once by induction beside sf_<f>_eq, because the engine unfolds the recursive equation once per t_dis call and derives no bound (Dafny unfolds without bound); containsK is the file's own gated h = s_len invariant trade-off of 2026-09-11 (a 60x wall-time cost when re-enabled for the return-bearing family; re-measure it on this machine today); is_even is the reverted ground-substitution attempt of 2026-09-10 (unproved to timeout); mystery1 is the gen_rec script for a self-recursive method, unexamined; 472 and 598 read rocq timeout on the real. Build the per-spec_fun lemma generator (proved by induction, never admitted), load-test it as the file's POST_SF_NIA note demands (the 34 committed tasks in rocq at flake 3 twice, and the 71 dafny_synthesis rows reading verified / refuted in rocq), then the others one by one; measure the eight in rocq and dafny at flake 3; regression bar for rocq; test_names.py, test_lower_rocq.py and test_lower_rocq_loop_cert.py must pass. Files yours: t/lower_rocq.py, t/test_lower_rocq_loop_cert.py (add cases), a dated docstring note.` },
 { key:'verus-closure', kind:'kernel', title:'verus: the closure-predicate reals and the three seq-of-array twins', task:`Sweep r26: dafny_synthesis_task_id_412__removeOddNumbers, 426__filterOddNumbers, 436__findNegativeNumbers, 554__findOddNumbers, 629__findEvenNumbers read verus unproved / refuted on the real. Wave P's verus item measured the cause: not the spec fn in executable position (legal verus) but "precondition not satisfied" at the loop helper's own recursive tail call, where the existential membership invariant (every isEven element of arr[0..i) has a witness index in evenList) fails to re-derive across the seq append that grows evenList; a prefix-preservation assert alone was measured insufficient, because the new element's own witness needs threading. Generate that existential-carry proof at the append (an assert forall with the old witnesses shifted by prefix preservation plus the new index for the appended element), measure the five in verus and dafny at flake 3 (the real must read verified, the twin stays refuted). Then the three newly lifted seq-of-array rows dafny_synthesis_task_id_2__sharedElements, 161__removeElements, 249__intersection, which read verus verified / unproved: the twin's refutation certificate does not close; read verus's message on the certificate file and fix the certificate emission for this shape. Regression bar for verus; t/test_lower_verus_refusals.py and t/test_lower_verus_nested_trigger.py must pass. Files yours: t/lower_verus.py, only the loop-helper and certificate functions your rows reach, new helpers prefixed verus_closure_ (the nonlinear-arithmetic functions belong to verus-sole this wave), tests, a dated docstring note.` },
 { key:'verus-sole', kind:'kernel', title:'verus: the five sole-blocked rows, nonlinear arithmetic', task:`t/COVERAGE-lifted-785.md (sweep r26), Sole blockers: verus alone blocks five rows: dafny_exercises_tmp_tmpjm75muf__session2exercises_exercisesquare_root__mroot1, formalmethods_tmp_tmpvda2r3_o_dafny_invariants_ex1__mult, metodos_formais_tmp_tmpbez22nnn_aula_2_ex1__mult, metodos_formais_tmp_tmpql2hwcsh_invariantes_multiplicador__mult, mfes_2021_tmp_tmpuljn8zd9_fcul_exercises_8_sum__sum. Reproduce each alone in verus and dafny at flake 3 with verus's own message; these are multiplication and square-root loops, the nonlinear arithmetic verus disables by default: close them with the mechanism verus documents (assert ... by(nonlinear_arith) with the loop's own facts as its hypotheses, or a lemma from vstd's arithmetic library), never by weakening the task's clauses and never by an assume; measure; regression bar for verus; t/test_lower_verus_refusals.py and t/test_lower_verus_nested_trigger.py must pass. Files yours: t/lower_verus.py, only the arithmetic and invariant-emission functions your rows reach, new helpers prefixed verus_sole_ (the loop-helper certificate functions belong to verus-closure this wave), tests, a dated docstring note.` },
 { key:'fstar-closure', kind:'kernel', title:'fstar: the closure-predicate timeouts, the early-exit witnesses, and the malformed seq-of-array rows', task:`Sweep r26: dafny_synthesis_task_id_412__removeOddNumbers, 426__filterOddNumbers, 436__findNegativeNumbers, 554__findOddNumbers, 629__findEvenNumbers read fstar timeout / timeout; wave P's fstar item measured the cause as a Z3 matching loop on the forall-exists membership invariant, independent of the spec_fun call (an unfold, inlining the predicate away, and an explicit exists pattern each measured not to close it), and named the fix it did not build: a per-loop synthesized witness or unfolding lemma keyed to the forall-exists membership-invariant shape (the new element's own witness index carried across the append, with an SMTPat that instantiates once per element rather than looping). Build it, measure the five in fstar and dafny at flake 3, never raise the fuel, ifuel or rlimit budgets. Then 284__allElementsEqual and 760__hasOnlyOneDistinctElement (unproved / refuted on the real): F* Error 19 at an early-exit branch that needs the local value that triggered the branch as an explicit existential witness; generate that witness assertion at every early-exit site. Then the three newly lifted rows dafny_synthesis_task_id_2__sharedElements, 161__removeElements, 249__intersection, which read fstar malformed / malformed: read F*'s message on the emitted source, fix the emission. Regression bar for fstar; t/test_lower_fstar_abstains.py and t/test_lower_fstar_refusals.py must pass. Files yours: t/lower_fstar.py, t/verifiers/fstar.py, tests, a dated docstring note.` },
 { key:'framac-capacity', kind:'kernel', title:'framac: the CAPACITY bound from the loop invariant, and the six sole-blocked rows', task:`Sweep r26: dafny_synthesis_task_id_412__removeOddNumbers, 426__filterOddNumbers, 436__findNegativeNumbers, 554__findOddNumbers, 629__findEvenNumbers read framac abstain / abstain: wave P's framac item closed the spec_fun-in-executable-position abstain (a contracted C mirror) and named the abstain beneath it, "seq return length is not statically determinable ... no ensures gives a CAPACITY bound": these tasks build a variable-length filtered output whose bound lives in the loop's own invariant (len(out) <= i <= len(in)), not in the ensures. Extend _ret_capacity to derive the bound from the loop invariants (and from a requires when it states one), measure the five in framac and dafny at flake 3. Then 307__deepCopySeq and 743__rotateRight (abstain, a plain seq-typed local later copied whole into the return: build into the return's own buffer when a local's only use is the append idiom followed by a whole copy), and the six framac-sole rows: dafny_synthesis_task_id_240__replaceLastElement, 262__splitArray, 460__getFirstElements, 577__factorialOfLastDigit (recursive spec_fun mirroring, out of scope for wave P), 86__centeredHexagonalNumber, dafny_verify_tmp_tmphq7j0row_test_cases_ghost__triple (timeout / timeout at r26): reproduce each alone with framac's own message, close what the lowering can close inside the pinned step budget, name the rest. Regression bar for framac (conformance 457 of 462; the five FAIL cells are biglen, seqlen, pair_seq, nest_eq, str_tab and may not grow); t/test_framac_spec_fun_exec.py and the other framac tests in t/reproduce.sh must pass. Files yours: t/lower_framac.py (not its certificate functions), t/verifiers/framac.py, tests, a dated docstring note.` },
 { key:'spark-tail', kind:'kernel', title:'spark: the two timeouts in the tail, the one sole-blocked row, and the abstain', task:`Sweep r26: dafny_synthesis_task_id_594__firstEvenOddDifference's spark twin moved from refuted (r25) to timeout (r26) in the sweep's contended tail, and dafny_tmp_tmpmvs2dmry_pancakesort_flip__flip's twin has read timeout since r25 (wave P: "VC_PRECONDITION ... severity medium/limit; VC_SUBPROGRAM_VARIANT ... medium/limit", ESCALATED_STEPS reaches the 180 second wall; five loop invariants, eight seq at/update-chasing foralls over the reversed-subrange shape). Reproduce both alone at DEFAULT_STEPS and read gnatprove's report: for 594 say whether it is contention (verified/refuted alone, twice) or a proof cost; for flip build the targeted lemma the wave P item named (in the spirit of the file's Rotate_* and Concat_*_Lemma family, for the update-chain reasoning of a reversed subrange), never raising DEFAULT_STEPS or ESCALATED_STEPS. Then formal_verication_dafny_tmp_tmpwgl2qz28_challenges_ex2__allow42, the one row spark alone keeps out of all seven, and 760__hasOnlyOneDistinctElement (spark abstain / abstain) and 284__allElementsEqual (spark timeout / refuted on the real): reproduce, read the message, close or name. Regression bar for spark; t/test_lower_spark_loop_cert.py must pass. Files yours: t/lower_spark.py, t/verifiers/spark.py, t/test_lower_spark_loop_cert.py (add cases), a dated docstring note.` },
]
const KERNEL_CHECK = (it, b) => `Adversarial checker. Work in ${b.worktree} (cd there; edit no tracked file; write only under ${S}/check27-${it.key}/). Rules: export PATH=$HOME/.cargo/bin:$HOME/.opam/default/bin:$HOME/.elan/bin:$HOME/.local/fstar/fstar/bin:$HOME/.local/gnatprove/gnatprove-x86_64-linux-16.1.0-1/bin:$HOME/.local/verus/verus-x86-linux:$PATH; never pgrep/pkill/ps|grep; no em-dashes; never write a colleague's name or the machine's hostname; grading of at most 14 tasks allowed; no sweeps. The builder's task:\n${it.task}\n\nClaims:\n${JSON.stringify(b,null,1)}\n\nCheck: git diff --stat shows only the allowed files, and the functions changed are the ones the builder lists and are inside its ownership (a lean or verus builder editing a function the other lean or verus item owns is a regression to report); no U+2014 in added lines; python3 -m py_compile every changed .py; re-run the builder's measurement for every cell it claims moved and compare each verdict (twice for any timeout claimed closed); open one lowered file per claimed row and read it against the task: the ensures and requires must be the task's own, every fact the lowering added must be a proved lemma or invariant (grep the emitted files for assume, admit, sorry, Admitted, assume_val, pragma Assume, axiom, trusted, and read each hit), and no budget constant was raised; for a certificate through a loop, evaluate real and twin at the witness with t/interp.py yourself and confirm they differ, then hand-edit the twin's certificate to claim a wrong value and confirm the kernel rejects it; grade four committed tasks in the kernel including one whose twin the builder did not touch; run the test files named. holds only if every claim reproduces.`
phase('Build')
const results = await pipeline(ITEMS,
  it => agent(`${RULES}\n\nYour key: ${it.key}. Your item: ${it.title}.\n\n${it.task}`, { label:'build:'+it.key, phase:'Build', schema:SCHEMA, model:'sonnet', effort:'medium', isolation:'worktree' }),
  (b, it) => b ? agent(KERNEL_CHECK(it, b), { label:'check:'+it.key, phase:'Check', schema:CHECK_SCHEMA, model:'sonnet', effort:'medium' }).then(c => ({ item: it.key, build: b, check: c })) : null)
return results.filter(Boolean)
