export const meta = {
  name: 'design-road-to-82',
  description: 'Design every remaining hurdle on the lifted corpus: inventory the open rows, diagnose each blocking cell with the tool\'s own message, cluster by cause, design a fix per cluster, refute each design adversarially, then assemble ordered build waves with file ownership and the path to 82 and to 131 of 164',
  phases: [ { title: 'Inventory', model: 'sonnet' }, { title: 'Diagnose', model: 'sonnet' }, { title: 'Design', model: 'sonnet' }, { title: 'Review' }, { title: 'Plan' } ],
}
const S = '/tmp/claude-1004/-home-tmcuzzort/2118453c-5ae7-47ef-9be5-9ab0b2538b3d/scratchpad'
const D = `${S}/design-road`
const BASE = '05c5b8d'
const COMMON = `Repository /home/tmcuzzort/tup at ${BASE} (do not edit any tracked file; do not commit; write only under ${D}/<your-key>/, mkdir -p it first). The t project is t/. Vocabulary: a task is a t program (requires, ensures, invariants, body); the real is the task and the twin a mutated copy; seven kernels (dafny, verus, spark, framac, lean, rocq, fstar) each have a lowering t/lower_<kernel>.py and an adapter t/verifiers/<kernel>.py; a cell reads verified / refuted when the kernel proves the real and rejects the twin through a certificate at a concrete witness; all seven means every column reads that. The lifted corpus is t/out/lifted-tasks (326 .json records, read-only; a sweep row name is the record's file name lowercased with spaces and dots turned into underscores); the lifter's per-program outcomes are t/out/lift/*.outcome.json (read-only); the DafnyBench sources are /home/tmcuzzort/tup/t-corpora/DafnyBench/DafnyBench/dataset/ground_truth/. Tables: t/COVERAGE-lifted-785.md (sweep r26, 182 of 326 in all seven, a Sole blockers section), t/COVERAGE-mbpp-dfy-lifter.md (the 164 MBPP-DFY programs: census, lifter verdict, sweep state per row; 57 of 164 in all seven, 99 lift, 131 lexically in fragment), t/AGREEMENT.md (34 committed tasks), t/CONFORMANCE.md (457 of 462), t/SPEC.md (the language, frozen 1.0-rc1), t/LIFTER-DECISIONS.md (35 rows), t/LIFTER-785-RESIDUALS.md, internal/ROADMAP-LOG.md (16.2's dated paragraphs, 2026-09-12 to 2026-09-15, carry every named gap with the tool's message; wave Q is building right now: lean-sole, lean-closure, rocq-sole, verus-closure, verus-sole, fstar-closure, framac-capacity, spark-tail, described in the last paragraphs' "next" sentences and in ${S}/wave-q.js). Rules: export PATH=$HOME/.cargo/bin:$HOME/.opam/default/bin:$HOME/.elan/bin:$HOME/.local/fstar/fstar/bin:$HOME/.local/gnatprove/gnatprove-x86_64-linux-16.1.0-1/bin:$HOME/.local/verus/verus-x86-linux:$PATH; never pgrep, pkill, or ps piped to grep; no em-dashes (U+2014); never write a colleague's name or this machine's hostname (say "the lab workstation"); no sudo, no pip, Python 3.12 standard library only; never claim a number you did not measure; quote a tool's message verbatim. Honesty rules every design must obey: a proof obligation is proved by the kernel, never assumed (no assume, admit, sorry, Admitted, assume_val, pragma Assume, axiom, trusted lemma for anything the task states); the ensures and requires the kernel checks are the task's own, never weakened; a twin reads REFUTED only through a kernel-accepted certificate at the harness's concrete witness; no budget constant is raised (heartbeats, steps, fuel, rlimit, timeouts); a lifted task means exactly what its Dafny source means; t's integers are unbounded; a construct t excludes (heap, floats, concurrency) is not smuggled in. Grading one row alone: python3 t/grade.py --tasks <dir with the record> --kernels <kernel>,dafny --flake 1 --jobs 2 --out <dir>-grade (dafny is the control column); a timeout under load is re-run once alone and labeled load if it then verifies.`
const INVENTORY_SCHEMA = { type:'object', properties:{ packages:{type:'array', items:{type:'object', properties:{ key:{type:'string'}, kind:{type:'string'}, file:{type:'string'}, rows:{type:'integer'}, mbpp_rows:{type:'integer'}, summary:{type:'string'} }, required:['key','kind','file','rows','mbpp_rows','summary']}}, open_164:{type:'integer'}, open_326:{type:'integer'}, not_lifted_164:{type:'integer'}, out_of_fragment_164:{type:'integer'}, notes:{type:'string'} }, required:['packages','open_164','open_326','not_lifted_164','out_of_fragment_164','notes'] }
const DIAG_SCHEMA = { type:'object', properties:{ key:{type:'string'}, clusters:{type:'array', items:{type:'object', properties:{ id:{type:'string'}, cause:{type:'string'}, message:{type:'string'}, rows:{type:'array', items:{type:'string'}}, mbpp_rows:{type:'array', items:{type:'string'}}, example_row:{type:'string'}, example_file:{type:'string'}, in_flight:{type:'string'} }, required:['id','cause','message','rows','mbpp_rows','example_row','example_file','in_flight']}}, load_timeouts:{type:'array', items:{type:'string'}}, measured:{type:'string'} }, required:['key','clusters','load_timeouts','measured'] }
const DESIGN_SCHEMA = { type:'object', properties:{ id:{type:'string'}, kernel_or_lifter:{type:'string'}, cause:{type:'string'}, mechanism:{type:'string'}, files:{type:'array', items:{type:'string'}}, functions:{type:'array', items:{type:'string'}}, soundness:{type:'string'}, tests:{type:'string'}, regression_bar:{type:'string'}, rows_unlocked:{type:'array', items:{type:'string'}}, rows_not_reached:{type:'array', items:{type:'string'}}, risks:{type:'string'}, depends_on:{type:'array', items:{type:'string'}}, effort:{type:'string'}, design_file:{type:'string'} }, required:['id','kernel_or_lifter','cause','mechanism','files','functions','soundness','tests','regression_bar','rows_unlocked','rows_not_reached','risks','depends_on','effort','design_file'] }
const REVIEW_SCHEMA = { type:'object', properties:{ id:{type:'string'}, verdict:{type:'string'}, refutations:{type:'array', items:{type:'string'}}, required_changes:{type:'array', items:{type:'string'}}, measured:{type:'string'} }, required:['id','verdict','refutations','required_changes','measured'] }
const PLAN_SCHEMA = { type:'object', properties:{ plan_file:{type:'string'}, waves:{type:'array', items:{type:'object', properties:{ name:{type:'string'}, items:{type:'array', items:{type:'string'}}, expected_164:{type:'integer'} }, required:['name','items','expected_164']}}, path_to_82:{type:'string'}, ceiling:{type:'string'}, decisions_for_treston:{type:'array', items:{type:'string'}}, summary:{type:'string'} }, required:['plan_file','waves','path_to_82','ceiling','decisions_for_treston','summary'] }

phase('Inventory')
const inv = await agent(`${COMMON}\n\nYour key: inventory. Build the inventory of every open row and split it into work packages. (1) Parse t/COVERAGE-lifted-785.md: for every row not in all seven, record each cell that is not verified / refuted (kernel, real outcome, twin outcome). (2) Parse t/COVERAGE-mbpp-dfy-lifter.md: for every one of the 164 not in all seven, record whether it is lifted, its lifter refusal token if refused (from the table and from t/out/lift/<program>.outcome.json), and whether it is lexically in fragment or which lexical gate blocks it. (3) Write one JSON file per package under ${D}/inventory/packages/: a kernel package per kernel (key <kernel>, kind kernel) holding every row (of the 326) where that kernel's cell is not verified / refuted, with the cell outcomes, a flag for the 164, and the record path under t/out/lifted-tasks; a lifter package per refusal class (key lifter-<token>, kind lifter) holding the refused programs of the 164 with the source path, the refusal token, the outcome file and, when the census says so, the lexical gate; and one package key out-of-fragment (kind construct) listing the 164 programs the census puts outside the fragment with their gate. Merge refusal classes with fewer than three programs into lifter-other. (4) Write ${D}/inventory/INVENTORY.md: the counts (open rows of 326, open rows of 164, not lifted of 164, out of fragment of 164), a table of packages with row counts, and the per-kernel blocking-cell counts split by outcome (unproved, timeout, abstain, malformed, vacuous, lower-error, decorative). Return the package list with counts and the four headline counts.`, { label:'inventory', phase:'Inventory', schema:INVENTORY_SCHEMA, model:'sonnet', effort:'medium' })
if (!inv) throw new Error('inventory returned nothing')
log(`inventory: ${inv.packages.length} packages; open 164: ${inv.open_164}, not lifted: ${inv.not_lifted_164}, out of fragment: ${inv.out_of_fragment_164}`)

const DIAG = (p) => p.kind === 'kernel'
 ? `${COMMON}\n\nYour key: ${p.key}. Diagnose kernel ${p.key}'s blocking cells. Read ${p.file} (${p.rows} rows, ${p.mbpp_rows} of them in the 164). For every row: copy its record into ${D}/${p.key}/rows/<row>/ and grade it alone with ${p.key} and dafny at flake 1 (jobs 2; run rows in small batches so the lab workstation is not swamped: at most 4 grade.py processes of yours at once); open the lowered real and twin files and the kernel's log under the grade output; record the kernel's exact message for the failing cell (an unproved goal, a timeout with what it was doing, an abstain with the lowering's own message, a malformed with the parser's message, a lower-error with the traceback's last line). A timeout that verifies when re-run once alone at jobs 1 is labeled load, listed in load_timeouts and excluded from clusters. Then cluster the rows by cause: the same shape in the task (a spec_fun called under a quantified invariant, a seq append with a membership invariant, nonlinear arithmetic, a value certificate through a loop, a recursive spec_fun bound, a whole-sequence equality, a nested seq, a pair, a string library member, an early-exit witness, and so on), the same kernel message, the same lowering function. Prefer fewer, deeper clusters over many shallow ones, but never merge two rows whose messages name different obstacles. For each cluster: an id (${p.key}-<short-name>), the cause in one paragraph a builder can act on, the verbatim message, the rows (and which are in the 164), one example row and its lowered file path, and in_flight naming the wave Q item whose task text covers this cluster (lean-sole, lean-closure, rocq-sole, verus-closure, verus-sole, fstar-closure, framac-capacity, spark-tail) or "none". Write ${D}/${p.key}/DIAGNOSIS.md with the clusters and every row's message, and return the clusters.`
 : p.kind === 'lifter'
 ? `${COMMON}\n\nYour key: ${p.key}. Diagnose the lifter refusal class in ${p.file} (${p.rows} programs of the 164). For every program: read its Dafny source, its outcome file, and the lifter's refusal token and message (re-lift it alone with python3 t/lifter.py --list <a one-line list file> --corpus-dir /home/tmcuzzort/tup/t-corpora/DafnyBench/DafnyBench/dataset/ground_truth --force --timeout 200 --out ${D}/${p.key}/lift/ and read the printed reason and the .outcome.json); read t/LIFTER-DESIGN.md, t/LIFTER-DECISIONS.md and t/LIFTER-785-RESIDUALS.md for what has already been decided about this token. Cluster the programs by the exact Dafny shape the lifter refuses (a function with a reads clause, a set comprehension, an array of seqs, a two-dimensional array, a let expression in a spec, a method call in the body, a heap-allocated object, a real, a datatype, a ghost variable, and so on) and, for each cluster, say whether the shape is inside t's language as SPEC.md stands (a lifter rule can lift it), needs a construct on the roadmap (name it), or needs a construct t excludes (heap, floats, concurrency, datatypes) and is out. For each cluster: an id (${p.key}-<short-name>), the cause, the verbatim message, the programs (all in the 164), an example program and its source path, and in_flight "none". Write ${D}/${p.key}/DIAGNOSIS.md and return the clusters.`
 : `${COMMON}\n\nYour key: ${p.key}. The 164 programs the lexical census puts outside t's fragment are listed in ${p.file} with their gate. Read each source and its gate; cluster by construct (real, datatype, heap, two-dimensional array, set, map, string method beyond the library, class, trait, iterator, and so on); for each cluster say whether the construct is on the roadmap's construct line (t/SPEC.md and internal/ROADMAP-LOG.md 12.7 name the landed ones and the far field), excluded by design, or undecided, and what adding it would cost in SPEC.md and in seven lowerings. For each cluster: an id (construct-<name>), the cause, the census gate as the message, the programs, an example program and its source path, and in_flight "none". Write ${D}/${p.key}/DIAGNOSIS.md and return the clusters.`

const DESIGN = (c, p) => `${COMMON}\n\nYour key: design-${c.id}. Design the fix for cluster ${c.id} (package ${p.key}). Cause: ${c.cause}\nMessage: ${c.message}\nRows: ${c.rows.join(', ')}\nIn the 164: ${c.mbpp_rows.join(', ') || 'none'}\nExample row: ${c.example_row}, lowered file ${c.example_file}\nIn flight in wave Q: ${c.in_flight}\nRead ${D}/${p.key}/DIAGNOSIS.md, the example row's record and lowered files, and the lowering or lifter module the fix lives in (t/lower_<kernel>.py or t/lift_*.py; read the whole function you would change and its neighbours, and the module docstring's dated notes, which record what was tried and reverted). If the cluster is in flight in wave Q, read that item's task text in ${S}/wave-q.js and design what wave Q will NOT cover or what to do if it fails; say so in the mechanism. Write ${D}/design/${c.id}.md with: the mechanism (the exact proof, certificate, lemma, invariant or lifter rule, in the kernel's own terms, with the emitted text you would generate for the example row); the files and functions to change or add, each named; why it is sound under the honesty rules (what is proved by the kernel, what is derived, nothing assumed); the tests to add (file, cases); the regression bar to run; the rows it unlocks by name and the rows in the cluster it will not reach and why; risks (proof cost under the pinned budgets, load, a shape the mechanism cannot express); dependencies on other clusters or on wave Q; effort as one of small, medium, large with a one-line reason. Do not implement; prototyping one emitted file by hand and running the kernel on it once is allowed and welcome (report the result), and its files stay under ${D}/design/${c.id}/. Return the design.`

const REVIEW = (d, c) => `${COMMON}\n\nYour key: review-${d.id}. Adversarial review of the design ${d.design_file} for cluster ${c.id} (cause: ${c.cause}; message: ${c.message}). Try to refute it on four axes and report what you found, not what you assume: (1) soundness under the honesty rules: does any step assume, weaken an ensures or requires, relabel a timeout, raise a budget, or prove something other than the task's own clauses; (2) semantics: does the mechanism preserve t/SPEC.md's meaning (integers unbounded, definedness, the loop frame rule, the twin rule) and, for a lifter rule, the Dafny source's meaning; (3) feasibility in the kernel: does the kernel actually accept the emitted shape (read the example lowered file; if the design prototyped one, run the kernel on it once yourself and quote the result; if it did not, write the smallest such file yourself under ${D}/review/${d.id}/ and run the kernel once, flake 1); (4) scope: does it reach the rows it claims, does it conflict with another design's functions (read ${D}/design/ for the same kernel), does it duplicate wave Q. Verdict: holds, holds-with-changes, or refuted, with each refutation as a sentence naming the step, and the required changes as an ordered list a reviser can apply. Write ${D}/review/${d.id}.md and return the verdict.`

const REVISE = (d, r, c) => `${COMMON}\n\nYour key: revise-${d.id}. Revise the design ${d.design_file} for cluster ${c.id} in the light of the review ${D}/review/${d.id}.md (verdict ${r.verdict}; refutations: ${r.refutations.join(' | ') || 'none'}; required changes: ${r.required_changes.join(' | ') || 'none'}). Apply every required change, keep what the review upheld, and rewrite ${D}/design/${c.id}.md in place as the final design; if the review refuted the mechanism outright, replace it with the alternative the review points at or, if none exists, mark the design "open: no sound mechanism found" and say what measurement would change that. Return the final design in the same shape.`

phase('Diagnose')
const packs = await pipeline(inv.packages,
  p => agent(DIAG(p), { label:'diagnose:'+p.key, phase:'Diagnose', schema:DIAG_SCHEMA, model:'sonnet', effort:'medium' }).then(d => ({ p, d })),
  ({ p, d }) => d ? parallel(d.clusters.map(c => () =>
      agent(DESIGN(c, p), { label:'design:'+c.id, phase:'Design', schema:DESIGN_SCHEMA, model:'sonnet', effort:'medium' })
        .then(des => des ? agent(REVIEW(des, c), { label:'review:'+c.id, phase:'Review', schema:REVIEW_SCHEMA, effort:'high' }).then(rev => ({ des, rev })) : null)
        .then(x => x && x.rev && x.rev.verdict !== 'holds' ? agent(REVISE(x.des, x.rev, c), { label:'revise:'+c.id, phase:'Design', schema:DESIGN_SCHEMA, model:'sonnet', effort:'medium' }).then(fin => ({ cluster: c, design: fin || x.des, review: x.rev })) : (x ? { cluster: c, design: x.des, review: x.rev } : null))
    )).then(results => ({ p, d, results: results.filter(Boolean) })) : null)
const done = packs.filter(Boolean)
const nDesigns = done.reduce((n, x) => n + x.results.length, 0)
log(`designs reviewed: ${nDesigns} across ${done.length} packages`)

phase('Plan')
const plan = await agent(`${COMMON}\n\nYour key: plan. Assemble the build plan from every final design under ${D}/design/*.md, every review under ${D}/review/*.md, every DIAGNOSIS.md under ${D}/<package>/, and ${D}/inventory/INVENTORY.md; wave Q's items are in ${S}/wave-q.js and are in flight (assume they land; plan what follows them and what to do if any fails). Write ${D}/PLAN.md, a design document in the style of t/DESIGN-framac-nested-seq.md (short sections, no em-dashes, every number measured in this run or copied from a named table), with: (1) the ledger: every open row of the 164 in one table, its blocking cells, the cluster ids, the design that unlocks it or the reason it stays out (a construct t excludes, a source that is its own problem such as dafny's vacuous 433 and 567); (2) the waves: ordered build waves R, S, T, ... each of at most eight items, each item a builder brief in the form of wave Q's task texts (rows by name, the mechanism from the design, files and functions owned with no two items of a wave sharing a function, tests, the regression bar, the expected rows), ordered by rows unlocked on the 164 per unit of effort and by dependency, with the expected all-seven count on the 164 after each wave stated as a projection, not a claim; (3) the path to 82: which waves reach it and what must hold; (4) the ceiling: the count reachable without a language change (the 131 lexically in fragment minus the rows whose designs are marked open), and what each excluded construct would add; (5) decisions for Treston: every language or design choice the designs need him to make, one line each with the cost of each answer; (6) the GPU track kept separate: the spec experiment and the training loop do not gate any wave. Return the plan file path, the waves with item keys and projected counts, the path to 82, the ceiling, the decisions, and a five-sentence summary.`, { label:'plan', phase:'Plan', schema:PLAN_SCHEMA, effort:'high' })
return { inventory: inv, designs: nDesigns, plan }
