export const meta = {
  name: 'road-1.0-wave-r',
  description: 'Wave R (after sweep r27): lean, rocq, framac and spark sole blockers, the closure-predicate and seq-of-array rows in lean, rocq and framac; sonnet builders in worktrees, independent sonnet checkers',
  phases: [ { title: 'Build', model: 'sonnet' }, { title: 'Check', model: 'sonnet' } ],
}
const S = '<scratch>'
const BASE = '215f0a2'
const RULES = `You are in a git worktree of the tup repository; run pwd and git log -1 and report both (base ${BASE} or later; if older, say so and stop). The t project is t/; tasks are .t files under t/tasks; the lifted corpus is t/out/lifted-tasks (326 .json task records, gitignored, so read it from $HOME/tup/t/out/lifted-tasks read-only; a sweep row name is the record's file name lowercased with spaces and dots turned into underscores); the DafnyBench sources are $HOME/tup/t-corpora/DafnyBench/DafnyBench/dataset/ground_truth/ (t/corpora.py resolves them); the lifter's own outcome files are under $HOME/tup/t/out/lift (read-only). t/AGREEMENT.md is the 34-task matrix (30 of 34 in all seven) on the reordered twin ladder (SPEC.md "The twins": behavioral rungs first, invariant-drop last; a REFUTED twin is a program whose output differs from the real at a concrete input the kernel confirmed through a certificate); t/CONFORMANCE.md is the conformance suite (457 of 462, every FAIL in framac); t/COVERAGE-lifted-785.md is sweep r27 of the lifted corpus after wave Q (192 of 326 rows in all seven, 57 of the 164 MBPP-DFY rows), its "Sole blockers" section names the rows where one kernel alone blocks all-seven; t/COVERAGE-mbpp-dfy-lifter.md joins the lexical census, the lifter and the sweep over the MBPP-DFY 164; internal/ROADMAP-LOG.md's 16.2 paragraphs dated 2026-09-14 and 2026-09-15 and 2026-09-16 (waves N, O, P, Q, sweeps r24 to r27) say what moved and what is named, with the exact tool messages. Rules: export PATH=$HOME/.cargo/bin:$HOME/.opam/default/bin:$HOME/.elan/bin:$HOME/.local/fstar/fstar/bin:$HOME/.local/gnatprove/gnatprove-x86_64-linux-16.1.0-1/bin:$HOME/.local/verus/verus-x86-linux:$PATH; never pgrep, pkill, or ps piped to grep; no em-dashes anywhere (U+2014; use commas or colons); never write a colleague's name or this machine's hostname into any file (say "the lab workstation"); no sudo, no pip, Python 3.12 standard library only; do not commit; touch only the files named as yours; two items may share a lowering file this wave, so add new helpers with your key as a prefix, edit only the functions your rows reach, and list every function you changed in your report (patches are applied in order and a conflicting hunk costs the later item its patch); dated notes (today's UTC date) in the module docstring with the command and what it printed; never claim a number you did not measure here; a gap you cannot close is named with the tool's exact message. Honesty rules for verdicts: never turn a prover timeout into unproved or refuted by relabeling; a proof obligation is proved by the kernel, never assumed (no assume, admit, sorry, Admitted, assume_val, pragma Assume, axiom, or trusted lemma in emitted code for anything the task states); the ensures and requires the kernel checks are the task's own, never weakened; a twin reads REFUTED only when the kernel accepts a certificate that the ensures fails at the harness's concrete witness (or its own verdict is a refutation with that witness); no budget constant is raised; a lifted task's clauses mean exactly what the Dafny source means. Grading: python3 t/grade.py --tasks <dir> --kernels <k1>,<k2> --flake 3 --jobs 8 --out <dir>-grade (two kernels at least, dafny as the control column). Regression bar for a kernel you change: the 34 committed tasks in that kernel at flake 3 against t/AGREEMENT.md's column cell for cell (a cell may only move from unproved or timeout to refuted on the twin side; no real may move), that kernel's conformance column before and after (conformance.py's probe_manifest()/run_items() restricted to one column) with no PASS lost, and that kernel's cells on the dafny_synthesis rows of t/COVERAGE-lifted-785.md that read verified / refuted (report any that move and why). When done: git add -N ., git diff > ${S}/r28-<yourkey>.patch, and report worktree, patch, files, functions changed, before/after numbers, commands with results, what is open by name.`
const SCHEMA = { type:'object', properties:{ key:{type:'string'}, worktree:{type:'string'}, patch:{type:'string'}, files_changed:{type:'array', items:{type:'string'}}, functions_changed:{type:'array', items:{type:'string'}}, before:{type:'string'}, after:{type:'string'}, measured:{type:'string'}, commands:{type:'array', items:{type:'string'}}, open:{type:'string'} }, required:['key','worktree','patch','files_changed','functions_changed','before','after','measured','commands','open'] }
const CHECK_SCHEMA = { type:'object', properties:{ key:{type:'string'}, holds:{type:'boolean'}, regressions:{type:'array', items:{type:'string'}}, measured:{type:'string'}, notes:{type:'string'} }, required:['key','holds','regressions','measured','notes'] }
const ITEMS = [
 { key:"lean-closure", kind:'kernel', title:"lean: the closure-predicate family and the seq-of-array rows", task:"Sweep r27 (t/COVERAGE-lifted-785.md): dafny_synthesis_task_id_412__removeOddNumbers, 426__filterOddNumbers, 554__findOddNumbers, 629__findEvenNumbers, 436__findNegativeNumbers, 594__firstEvenOddDifference and 775__isOddAtIndexOdd are still out in lean (verus and fstar closed their side in wave Q; read internal/ROADMAP-LOG.md 16.2, the wave P and wave Q paragraphs). The residual named: the preservation goal's instantiation over two sequences, \"(deterministic) timeout at 'whnf', maximum number of heartbeats\"; the invariant over the appended output needs the new element's own witness index carried explicitly across the append (wave Q's verus-closure built exactly this existential-carry proof in t/lower_verus.py, helpers verus_closure_*: read it and port the idea). A wave Q attempt left <scratch>/r27-lean-closure.patch with no report and no check: read it as a hint only. Then the seq-of-array rows dafny_synthesis_task_id_2__sharedElements, 161__removeElements, 249__intersection, 447 (find its row by grepping task_id_447) in lean: reproduce with lean's message, close or name. Never raise maxHeartbeats. Measure every row in lean and dafny at flake 3; regression bar for lean. Files yours: t/lower_lean.py, only sfun_quantified, _grind_base, _close's base line and new helpers prefixed lean_closure_ (the loop, recursion and certificate functions belong to lean-sole this wave), t/test_lower_lean_closure_predicate.py (add cases), a dated docstring note. Before building, read <scratch>/design-road/<kernel>/DIAGNOSIS.md for your kernel and the design documents under <scratch>/design-road/design/ and <scratch>/design-road/review/ whose names start with your kernel (ls them; read only those that name your rows). They were written by the 2026-09-15 design workflow against sweeps r26 and r27 and most are UNREVIEWED proposals: treat each as a hypothesis, reproduce before acting, and say in your report which design you followed and whether it held." },
 { key:"lean-sole", kind:'kernel', title:"lean: the eight sole-blocked rows", task:"Sweep r27 Sole blockers: lean alone keeps out getEven (two rows), mfirstCero, 106__appendArrayToSeq, 578__interleave, gcdCalc, find, arrayUpToN (full names in the table). Wave Q's lean-sole item (log 16.2) named: mfirstCero and find need _cert_undefined_loop to handle a loop guard whose own evaluation is Undef (this wave you own _cert_undefined_loop and _cert_undefined for that change only: a certificate still reads REFUTED only when the kernel checks it), 106 the invariant-application leaf, 578 and the two getEven timeouts under load (reproduce alone twice first: contention is not a lowering bug, say so with numbers), arrayUpToN unreached, gcdCalc new at r27 (check whether it regressed from r26 and why). Never raise maxHeartbeats. Measure the eight in lean and dafny at flake 3; regression bar for lean. Files yours: t/lower_lean.py, the loop, recursion and certificate functions your rows reach (not sfun_quantified, _grind_base or lean_closure_* helpers), t/test_lower_lean_loop_cert.py (add cases), a dated docstring note. Before building, read <scratch>/design-road/<kernel>/DIAGNOSIS.md for your kernel and the design documents under <scratch>/design-road/design/ and <scratch>/design-road/review/ whose names start with your kernel (ls them; read only those that name your rows). They were written by the 2026-09-15 design workflow against sweeps r26 and r27 and most are UNREVIEWED proposals: treat each as a hypothesis, reproduce before acting, and say in your report which design you followed and whether it held." },
 { key:"rocq-sole", kind:'kernel', title:"rocq: the nine sole-blocked rows and the scan-loop invariant generator", task:"Sweep r27 Sole blockers: rocq alone keeps out expt, 472__containsConsecutiveNumbers, 598__isArmstrong, 808__containsK, is_even, invertArray, computeFact2, flex max, mystery1 (full names in the table). Wave Q's rocq-sole item (log 16.2) built the per-spec_fun bound lemma (t_eqs_rec) and named the rest: a scan-loop invariant generator for 472, 808, invertArray and flex max (the loop's own prefix fact, proved by induction), containsK's gated h = s_len invariant (re-measure its cost), is_even's ground substitution, mystery1's gen_rec script, expt and computeFact2 new at r27 (check whether they regressed from r26 and why). Never Admitted, never an axiom. Load-test as the file's POST_SF_NIA note demands. Measure the nine in rocq and dafny at flake 3; regression bar for rocq; test_names.py, test_lower_rocq.py and test_lower_rocq_loop_cert.py must pass. Files yours: t/lower_rocq.py, only the loop-invariant, recursion and arithmetic functions your rows reach, new helpers prefixed rocq_sole_, t/test_lower_rocq_loop_cert.py (add cases), a dated docstring note. Before building, read <scratch>/design-road/<kernel>/DIAGNOSIS.md for your kernel and the design documents under <scratch>/design-road/design/ and <scratch>/design-road/review/ whose names start with your kernel (ls them; read only those that name your rows). They were written by the 2026-09-15 design workflow against sweeps r26 and r27 and most are UNREVIEWED proposals: treat each as a hypothesis, reproduce before acting, and say in your report which design you followed and whether it held." },
 { key:"rocq-closure", kind:'kernel', title:"rocq: the closure-predicate family and the seq-of-array rows", task:"Sweep r27: the closure-predicate rows 412, 426, 436, 554, 629 (and 594, 775 where rocq reads anything but verified / refuted) and the seq-of-array rows 2__sharedElements, 161__removeElements, 249__intersection and 447 are out in rocq. Read each row's rocq verdict in t/COVERAGE-lifted-785.md, reproduce alone with rocq's message, and close with the existential-carry idea wave Q built in t/lower_verus.py (verus_closure_*: the new element's witness index carried across the append), or an abstain replaced by a real lowering for the seq-of-array shape. Never Admitted, never an axiom. Measure in rocq and dafny at flake 3; regression bar for rocq; test_names.py, test_lower_rocq.py and test_lower_rocq_loop_cert.py must pass. Files yours: t/lower_rocq.py, only new helpers prefixed rocq_closure_ and the call sites that dispatch to them (the loop-invariant, recursion and arithmetic functions belong to rocq-sole this wave), a new test file t/test_lower_rocq_closure.py, a dated docstring note. Before building, read <scratch>/design-road/<kernel>/DIAGNOSIS.md for your kernel and the design documents under <scratch>/design-road/design/ and <scratch>/design-road/review/ whose names start with your kernel (ls them; read only those that name your rows). They were written by the 2026-09-15 design workflow against sweeps r26 and r27 and most are UNREVIEWED proposals: treat each as a hypothesis, reproduce before acting, and say in your report which design you followed and whether it held." },
 { key:"framac", kind:'kernel', title:"framac: the six sole-blocked rows, the closure rows and the seq-of-array rows", task:"Sweep r27 Sole blockers: framac alone keeps out 240__replaceLastElement, 262__splitArray, 460__getFirstElements, 577__factorialOfLastDigit (recursive spec_fun mirroring), 86__centeredHexagonalNumber, ghost__triple. Wave Q's framac-capacity item (log 16.2) derived CAPACITY from the loop invariant; read what it named open. Reproduce each alone with framac's message, close what the lowering can close inside the pinned step budget, name the rest. Then the closure rows 412, 426, 436, 554, 629 and the seq-of-array rows 2, 161, 249, 447 where framac still abstains or fails. Regression bar for framac (conformance 457 of 462; the five FAIL cells biglen, seqlen, pair_seq, nest_eq, str_tab may not grow); t/test_framac_spec_fun_exec.py, t/test_framac_capacity.py and the other framac tests in t/reproduce.sh must pass. Files yours: t/lower_framac.py (not its certificate functions), t/verifiers/framac.py, tests, a dated docstring note. Before building, read <scratch>/design-road/<kernel>/DIAGNOSIS.md for your kernel and the design documents under <scratch>/design-road/design/ and <scratch>/design-road/review/ whose names start with your kernel (ls them; read only those that name your rows). They were written by the 2026-09-15 design workflow against sweeps r26 and r27 and most are UNREVIEWED proposals: treat each as a hypothesis, reproduce before acting, and say in your report which design you followed and whether it held." },
 { key:"spark-sole", kind:'kernel', title:"spark: the six sole-blocked rows", task:"Sweep r27 Sole blockers: spark alone keeps out three pot rows, fatorial, mod, factIter (full names in the table). These are new sole blockers at r27: first establish for each whether it read verified / refuted in spark at r26 (git show 05c5b8d:t/COVERAGE-lifted-785.md and 034f1e7) and whether wave Q's spark-tail patch or load moved it; reproduce each alone twice at DEFAULT_STEPS with gnatprove's report; contention is named with numbers, a proof cost gets a targeted lemma in the spirit of the file's existing lemma family (nonlinear multiplication and power loops), never raising DEFAULT_STEPS or ESCALATED_STEPS. Regression bar for spark; t/test_lower_spark_loop_cert.py must pass. Files yours: t/lower_spark.py, t/verifiers/spark.py, t/test_lower_spark_loop_cert.py (add cases), a dated docstring note. Before building, read <scratch>/design-road/<kernel>/DIAGNOSIS.md for your kernel and the design documents under <scratch>/design-road/design/ and <scratch>/design-road/review/ whose names start with your kernel (ls them; read only those that name your rows). They were written by the 2026-09-15 design workflow against sweeps r26 and r27 and most are UNREVIEWED proposals: treat each as a hypothesis, reproduce before acting, and say in your report which design you followed and whether it held." },
]
const KERNEL_CHECK = (it, b) => `Adversarial checker. Work in ${b.worktree} (cd there; edit no tracked file; write only under ${S}/check28-${it.key}/). Rules: export PATH=$HOME/.cargo/bin:$HOME/.opam/default/bin:$HOME/.elan/bin:$HOME/.local/fstar/fstar/bin:$HOME/.local/gnatprove/gnatprove-x86_64-linux-16.1.0-1/bin:$HOME/.local/verus/verus-x86-linux:$PATH; never pgrep/pkill/ps|grep; no em-dashes; never write a colleague's name or the machine's hostname; grading of at most 14 tasks allowed; no sweeps. The builder's task:\n${it.task}\n\nClaims:\n${JSON.stringify(b,null,1)}\n\nCheck: git diff --stat shows only the allowed files, and the functions changed are the ones the builder lists and are inside its ownership (a lean or verus builder editing a function the other lean or verus item owns is a regression to report); no U+2014 in added lines; python3 -m py_compile every changed .py; re-run the builder's measurement for every cell it claims moved and compare each verdict (twice for any timeout claimed closed); open one lowered file per claimed row and read it against the task: the ensures and requires must be the task's own, every fact the lowering added must be a proved lemma or invariant (grep the emitted files for assume, admit, sorry, Admitted, assume_val, pragma Assume, axiom, trusted, and read each hit), and no budget constant was raised; for a certificate through a loop, evaluate real and twin at the witness with t/interp.py yourself and confirm they differ, then hand-edit the twin's certificate to claim a wrong value and confirm the kernel rejects it; grade four committed tasks in the kernel including one whose twin the builder did not touch; run the test files named. holds only if every claim reproduces.`
phase('Build')
const results = await pipeline(ITEMS,
  it => agent(`${RULES}\n\nYour key: ${it.key}. Your item: ${it.title}.\n\n${it.task}`, { label:'build:'+it.key, phase:'Build', schema:SCHEMA, model:'sonnet', effort:'medium', isolation:'worktree' }),
  (b, it) => b ? agent(KERNEL_CHECK(it, b), { label:'check:'+it.key, phase:'Check', schema:CHECK_SCHEMA, model:'sonnet', effort:'medium' }).then(c => ({ item: it.key, build: b, check: c })) : null)
return results.filter(Boolean)
