# Verification dossier — progress so far v3.md (fleet wf_7eccca12-f75, 2026-08-22)

Lenses returned: 5  Refuter reports: 5  Synthesis: yes (appended below)

## CLAIMS VERIFIED CLEAN (re-derived and matched)
### lens 1
- t-distribution implemented from scratch (regularized incomplete beta, Lentz continued fraction) and self-checked: t_.975(10) = 2.2281 and t_.975(30) = 2.0423 reproduced exactly; two-sided p at t=2.2281, df=10 returns .050003; at t=1.96, df=1e7 returns .049996
- Table 2, Original adapter session A (llama3-forged-rep vs llama3-forged-null-rep): n=41, M=.5018, SD=.0401 — all confirmed. Delta raw -0.0283 pp (paper -0.03), t(78.99) = -0.032 (paper -0.03), p = .9746 (paper .975), 95% CI [-1.789, +1.733] pp (paper [-1.79, +1.73])
- Table 2, session A typing-forgiven: delta -0.0146 pp (paper -0.01), t(78.77) = -0.018, p = .9858 (paper .986). Typing fails/replicate 11.122 (paper 11.1)
- Table 2, Original adapter session B (ctl-his vs ctl-null): n=15, M=.5067, SD=.0325 confirmed. Raw delta -0.0440 pp (paper -0.04), t(25.34) = -0.0305 (paper -0.03), p = .9759 (paper .976). Forgiven +0.5173 pp (paper +0.52), t(26.01)=0.370, p = .7141 (paper .714). Typing 11.067 (paper 11.1)
- Table 2, Retrain 1 (llama3-forged-rep1 vs ctl-null): n=40, M=.6032, SD=.0405 confirmed. Raw delta +9.6116 pp (paper +9.61), t(22.82) = 7.1875 (paper 7.19), p = 2.679e-7 (paper 2.7e-7), 95% CI [+6.84, +12.38] pp
- Table 2, Retrain 1 typing-forgiven: +4.4048 pp (paper +4.40), t(23.87) = 3.4205, p = .002254 (paper .0023), 95% CI [+1.75, +7.06] pp. Typing 2.125 (paper 2.1)
- Table 2, Retrain 2 (llama3-forged-rep2 vs ctl-null): n=40, M=.6161, SD=.0400 confirmed. Raw +10.9028 pp (paper +10.90), t(22.61) = 8.1751 (paper 8.18), p = 3.353e-8 (paper 3.4e-8), 95% CI [+8.14, +13.66] pp
- Table 2, Retrain 2 typing-forgiven: +5.6468 pp (paper +5.65), t(23.03) = 4.4309, p = 1.918e-4 (paper 1.9e-4), 95% CI [+3.01, +8.28] pp. Typing 2.050 (paper 2.1)
- Table 2, Retrain 3 (llama3-forged-rep3 vs ctl-null3): n=15, M=.6060, SD=.0351 confirmed. Raw +8.7733 pp (paper +8.77), t(27.01) = 6.1627 (paper 6.16), p = 1.377e-6 (paper 1.4e-6), 95% CI [+5.85, +11.69] pp
- Table 2, Retrain 3 typing-forgiven: +3.6567 pp (paper +3.66), t(27.99) = 2.7622, p = .01003 (paper .010), 95% CI [+0.95, +6.37] pp. Typing 2.600 (paper 2.6)
- Table 2 Note, "The nulls record 10.2-11.1 typing failures per 155-generation replicate": ctl-null 10.200, ctl-null3 10.533, llama3-forged-null-rep 11.100 — range is exactly 10.2 to 11.1. n_tasks=31 x n_samples=5 = 155 generations on every row of every arm
- Welch tests on typing_nameerror_fails_total, retrains vs their nulls: t(16.2) = -8.695, t(16.0) = -8.797, t(18.4) = -13.807 (paper -8.7, -8.8, -13.8); p = 1.71e-7, 1.58e-7, 3.77e-11, all below the claimed 2e-7
- Percentage share of raw gain accounted for by the typing counter, (raw-forgiven)/raw: 54.17%, 48.21%, 58.32% — paper's "54%, 48%, and 58%" confirmed, and the Abstract's "48-58%" band is right
- Cross-check against the n=40 same-hardware overnight null (llama3-forged-null-rep): retrain 1 raw +10.1125 pp (paper +10.11), retrain 2 raw +11.4038 pp (paper +11.40); forgiven +4.3230 pp (paper +4.32) and +5.5650 pp (paper +5.56). Shares 57.2% and 51.2%, still inside the 48-58% band, so the decomposition genuinely does not depend on which null is used
- Positive control (llama3-forged-poscontrol vs llama3-forged-poscontrol-null, n=9 each): .6179 vs .5039, +11.3978 pp, t(15.89) = 6.7554 (paper +11.40, t(15.9) = 6.76); forgiven .6186 vs .5986, +2.0078 pp, p = .2275 (paper +2.01, p = .23); typing 0.111 vs 14.667 per replicate (paper 0.1 vs 14.7). Typing share of its raw gain = 82.4%
- Pairwise retrain differences on raw pass@1: -1.291, -0.280, +1.012 pp (paper -1.29, -0.28, +1.01), p = .155, .803, .367 — all non-significant as claimed
- Per-task raw deltas for the four typing tasks: ace_oss_11023 +56.0/+50.5/+42.7, ace_oss_23069 +28.5/+34.5/+26.7, ace_oss_4327 +38.7/+40.2/+38.7, ace_oss_23132 +22.7/+19.7/+14.7 — matches the paper's +56/+50/+43, +29/+35/+27, +39/+40/+39, +23/+20/+15
- Per-task forgiven deltas for those four: 0/0/0, 0/0/0, -2.7/-1.2/-1.3, -1.3/-4.3/0.0 — matches the paper's "0, 0, -3/-1/-1, and -1/-4/0"
- Per-task null typing rates "53%, 54%, 40%, and 22%" reproduce exactly when the three null arms are pooled: 52.9%, 54.0%, 40.3%, 22.0% (185/350, 189/350, 141/350, 77/350)
- Construct-valid tasks: ace_oss_17851 forgiven +44.0/+44.0/+38.7 (paper +44/+44/+39), ace_oss_24748 +45.0/+44.0/+33.3 (paper +45/+44/+33), ace_oss_3243 forgiven -24.8/-22.3/-30.7 (paper -25/-22/-31)
- Appendix E Table E1: every one of the 31 rows x 9 delta columns I spot-recomputed matches, and the entire Mean/SD footer row reproduces — +9.61/17.76, +10.90/16.46, +8.77/15.12, +4.40/14.12, +5.65/13.40, +3.66/12.69, +0.52/9.05, +11.40/33.36, +2.01/26.50
- "Fifteen tasks move up and three move down under all three adapters (twelve and three on the forgiven metric)": computed 15 up / 3 down raw, 12 up / 3 down forgiven
- Internal consistency of the artifact: for all 334 rows, aggregate_if_typing_imported['pass@1'] - aggregate['pass@1'] equals typing_nameerror_fails_total/155 to within 1e-4 (the 4-dp storage rounding), so the forgiven metric is exactly the raw metric plus the typing counter
- Table 2 Note "All arms carry an identical four-file verifier fingerprint": all 221 rows across the eight Table 2 arms carry a byte-identical verifier record (forge.py 13d52d9c..., screen_tasks.py 3dfc4e9c..., task_bank.py 15770d4d..., data/screen_results.jsonl cd83ba8a..., python 3.11.9), and identical sampler records (5 draws, no greedy anchor, temp 0.8). The positive-control pair differs (forge.py f22eede7..., python 3.12.10) exactly as the amendment discloses
- Abstract "three adapters that beat their same-session nulls by +8.8 to +10.9 pp": computed +8.77, +9.61, +10.90
### lens 2
- Arm→null mapping recovered from data/ruler_noise.jsonl by model name + timestamp: R1=llama3-forged-rep1 & R2=llama3-forged-rep2 vs ctl-null (n=15, 2026-08-05 13:04); R3=llama3-forged-rep3 vs ctl-null3 (n=15); OrigA=llama3-forged-rep (n=41) vs llama3-forged-null-rep (n=40); OrigB=ctl-his (n=15) vs ctl-null; PC=llama3-forged-poscontrol (n=9) vs llama3-forged-poscontrol-null (n=9). All 'sampled' arrays are length 5 for these arms (greedy-free); pooled per-task rate = sum(sampled)/(5*n_reps) reproduces each row's 'aggregate.pass@1' exactly, and +typing_nameerror_fails reproduces 'aggregate_if_typing_imported.pass@1' exactly.
- (a) Pearson r, primary metric — R1vR2 .9744 (paper .974); R1vR3 .8013 (.801); R2vR3 .7933 (.793); OrigB v R1 .1712 (.171); OrigA v R1 -.0744 (-.074); OrigA v OrigB .4669 (.467); PC v R1/R2/R3 .3820/.3809/.5613 (.382/.381/.561). All match.
- (a) Pearson r, typing-forgiven — R1vR2 .9654 (.965); R1vR3 .7034 (.703); R2vR3 .6833 (.683); OrigB v R1 .3911 (.391); OrigA v R1 .0813 (.081); OrigA v OrigB .4527 (.453); PC v R1/R2/R3 .2680/.2666/.4940 (.268/.267/.494). All match.
- (b) Split-half reliability, Spearman-Brown, splitting BOTH the arm and its null by file-order parity — R1 .9448/.9146, R2 .8884/.8208, R3 .6878/.5856 (paper .945/.915, .888/.821, .688/.586); OrigA .2092, OrigB .5188 (paper .209, .519); PC .9268/.9220 (paper .927/.922). Exact reproduction of every reliability in the Table 2b note, confirming the null is split too (subtracting the full null from both halves instead gives .971/.954/.896/.701/.536/.971 — not the paper's numbers).
- (c) Disattenuated r = r/sqrt(rel1*rel2) — R1vR2 1.064/1.114 (paper 1.06/1.11); R1vR3 0.994/0.961 (0.99/0.96); R2vR3 1.015/0.986 (1.02/0.99); OrigB v R1 0.244/0.565 (0.24/0.57); PC v R1 0.408/0.292 (0.41/0.29); PC v R2 0.420/0.306 (0.42/0.31); PC v R3 0.703/0.672 (0.70/0.67). All match. OrigA's forgiven reliability is negative (-0.118), so its disattenuated r is undefined — consistent with the paper printing an em-dash.
- (d) Permutation p, 20,000 task-label shuffles — one-sided values reproduce the paper everywhere: OrigB v R1 .181/.0158 (paper .18/.016); OrigA v R1 .649/.318 (.66/.32); OrigA v OrigB .0039/.0056 (.004/.005); PC v R1 .0173/.0748 (.017/.076); PC v R2 .0180/.0770 (.018/.079); PC v R3 .0005/.0032 (.0006/.003); retrain pairs 0/20000 except the forgiven R1vR3 and R2vR3 cells, which sit at 0-4/20000 across five seeds and so are consistent with the paper's '1 x 10^-4'.
- (e) Counts — 15 tasks strictly up and 3 strictly down under all three retrains on the primary metric; 12 up and 3 down on the typing-forgiven metric. Exactly matches 'Fifteen tasks move up and three move down under all three adapters (twelve and three on the forgiven metric)'.
- (f) Per-task delta SDs — retrains 17.76 / 16.46 / 15.12 pp (paper '15-18 pp'); positive control 33.36 pp raw (paper '33 pp') and 26.50 pp forgiven. Matches Table E1's Mean/SD row (+9.61/17.76, +10.90/16.46, +8.77/15.12, +4.40/14.12, +5.65/13.40, +3.66/12.69, +0.52/9.05, +11.40/33.36, +2.01/26.50) to the reported precision.
- (f) Positive-control negative swings -60.0 / -37.8 / -31.1 pp (ace_oss_17880, ace_oss_32606, ace_oss_34760) are exactly the three most extreme negatives, matching '-60, -38, and -31 pp'.
- Entire Table E1 (31 tasks x 10 columns) reproduces cell-for-cell from my own pooling, including the typing-artifact tasks (ace_oss_11023 +56.0/+50.5/+42.7 raw, 0/0/0 forgiven; ace_oss_23069 +28.5/+34.5/+26.7 vs 0/0/0; ace_oss_4327 +38.7/+40.2/+38.7 vs -2.7/-1.2/-1.3; ace_oss_23132 +22.7/+19.7/+14.7 vs -1.3/-4.3/0.0), the construct-valid pair (ace_oss_17851 +44.0/+44.0/+38.7 forgiven; ace_oss_24748 +45.0/+44.0/+33.3), and the consistent loss (ace_oss_3243 -24.8/-22.3/-30.7 forgiven).
- Null typing-failure rates quoted in the text for the four artifact tasks (53%, 54%, 40%, 22%) reproduce as 52.9%, 54.0%, 40.3%, 22.0% when pooled over the three session nulls, matching Table E1's percentages.
- Typing failures per 155-generation replicate: nulls 10.20 (ctl-null), 10.53 (ctl-null3), 11.10 (llama3-forged-null-rep) -> paper's '10.2-11.1'; original adapter 11.07 (session B) and 11.12 (session A) -> '11.1'; retrains 2.12, 2.05, 2.60 -> '2.1, 2.1, and 2.6'; positive-control null 14.67 -> '14.7'.
- Text claims at line 197 verified: original adapter split-half .21 (session A) / .52 (session B); its two sessions correlate at .467 with one-sided permutation p = .0039 ('.47 (p = .004)'); against retrain 1 it is -.074/.171 primary and .081/.391 forgiven, the last at one-sided p = .0158 ('p = .016').
- Text claim at line 201 verified: positive control split-half .927 ('.93'), correlations with the retrains .382/.381/.561 primary and .268/.267/.494 forgiven, forgiven mean gain +2.007 pp ('+2.0 pp').
- BLOCKED: Not blocked, but noted: 'odd versus even replicate index' is ambiguous for session A, because llama3-forged-rep has duplicated replicate numbers (26, 28, 31, 34, 37, 40) and llama3-forged-null-rep has duplicates (26, 29, 32, 35, 38) from a resumed run. Parity on the recorded 'replicate' field gives .2376; parity on file order gives .2092, which is the paper's .209 — so I used file order throughout. The choice moves the session-A reliability by ~.03 and no other arm at all.
### lens 3
- trl/trainer/dpo_trainer.py:376 is exactly `            model = get_peft_model(model, peft_config)` (grep confirms only two `get_peft_model` occurrences: the import at :70 and the call at :376). Installed trl __version__ = 0.12.2.
- trl/trainer/dpo_trainer.py:640 is exactly `        super().__init__(` (the only `super().__init__` in the file). Both 376 and 640 sit inside `DPOTrainer.__init__` (def at :217; next def is :696), so the PEFT wrap provably precedes the Trainer constructor.
- transformers/trainer.py:424 is exactly `        enable_full_determinism(self.args.seed) if self.args.full_determinism else set_seed(self.args.seed)`, preceded at :423 by the comment `# Seed must be set before instantiating the model when using model`. Installed transformers __version__ = 4.46.3. The only other set_seed is at :2081 (inside train()).
- peft/tuners/lora/layer.py:334 is exactly `                    nn.init.kaiming_uniform_(self.lora_A[adapter_name].weight, a=math.sqrt(5))`, inside `def reset_lora_parameters` (def at :328). Installed peft = 0.20.0 (peft-0.20.0.dist-info).
- PEFT passes NO generator: line 334 supplies only `a=math.sqrt(5)`. torch 2.11.0+cu128's signature is `kaiming_uniform_(tensor, a=0, mode='fan_in', nonlinearity='leaky_relu', generator=None)` — a generator kwarg exists and is left at None, so the draw comes from the default global torch generator. B is zeroed at line 339: `nn.init.zeros_(self.lora_B[adapter_name].weight)`.
- train_native.py: `grep -n -i 'seed'` returns ZERO matches in the entire file (no set_seed / manual_seed / seed of any kind). config.py, which it imports, also has zero matches.
- train_native.py:197-198 `trainer = DPOTrainer(model=model, args=cfg, train_dataset=ds, processing_class=tok, peft_config=peft_cfg)` — peft_config is passed, triggering the 376-before-640 path.
- TRL 0.12.2's SFTTrainer shows the same order: get_peft_model at sft_trainer.py:283/285, super().__init__ at :408.
- dpo_adapter_poscontrol/adapter_model.safetensors: 448 tensors total = 224 lora_A + 224 lora_B; lora_A dtype F32; shapes 192 × (16, 4096) and 32 × (16, 14336). Confirms the '224 draws of lora_A'.
- Init-box math: for kaiming_uniform_(a=sqrt(5), mode=fan_in, leaky_relu), gain = sqrt(2/6), bound = sqrt(3)*gain/sqrt(fan_in) = 1/sqrt(fan_in) exactly, and init sd = 1/sqrt(3*fan_in) = 0.00902110 for fan_in 4096 — confirms the paper's '≈1/√fan_in' box and '1/√(3·4096) ≈ 0.009'.
- In-box fraction under PER-TENSOR averaging (unweighted mean of 224 per-tensor fractions) = 0.954767 = 95.477% → matches paper's 95.5%. (Pooled-over-entries value differs; see findings.)
- Std ratio under PER-TENSOR averaging = 1.02272 (population sd) / 1.02273 (sample sd) → matches paper's 1.02.
- Kurtosis (non-excess m4/m2^2) under PER-TENSOR averaging = 2.05340 → matches paper's 2.05. (Fragile; see findings.)
- warmup_steps = TrainingArguments.get_warmup_steps = ceil(57 * 0.1) = 6 — confirms 'six warm-up steps'.
- Adam worst-case coordinate factor (1-β1)/√(1-β2) with β1=0.9, β2=0.999 = 3.162278 — confirms the paper's 3.16.
- Typical-case relative movement bound = Σlr / 0.0090211 = 1.4250e-4 / 0.0090211 = 1.5796% → rounds to the paper's '1.6% (typical)'.
- 16/4096 = 0.00390625 → 0.0039 confirmed; 16/14336 = 0.00111607 → 0.0011 confirmed; 7-module unweighted average (6×4096 + 1×14336) = 0.00350765 → 0.0035 confirmed (identical to the 224-tensor entry-count-weighted average, since 192:32 = 6:1).
- Simulation ΔW_i = G A_i^T A_i with common Gaussian G and independent uniform A_i (bound 1/√d_in), two independent implementations (dense matmul, 200 trials; and a trace-identity formulation, 200 trials): d_in=4096 → mean cosine 0.003904 / 0.003851, sd 0.00044 / 0.00039; d_in=14336 → mean 0.001134 / 0.001105, sd 0.00016 / 0.00017. Reproduces the paper's '0.0039 ± 0.0005 and 0.0011 ± 0.0002' (my sd on the 4096 case is 0.0004 rather than 0.0005, a simulation-setup detail: d_out=1024 gives 0.00053).
- The r/d_in derivation is robust to the exact model: true rank-16 orthogonal projectors P_i = A_i^T(A_iA_i^T)^{-1}A_i give mean cosine 0.003953 ± 0.00041 at d_in=4096, and the result is insensitive to d_out (0.00386 / 0.00390 / 0.00403 at d_out = 1024 / 4096 / 14336) and to uniform-vs-Gaussian A (0.00393).
- Positive-control ‖ΔW‖_F = ‖BA‖·α/r per layer: mean 0.5275 over 224 layers (median 0.4866) — matches the paper's '0.53' in 'Its update is about 26× the retrains' in Frobenius norm per layer (0.53 versus 0.02).'
- dpo_adapter_poscontrol/run_meta.json records lr 0.0002 (= 40× 5e-6) and epochs 3.0, and adapter_config.json records r=16, lora_alpha=32, lora_dropout=0.05, the same seven target modules, init_lora_weights=true, peft_version 0.20.0 — all consistent with the positive-control description. 918/16 = 57.375 → 57 steps/epoch × 3 = 171, matching 'three full epochs over 918 pairs (171 optimizer steps)'.
- BLOCKED: Cannot recompute the cross-run measured cosine '0.0042 (SD 0.0044, minimum −0.0087)', the lora_A mean cosine −0.0005, or the '0 of 224 bitwise identical' claim. The only adapter safetensors in the repo are C:\Users\tmcuzzort\srlm-forge\dpo_adapter_poscontrol\adapter_model.safetensors and its 50,000× amplified export copy at dpo_adapter_poscontrol\export\amplified\adapter_model.safetensors. Retrain 1/2/3 and the original null adapter are not present on this machine.
- BLOCKED: Cannot verify PEFT 0.14.0 (the version the original adapter's model card records). Only PEFT 0.20.0 is installed, so the cited lora/layer.py:334 line number applies to the replication/positive-control environment, not to the original run. The manuscript does not claim otherwise, but the line-number citation is environment-specific.
- BLOCKED: Cannot verify the claim that the same order is present in TRL's CURRENT GRPOTrainer: grpo_trainer.py does not exist in the installed trl 0.12.2, and no newer TRL is installed. (SFTTrainer and DPOTrainer were verified in 0.12.2.)
- BLOCKED: Cannot verify the retrains' 0.02 Frobenius norm per layer (the '0.53 versus 0.02' comparison) — same missing-artifact reason as above; only the 0.53 side was re-derived.
### lens 4
- bitsandbytes installed version is 0.50.1 (C:\Users\tmcuzzort\srlm-forge\.venv-train\Lib\site-packages\bitsandbytes\, dist-info METADATA Version: 0.50.1) - matches the paper's 'bitsandbytes 0.50.1'.
- optim/optimizer.py:524 is verbatim `            blocksize = 256`, inside `Optimizer2State.init_state` (def at line 492, class Optimizer2State at line 403) - this IS the optimizer-state block size, and adamw_bnb_8bit/AdamW8bit uses Optimizer2State. A second identical line is at :691 in Optimizer1State.init_state.
- Warmup = 18 steps confirmed independently: math.ceil(0.1*171)=18, and the run's own log (training_poscontrol.log) records learning_rate 5.555555555555556e-05 at step 5 = 2e-4*(5/18), 1.111e-04 at step 10, and 1.999156886888064e-04 at step 20, which my reconstruction of transformers' cosine scheduler reproduces bit-for-bit.
- The adapter has 448 tensors: 224 lora_A and 224 lora_B (32 layers x 7 modules). lora_A dtype float32, down_proj lora_A shape (16, 14336).
- Exactly ONE lora_A tensor contains elements above (init bound + displacement bound), and it is base_model.model.model.layers.1.mlp.down_proj.lora_A.weight - true at the paper's bound 0.0547 AND at my recomputed 0.054036. Next-closest tensor (layers.0.self_attn.v_proj.lora_A, max|a|=0.0593) reaches only 0.85x its threshold, so the result is robust.
- At the paper's own stated bound (0.00835191 + 0.0547 = 0.0630519) the scan returns exactly 169 elements, column range [2304, 2558], 93 distinct columns, all 16 rank rows - every one of those four figures reproduces exactly.
- Column range endpoint: max violating column is 2558 at every threshold tested, so '[2304, 2558]' is the correct observed range. v3 of the manuscript writes [2304, 2558] in both places it appears (lines 344 and 781); '2559' occurs zero times in this version, so the endpoint inconsistency flagged in my brief is NOT present in v3.
- Largest element |a| = 0.175030 (row 9, col 2474), which is also the tensor's global max. 0.175030 / (1/sqrt(14336) = 0.0083519) = 20.96 -> 'twenty-one times' confirmed; 0.175030 / 0.0547 = 3.20 -> 'three times the worst-case Adam displacement' confirmed; init bound 1/sqrt(14336) = 0.008352 -> '0.0084' confirmed.
- Mean per-column max |a| = 0.05769 inside the 256-wide window [2304,2559] (0.05773 over [2304,2558]) and 0.00881 outside -> paper's '0.058 inside vs 0.009 outside' confirmed.
- The layer's lora_B stays well inside its bound: max |B| for layers.1.mlp.down_proj.lora_B = 0.005151, i.e. 9.4% of the 0.0547 displacement bound (B initializes to exactly 0). Zero elements exceed.
- Block alignment is exact: flattening the (16,14336) row-major tensor into 256-element blocks, the 169 (or 176) violating elements occupy exactly 16 distinct blocks - flat block ids 9, 65, 121, ... 849, i.e. every block index congruent to 9 mod 56 (14336/256 = 56 blocks per row), one per rank row. This is a very strong confirmation of the block-localization claim.
- Massive activation confirmed exactly: hooking the input to model.model.layers[1].mlp.down_proj of the cached unsloth/llama-3-8b-Instruct-bnb-4bit (bf16, CUDA), channel 2427 carries |activation| = 386.0 and the next-largest channel (198) carries 46.25. These two values are invariant across all five prompts I tried (they occur at the BOS token), matching the paper's '386' and '46' to the digit.
- Layers 0 and 2 have no comparable channel: layer 0 max per-channel activation = 6.53 (ch 491) on the code prompt and 3.48 on a second prompt; layer 2 max = 1.07 (ch 10032) and 0.72. Channel 2427 itself ranks 13,422nd in layer 0 and 8,976th in layer 2. Confirmed.
- Median activation ~0.013: I get median-of-per-channel-max-over-tokens = 0.0152 for the 35-token chat code prompt, 0.0120 for a 19-token one, 0.0189 for a 64-token one. The statistic is prompt-length dependent and 0.013 sits inside the reproducible range, so I treat the claim as confirmed in magnitude (I could not pin the exact prompt used).
- Materiality: layer-1 down_proj ||dW||_F^2 = 1.00409 (dW = (alpha/r=2)*B@A, float64). Columns [2304,2559] hold 70.415% of it -> '70.4%' confirmed. Uniform share 256/14336 = 1.786% -> '1.8%' confirmed. Whole-adapter total (sum over all 224 layers) = 75.483, so the window is 0.9367% of it -> '0.94%' confirmed. (Using the 255-wide [2304,2558] instead gives 70.395% / 1.779% / 0.9364% - same rounded values.)
- Adapter delta at that layer = 0.58% of the base layer output: ||delta||_F / ||down_proj(h)||_F = 0.5823% on the 35-token code prompt (0.6038% on a second prompt), with delta = 2*(h@A^T)@B^T. Confirmed.
- 'the layer's entire adapter contribution is channel 2427's' confirmed: ||delta computed from channel 2427 alone|| / ||full delta|| = 113% (the >100% reflects partial cancellation by the other 14,335 channels); the 256-column window alone gives 113%.
- BLOCKED: Could not identify the exact prompt used for the activation measurement, so the 'median of 0.013' figure is confirmed only as being inside the reproducible prompt-dependent range (0.012-0.019). The 386/46 figures are prompt-invariant and match exactly.
- BLOCKED: Did not run the ablation (adamw_torch vs adamw_bnb_8bit) that would test the causal mechanism - the paper itself lists this as unrun (F3 in Planned Experiments), so nothing to verify.
### lens 5
- Corpus size: data/dpo_pairs_capped.jsonl has exactly 918 JSON lines; sha256 = 85fc0bdc23bf36701db851dd01dcd4b1d5f02e610ccdfc668908cc32b4655ae6, matching the paper's 'SHA-256 85fc0bdc...' and data/dataset_verification.json.
- '1,836 halves' arithmetic CONFIRMED: 918 x 2 = 1,836; my iterator visited exactly 1,836 chosen/rejected strings.
- The strong corpus claim CONFIRMED in full: across all 1,836 halves the substring 'import' occurs 0 times; 0 lines begin (at any indentation) with 'import ' or 'from ... import'; 0 halves contain '->'; the 2,033 def lines resolve to 47 distinct signatures, none with an annotated parameter or return; 0 typing imports.
- '17 on a NameError (none involving a typing name)' CONFIRMED: exactly 17 rejected halves end in NameError; undefined names are expand_around_center (10), lru_cache (4), stack (2), itertools (1) - none is a typing generic.
- 'a larger bank of 1,244' CONFIRMED: data/dpo_pairs.jsonl has 1,244 rows across 23 tids; the capped file has 13 tids, largest task share 120/918 = 13.1%.
- '1,279 unique pairs across 2,200 rows in three files' CONFIRMED: dpo_pairs.jsonl (1,244) + dpo_pairs_capped.jsonl (918) + repair_pairs.jsonl (38) = 2,200; dataset_verification.json records total_pairs 1279, violations 0.
- Table C1 (concentration) reproduced EXACTLY, all five rows, by re-implementing the AST identifier-canonicalisation of measure_bank_concentration.py: task 13/9.93/13.1%; chosen-exact 407/165.64/8.5%; chosen-structural 273/90.41/9.8%; rejected-structural 632/441.59/4.2%; contrast 767/666.41/1.4%. The last two rows are novel to the paper (not in data/bank_concentration.json) and both check out.
- 'the sentinel string appeared in 0 of 2,200 banked rows' CONFIRMED: '__PASS__' occurs 0 times in the 2,200 rows of the three pair files.
- 'the training corpus's 13 seed tasks do not [carry annotated signatures]' CONFIRMED: the 13 distinct prompts in the capped file contain no annotated signature and no 'import'.
- Timeout count: 6 rejected halves in the capped corpus carry 'timeout >8s' as their only error (7 in the 1,244-pair bank). The PRIOR LEAD's '912 error entries carry exactly one exception line and 6 are timeouts' is CONFIRMED (918 - 6 = 912 tracebacks).
- Appendix C per-task table: the Trained column reproduces bit-for-bit from data/ruler_noise.jsonl model 'llama3-forged' (all 31 k/n values). Column sums 3085/6200 = .4976, 3051/6200 = .4921, 2433/4960 = .4905, difference +0.548 pp; sign split 15 up / 14 down / 2 flat; per-task delta SD 7.927 pp - all match the body text.
- Wilson bounds in the Failure-3 responsiveness paragraph CONFIRMED: p=.19, n=200 gives lower .14167 (.1417); p=.84, n=200 gives upper .88432 (.8843).
- All Table 2 effects reproduce from data/ruler_noise.jsonl: rep1 .6032 - ctl-null .5071 = +9.61; rep2 .6161 - .5071 = +10.90; rep3 .6060 - ctl-null3 .5183 = +8.77; forgiven +4.40 / +5.65 / +3.66; against session A's null: +10.11 / +11.40 raw and +4.32 / +5.57 forgiven.
- Typing failures per replicate CONFIRMED: llama3-forged-rep 11.12, ctl-his 11.07, rep1 2.12, rep2 2.05, rep3 2.60; nulls 11.10 / 10.20 / 10.53 (paper: '10.2-11.1'); poscontrol 0.11 vs its null 14.67.
- Table E1 column means/SDs recomputed from its own 31 rows: +9.60/17.76, +10.89/16.46, +8.79/15.12, +4.39/14.12, +5.64/13.40, +3.66/12.69, +0.52/9.05, +11.39/33.35, +2.01/26.49 - all match to display rounding.
- Table 2b correlations recomputed from Table E1: raw .974/.801/.793 and forgiven .965/.703/.683; positive control .382/.381/.561 and .268/.267/.494; every disattenuated value (1.06, 0.99, 1.02, 1.11, 0.96, 0.99, 0.24, 0.57, 0.41, 0.42, 0.70, 0.29, 0.31, 0.67) recomputes from the stated split-half reliabilities.
- Table E1 'Typing' column reproduces EXACTLY by pooling the three null arms (llama3-forged-null-rep n=40, ctl-null n=15, ctl-null3 n=15): 52.9, 54.0, 40.3, 22.0, 20.3, 11.1, 3.4, 2.9, 2.9, 2.6, 1.7, 1.7 percent.
- Failure 5 fully reproduces from ruler_noise.jsonl: base pre-pin n=10 M=.5910, post-pin n=40 M=.5150, diff +7.60 pp, t=8.22, df=24.1; base greedy .6323 (980/1550) vs sampled .5047 (3129/6200) = +12.76 pp; matched base M=.4905, SD=.0476, n=40; the -2.55 pp (greedy) and -1.42 pp (pre-pin pooling) decomposition of the .5302 -> .4905 gap is exact.
- Noise floor CONFIRMED: SD 0.0283 over the first 10 pinned base replicates (M=.5168); greedy-free characterisation SD 0.0476 over the 40 matched base rows.
- Appendix A Welch arithmetic CONFIRMED: SE = sqrt(.041526^2/40 + .032727^2/40) = .0083599; t = .0054825/.0083599 = 0.6558; Welch-Satterthwaite df = 73.96 (74.0); 95% CI [-1.118, +2.214] pp. Variance ratio (.0415/.0327)^2 = 1.611 (F(39,39) = 1.61).
- Sizing CONFIRMED: 0.0476 * sqrt(2/40) = 1.064 pp SE; 2.8 * 1.064 = 2.98 pp MDE; 2.8 * 0.836 = 2.34 pp achieved sensitivity.
- Typing-decomposition percentages CONFIRMED: (9.61-4.40)/9.61 = 54.2%, (10.90-5.65)/10.90 = 48.2%, (8.77-3.66)/8.77 = 58.3% - the paper's '54%, 48%, and 58%' and the abstract's '48-58%'.
- Independent-counter arithmetic CONFIRMED: trained 463/6200 = 7.47% (matches llama3-forged's 11.575/replicate x 40); 515/6200 = 8.31%; the 0.84 pp gap equals the raw (+0.55) minus forgiven (-0.29) difference exactly.
- Chi-square p-values in 'The Arms Were Different Models' CONFIRMED from Appendix C counts: ace_oss_32606 67/200 vs 101/200 gives chi2 = 11.86, p = .00057; ace_oss_23132 123/200 vs 153/200 gives chi2 = 10.52, p = .00118.
- Limitation 10 CONFIRMED exactly: llama3-forged-rep has 41 rows across 35 distinct replicate ids; llama3-forged-null-rep has 40 rows across 35.
- Failure 4 CRLF hashes CONFIRMED against the live bytes: task_bank.py on disk is 1,485 bytes with CRLF endings and hashes to 15770d4d658308d6af19ad9bae2ff8f9dac70161ae6329a2872d493bfb72d8b3; LF-normalised it hashes to b28a47cb95f1e3488b6e14213032d103ce37646ee1617c34a55700d0430bc736. Both digests in the manuscript are correct.
- Positive-control fingerprints CONFIRMED: forge.py currently hashes to f22eede7de2e37b9abe1cb970ac61d220ec55d9eff8e021ce8451c31238b28b2 (the 'post-Failure-6' hash) while data/prereg_positive_control.json names 13d52d9c0045755... - exactly as the manuscript and the amendment file state. The positive-control prereg does carry a four-file fingerprint.
- Positive-control interim figures CONFIRMED from ruler_noise.jsonl: llama3-forged-poscontrol n=9 M=.6179 vs llama3-forged-poscontrol-null M=.5039 (+11.40 pp); forgiven .6186 vs .5986 (+2.01 pp).
- Step/warm-up arithmetic CONFIRMED: 918/16 = 57.4 (57 or 58 depending on drop_last); 3 x 57 = 171; ceil(0.1 x 57) = 6 warm-up steps and ceil(0.1 x 171) = 18; 32 layers x 7 modules = 224 lora_A (+224 lora_B = 448 tensors).
- Sum-of-learning-rates check CONFIRMED: 6-step linear warm-up plus 51-step cosine decay at peak 5e-6 sums to ~1.45e-4 (paper: 1.47e-4); 1.47e-4 / 0.00902 = 1.6% typical and x3.16 = 5.2% worst case, as stated; 1/sqrt(3*4096) = 0.00902 and 1/sqrt(14336) = 0.00835.
- BLOCKED: The primary comparison's null arm (Table 1, n = 40) could not be re-derived from any retained artifact. data/ruler_noise.jsonl (334 rows, both working-tree and git HEAD versions) contains only 5 rows for model 'llama3-forged-null' (the arm name the preregistration fixes), replicate ids 1-5, totalling 775 draws and M = .5097. Consequently Table 1's null M = .4921 / SD = .0327, the 515/6,200 typing count, the null typing-forgiven mean .5752, and Appendix C's entire Null column could not be checked against raw rows. (Reported as a finding as well, since Appendix C asserts it was generated from that file.)
- BLOCKED: dpo_adapter_native/ (the adapter under test, listed in Appendix B as a retained artifact) is not present in this working copy, so no weight-level claim about the original adapter could be checked. Only dpo_adapter_poscontrol/ is present.
- BLOCKED: The 8-bit-optimizer bound '3.16 x sum(lr) = 0.0547' for the positive control could not be reconciled to better than ~15% from the stated schedule (I get sum(lr) ~ 0.017 for 171 steps at 2e-4 with 18 warm-up steps, giving 3.16*sum(lr) ~ 0.054 BEFORE adding the 0.0084 initialization box, i.e. ~0.062 total). Reading the tensors would be needed to settle whether the paper's 0.0547 includes the box; not reported as a finding because the ambiguity is in the prose, not demonstrably in the number.

## DISCREPANCIES with referee verdicts
### D1 [minor/high] lens 1 — referee: PENDING
- quote: ""Typing" is the share of null-arm draws on that task whose only failure was a `NameError` on an un-imported `typing` name, pooled over the three null arms (12,090 draws)."
- location: Appendix E, note under Table E1 (~line 725)
- paper says: 12,090 draws
- computed: 10,850 draws (350 per task x 31 tasks)
- method: data/ruler_noise.jsonl, task_set 'ruler_v2_null'. Summed per_task[]['n'] over the three null arms the Typing column actually comes from: ctl-null (15 replicates) + ctl-null3 (15) + llama3-forged-null-rep (40) = 70 replicates x 31 tasks x 5 draws = 10,850. Confirmed this is the right pooling because every value in the Typing column reproduces from it exactly (11023 = 185/350 = 52.9%, 23069 = 189/350 = 54.0%, 4327 = 141/350 = 40.3%, 23132 = 77/350 = 22.0%, 17851 = 6/350 = 1.7%, 3243 = 39/350 = 11.1%). No combination of null arms in the file gives 12,090: adding llama3-forged-null (n=5) gives 11,625, adding llama3-forged-poscontrol-null (n=9) gives 12,245, all five null arms give 13,020.

### D2 [minor/high] lens 1 — referee: UNCERTAIN
- quote: "the original adapter fails 11.1 times — identical to null, in both sessions"
- location: Results, 'The Typing-Import Decomposition' (~line 153)
- paper says: identical to null in both sessions
- computed: session A yes (11.122 vs 11.100); session B no (11.067 vs 10.200, a +0.87 gap, +8.5%)
- method: Mean of typing_nameerror_fails_total per arm from data/ruler_noise.jsonl: llama3-forged-rep 11.1220 vs llama3-forged-null-rep 11.1000; ctl-his 11.0667 vs ctl-null 10.2000. Welch on session B: t(27.0) = 0.749, p = .46, 95% CI [-1.51, +3.24]. The sentence immediately preceding it in the same paragraph states the nulls span 10.2-11.1, so 'the adapter fails 11.1, identical to null' cannot hold for the session-B null of 10.2. The supportable claim is 'statistically indistinguishable from null' (p = .46), not 'identical'.
- referee reasoning: I reproduce the challenger's arithmetic exactly with my own script: mean typing_nameerror_fails_total is llama3-forged-rep 11.1220 (n=41) vs llama3-forged-null-rep 11.1000 (n=40), diff +0.0220, Welch t=0.039, df=72.1, p=.969; ctl-his 11.0667 (n=15) vs ctl-null 10.2000 (n=15), diff +0.8667, Welch t=0.749, df=27.0, p=.4605, 95% CI [-1.508, +3.242]. I also verified every row's typing_nameerror_fails_total equals the sum of its per_task typing counts, so the field is internally consistent. But every NUMBER the paper states here is correct: the adapter is 11.1 in both sessions (11.122 and 11.067 both round to 11.1, as Table 2 reports), and the three Table 2 nulls really do span 10.2-11.1 (ctl-null 10.200, ctl-null3 10.533, llama3-forged-null-rep 11.100). The dispute is entirely over the word 'identical'. Reading A (the challenger's): 'identical to [its same-session] null' - false in session B, where the same-session null is 10.2. Reading B: 'the original adapter fails 11.1 times in both sessions, which is identical to null [the 10.2-11.1 band just defined in the same sentence]' - true, and exactly true for session A where the null is 11.100. I used Reading B as the more natural parse because the null is introduced one clause earlier as a range, and because Table 2 prints both the 11.1 adapter values and the 10.2-11.1 null range so no reader is misled. The substantive claim - that the original adapter did not learn the import - is fully supported (p=.97 and p=.46). This is a wording tightening ('statistically indistinguishable from null'), not a computed discrepancy, so I decline to confirm it as a numerical error.

### D3 [cosmetic/high] lens 1 — referee: CONFIRMED
- quote: "the construct-valid gain is +3.7 to +5.7 pp, still significant in every retrain"
- location: Abstract (~line 18); repeated in Results, 'The Typing-Import Decomposition' as "at about half its stated size: +3.7 to +5.7 pp" (~line 157)
- paper says: +3.7 to +5.7 pp
- computed: +3.66 to +5.65 pp, i.e. +3.7 to +5.6 pp at one decimal
- method: Typing-forgiven Welch deltas vs same-session nulls, from aggregate_if_typing_imported['pass@1'] in data/ruler_noise.jsonl: retrain 3 = 3.656667 pp, retrain 1 = 4.404833 pp, retrain 2 = 5.646833 pp. 5.646833 rounds to 5.6 at one decimal; the paper's 5.7 comes from double-rounding Table 2's correctly-rounded +5.65. Purely a rounding artifact - the interval is right to two decimals in Table 2 and Appendix E.
- referee reasoning: Re-derived independently with my own Welch implementation (own betacf/betai t-distribution, no scipy) from aggregate_if_typing_imported['pass@1'] against each arm's same-session null: R1 vs ctl-null = +4.404833 pp (t=3.420, df=23.9, p=.00225), R2 vs ctl-null = +5.646833 pp (t=4.431, df=23.0, p=1.92e-4), R3 vs ctl-null3 = +3.656667 pp (t=2.762, df=28.0, p=.0100). Table 2's two-decimal values (+4.40/+5.65/+3.66) are all correct, as are the 48/54/58% artifact shares (I get 54.2/48.2/58.3%). I also checked every alternative construction of the upper endpoint, none of which reaches 5.65: mean of the 31 per-task forgiven deltas for R2 = 5.6452; R2 against session A's n=40 null (the paper's own cross-check) = +5.5650. So 5.6468 -> 5.6 at one decimal under any route, and the abstract's '5.7' is a double-rounding of the correctly-rounded table value 5.65. I confirm it only because it is a genuine last-digit error, and I note its size honestly: 0.053 pp, no conclusion is affected, and the lower endpoint 3.7 is correct.
- CORRECTED VALUE: +3.7 to +5.6 pp (exact: +3.657 to +5.647 pp; the endpoints are R3 = +3.6567 and R2 = +5.6468)

### D4 [cosmetic/high] lens 1 — referee: CONFIRMED
- quote: "The construct-valid gain is concentrated on two tasks with no typing failures at all — `ace_oss_17851` (+44/+44/+39 pp) and `ace_oss_24748` (+45/+44/+33 pp)"
- location: Results, 'The Typing-Import Decomposition' (~line 153)
- paper says: no typing failures at all (both tasks)
- computed: ace_oss_24748 = 0/350 null draws (0.0%), correct; ace_oss_17851 = 6/350 null draws (1.7%), not zero
- method: Pooled per_task['typing_nameerror_fails'] over ctl-null + ctl-null3 + llama3-forged-null-rep from data/ruler_noise.jsonl: ace_oss_17851 6/350 = 1.71%, ace_oss_24748 0/350 = 0.00%. Consistent with the paper's own Table E1, which lists 1.7% for ace_oss_17851, and with the fact that 17851's raw deltas (+45.3/+45.3/+40.0) exceed its forgiven deltas (+44.0/+44.0/+38.7) by 1.3 pp - which they could not do if the task had no typing failures. Appendix E's note states the weaker and correct version ('neither of which has a material typing rate'); the body text overstates it.
- referee reasoning: Computed directly from data/ruler_noise.jsonl, pooling the three null arms that Table E1 in fact uses (llama3-forged-null-rep + ctl-null + ctl-null3): ace_oss_17851 has 6 typing_nameerror_fails in 350 draws = 1.71%, which is the 1.7% Table E1 prints; ace_oss_24748 has 0/350 = 0.00%. The per-arm split for 17851 is 4/200 (null-rep), 1/75 (ctl-null), 1/75 (ctl-null3) — the failures are present in all three nulls, not a rounding artifact. The internal tell is decisive and I reproduced it: for ace_oss_17851 the raw and forgiven deltas differ (+45.3/+45.3/+40.0 raw vs +44.0/+44.0/+38.7 forgiven), which is arithmetically impossible if the task had no typing failures, whereas for ace_oss_24748 raw and forgiven are identical (+45.0/+45.0, +44.0/+44.0, +33.3/+33.3). The parenthetical figures the Results quotes for 17851 (+44/+44/+39) are themselves the forgiven column, so the sentence quotes typing-forgiven numbers for a task it simultaneously says has no typing failures. Appendix E's note states the correct, weaker version.
- CORRECTED VALUE: Two tasks with negligible typing failures — ace_oss_24748 (0.0% of null draws) and ace_oss_17851 (1.7%) — i.e., Appendix E's own wording, 'neither of which has a material typing rate'

### D5 [minor/medium] lens 1 — referee: REFUTED
- quote: "with the arms interleaved replicate by replicate rather than run as sequential blocks"
- location: Results, 'The Null Replicated; The Training Did Not' (~line 125); the arms carried into Table 2 row 1 and used as the n=40 cross-check null
- paper says: n = 41 trained / n = 40 null, interleaved replicate by replicate
- computed: row counts are 41 and 40 as stated, but the replicate index collides: 41 trained rows carry only 35 distinct replicate ids and 40 null rows carry only 35, with ids 27/30/33/36/39 absent from the trained arm and 28/31/34/37/40 absent from the null arm
- method: Counted r['replicate'] per model in data/ruler_noise.jsonl. llama3-forged-rep: ids 26, 28, 31, 34, 37, 40 each appear twice (distinct timestamps and distinct scores, e.g. replicate 26 at 23:13:58 raw .4968 and at 23:15:31 raw .5290), ids 27, 30, 33, 36, 39 never appear. llama3-forged-null-rep: ids 26, 29, 32, 35, 38 each appear twice, ids 28, 31, 34, 37, 40 never appear. This is an id-assignment defect in the resumable driver, not duplicated data - every row is a distinct evaluation, so no Table 2 or Appendix E number changes (I recomputed all of them from the rows as banked and they match). It does mean the two arms are not paired round-for-round in the way the interleaving description implies, and it is worth a sentence since the paper elsewhere reports n as a preregistered fixed quantity.
- referee reasoning: I reproduce the challenger's raw observation exactly - llama3-forged-rep has 41 rows carrying 35 distinct replicate ids (26, 28, 31, 34, 37, 40 twice each; 27, 30, 33, 36, 39 absent) and llama3-forged-null-rep has 40 rows carrying 35 distinct ids (26, 29, 32, 35, 38 twice each; 28, 31, 34, 37, 40 absent) - but it refutes nothing the manuscript says. (1) The manuscript's stated quantities are correct: 41 trained rows and 40 null rows, M=.5018/SD=.0401 and M=.5021/SD=.0396, which I recomputed. (2) I went further than the challenger and SHA-256'd every row's per_task payload across the entire 334-row file: zero duplicated payloads. Even the two llama3-forged-null-rep rows 13 s apart that share pass@1 = .4968 have different per-task vectors and different typing totals (6 vs 14), so nothing is double-banked and no n is inflated. (3) The interleaving claim itself holds against the timestamps: rows 1-25 are strict trained/null alternation at ~2 min cadence, and after 23:13 the resumable driver switches to alternating pairs (T,T,N,N) with a longest same-arm run of 3 rows - never anything resembling '41 trained then 40 null', which is what 'sequential blocks' denies. (4) Nothing in the paper claims unique replicate ids or round-for-round pairing; the preregistration fixes unpaired Welch with each arm retaining its own variance, and Table 2 is computed that way, so 'paired round-for-round' is a property the manuscript never asserts and never uses. The challenger concedes 'no Table 2 or Appendix E number changes', which is the whole test of a manuscript-vs-artifact discrepancy. This is an artifact-provenance curiosity worth a line in the repository's own notes, not an error in the paper.

### D6 [minor/high] lens 2 — referee: CONFIRMED
- quote: "pooled over the three null arms (12,090 draws)"
- location: Appendix E, preamble to Table E1 (~line 725)
- paper says: 12,090 draws
- computed: 10,850 draws
- method: The three same-session null arms in ruler_noise.jsonl are ctl-null (15 replicates), ctl-null3 (15) and llama3-forged-null-rep (40) = 70 replicates. Each replicate has 31 tasks x 5 sampled draws = 155 generations, so 70 x 155 = 10,850. Summing len(t['sampled']) over every per_task entry of those three arms gives exactly 10,850. Pooling those same three arms reproduces the quoted typing percentages exactly (52.9 / 54.0 / 40.3 / 22.0), so the arm set is right and only the draw count is wrong. I enumerated all 31 subsets of the five null-named arms in the file; none totals 12,090 draws (the closest, adding llama3-forged-poscontrol-null, gives 12,245). 12,090 corresponds to 78 replicates, 8 more than exist.
- referee reasoning: I searched all 31 subsets of the five null arms in data/ruler_noise.jsonl for the pooling that reproduces Table E1's printed Typing column, comparing each pooled per-task rate (sum of typing_nameerror_fails / sum of n) to the printed value at one decimal on all 31 tasks. Exactly one subset matches on all 31 with zero mismatches: llama3-forged-null-rep + ctl-null + ctl-null3. Summing those arms' per_task 'n' fields directly gives 10,850 draws (6,200 + 2,325 + 2,325 = 70 replicates × 155). 12,090 would require 78 replicates and no subset of the null arms has 78 (70, 75, 79 and 84 are the reachable totals; adding the 9-replicate poscontrol null gives 12,245 and breaks the rate match). The rates themselves are all correct — I reproduced every one of the twelve non-zero values (52.9, 54.0, 40.3, 22.0, 20.3, 11.1, 3.4, 2.9, 2.9, 2.6, 1.7, 1.7) exactly; only the denominator is wrong.
- CORRECTED VALUE: 10,850 draws (70 replicates × 155 generations: llama3-forged-null-rep 40 + ctl-null 15 + ctl-null3 15)

### D7 [minor/high] lens 2 — referee: UNCERTAIN
- quote: "with swings of +73, +64, and +62 pp on three tasks and −60, −38, and −31 pp on three others"
- location: Results, 'The High-Learning-Rate Regime Leaves the Seed-Invariant Solution' (~line 201)
- paper says: +73, +64, +62 (three largest positive swings)
- computed: the three largest positive swings are +73.3, +68.9, +64.4; four tasks exceed +62
- method: Positive-control raw delta profile (llama3-forged-poscontrol minus llama3-forged-poscontrol-null, pooled sum(sampled)/(5n) per task), sorted descending: ace_oss_633 +73.3, ace_oss_23069 +68.9, ace_oss_11739 +64.4, ace_oss_24748 +64.4, ace_oss_31439 +62.2. The quoted triple skips the second-largest swing (+68.9 on ace_oss_23069) and one of the two tasks tied at +64.4, reaching down to the fifth-ranked +62.2 instead. The paired negative list (-60.0, -37.8, -31.1) IS the true bottom three, so the sentence reads as an extremes list. All five values are present in the paper's own Table E1 PosCtl raw column, so the table and the sentence disagree about what the extremes are.
- referee reasoning: I recomputed the positive-control raw delta profile myself (llama3-forged-poscontrol minus llama3-forged-poscontrol-null, pooled sum(sampled)/draws per task; my column reproduces Table E1's PosCtl raw to 0.0 total error, mean +11.40, SD 33.36, matching the text's '33 pp'). Sorted descending: +73.333 (ace_oss_633), +68.889 (ace_oss_23069), +64.444 (ace_oss_11739), +64.444 (ace_oss_24748), +62.222 (ace_oss_31439). Sorted ascending: -60.000 (ace_oss_17880), -37.778 (ace_oss_32606), -31.111 (ace_oss_34760). So the challenger's arithmetic is exactly right: the negative triple IS the true bottom three, while the positive triple is ranks 1, 3-or-4, and 5, skipping the second-largest (+68.9). But the sentence does not say 'the three largest'; it says 'swings of +73, +64, and +62 pp on three tasks', and all three of those swings genuinely exist in the paper's own Table E1. Under the illustrative reading nothing false is asserted; under the extremes reading implied by the parallel exhaustive negative list, the triple is wrong. I used the extremes reading to grade and could not find any alternative selection rule that justifies the published triple (it is not the top-three forgiven swings, and it is not 'swings on non-typing tasks' - ace_oss_31439's forgiven delta is only +8.9, so it is typing-driven, while the skipped ace_oss_11739 has 0.0% typing). Both readings are defensible, so UNCERTAIN rather than CONFIRMED.
- CORRECTED VALUE: If extremes are intended: '+73, +69, and +64 pp on three tasks and −60, −38, and −31 pp on three others' (or 'from +73 down to +62 pp on five tasks'). If illustration is intended, add 'among others' / 'for example'.

### D8 [minor/high] lens 2 — referee: CONFIRMED
- quote: "Permutation *p* from 20,000 shuffles of task labels."
- location: Table 2b note (~line 191) and Table 2b column header
- paper says: p values reported without direction (e.g. .18, .66, .32, .016, .076)
- computed: all reported values are ONE-SIDED; two-sided equivalents are .358, .689, .660, .0329, .1432
- method: For each Table 2b pair I shuffled one 31-vector 20,000 times and counted both P(r_perm >= r_obs) (one-sided) and P(|r_perm| >= |r_obs|) (two-sided). One-sided reproduces every published cell (OrigB v R1 .181 vs .18; OrigA v R1 .649/.318 vs .66/.32; OrigA v OrigB .0039/.0056 vs .004/.005; PC v R1 .0173/.0748 vs .017/.076; PC v R2 .0180/.0770 vs .018/.079; PC v R3 .0005/.0032 vs .0006/.003). Two-sided is roughly double throughout and matches nothing. The direction is never stated, while the article's preregistered primary test is explicitly 'two-sided' (Method, Preregistration) and Welch's test is two-sided throughout Tables 1 and 2 — so an undeclared switch to a one-sided test occurs only in Table 2b. No conclusion flips (OrigB v R1 forgiven is .0329 two-sided, still < .05; PC v R1/R2 forgiven were already non-significant), but the reported p-values are half their two-sided values.
- referee reasoning: I ran my own permutation test (200,000 shuffles, seeded, shuffling the second 31-vector and recomputing Pearson r) on every Table 2b pair, tallying BOTH tails separately. One-sided P(r_perm >= r_obs) reproduces every published cell; two-sided P(|r_perm| >= |r_obs|) reproduces none of the non-degenerate ones. My values (one-sided / two-sided vs paper): OrigB v R1 primary .1797/.3564 vs .18; OrigB v R1 forgiven .0151/.0312 vs .016; OrigA v R1 primary .6534/.6901 vs .66; OrigA v R1 forgiven .3202/.6572 vs .32; OrigA v OrigB .0041/.0081 vs .004 and .0055/.0118 vs .005; PC v R1 .0178/.0330 vs .017 and .0759/.1434 vs .076; PC v R2 .0183/.0341 vs .018 and .0769/.1467 vs .079; PC v R3 .00058/.00098 vs .0006 and .0035/.0065 vs .003. The decisive cell is OrigA v R1: with r = -.0744 the paper reports .66 (an upper-tail p, only possible one-sided; two-sided is .690) and with r = +.0813 it reports .32 (two-sided is .657). A single row therefore carries both a p above and a p below .5 for near-zero correlations of opposite sign - the signature of an undeclared one-sided upper-tail test. The paper's Preregistration section explicitly fixes 'Welch's t-test on run-level pass@1, two-sided', and Tables 1 and 2 are two-sided throughout, so Table 2b's note omits a direction change from the article's own declared convention. The individual numbers are correct as one-sided p-values; the defect is the missing declaration, and no conclusion flips (the 'significant (p = .016)' claim at line ~194 is .031 two-sided, still < .05).
- CORRECTED VALUE: Declare the direction, e.g. 'Permutation p from 20,000 shuffles of task labels, one-sided (upper tail).' If two-sided is intended to match the preregistered convention, the cells become .36 / .031, .69 / .66, .008 / .012, and .033, .034, .0010 / .143, .147, .0065.

### D9 [minor/high] lens 2 — referee: REFUTED
- quote: "Retrain 1 vs. retrain 2 | **.974** | **.965** | 1.06 / 1.11"
- location: Table 2b, row 1, and Table 2b note ('Disattenuated r = r/√(rel₁·rel₂)')
- paper says: r = .974 / .965, disattenuated 1.06 / 1.11
- computed: r = .8739 / .8489 when the shared null noise is removed the same way the reliability denominator removes it
- method: Retrain 1 and retrain 2 are both differenced against the SAME null arm (ctl-null), so their delta profiles share one null-noise vector, which inflates their correlation. The split-half reliabilities in the denominator, by contrast, are computed by splitting the null as well (odd-null vs even-null halves — that is the only variant that reproduces .945/.915/.888/.821/.688/.586/.519/.209/.927/.922), so the denominator explicitly excludes the covariance the numerator includes. Recomputing r for R1 vs R2 with independent null halves (R1 minus odd-index ctl-null replicates, R2 minus even-index ctl-null replicates) gives .8739 primary and .8489 forgiven, versus .9744/.9654 with the shared null. This asymmetry is why only this row exceeds 1.0 after disattenuation; R1vR3 and R2vR3, which use genuinely different nulls (ctl-null vs ctl-null3), give 0.99/1.02 primary and 0.96/0.99 forgiven. The abstract's 'disattenuated r ≈ 1.0 on both metrics' survives on those two independent pairs, but the 1.06 / 1.11 cell is an artifact of the shared null rather than a reliability ceiling being exceeded.
- referee reasoning: The paper defines its profiles in Appendix E as 'Per-task pass-rate change against each arm's same-session null, pooled over that arm's replicates.' Computing exactly that from data/ruler_noise.jsonl - R1 = pooled(llama3-forged-rep1) - pooled(ctl-null), R2 = pooled(llama3-forged-rep2) - pooled(ctl-null), sum(sampled)/n_draws per task, forgiven = (sum(sampled) + typing_nameerror_fails)/n_draws - I get r = .97441 primary and .96542 forgiven. Both cells are exactly right, as is every other r in Table 2b (.8013/.7034, .7933/.6833, .1712/.3911, -.0744/.0813, .4669/.4527, .3820/.3809/.5613 and .2680/.2666/.4940). The challenger did not recompute the paper's quantity; they computed a different estimator (each retrain differenced against a disjoint half of ctl-null), which the paper nowhere claims to report. That estimator is also not well defined: assigning R1 the odd null half and R2 the even gives .8739/.8489 (their figures), but the equally arbitrary swap gives .8597/.7727, so it has no canonical value. Their premise about the denominator is correct - I verified that the published reliabilities are reproduced ONLY by splitting the null as well (my split-both, Spearman-Brown values: .9448, .8884, .6878 primary and .9146, .8208, .5856 forgiven; OrigA .2092, OrigB .5188, PC .9268/.9220 - all matching .945/.888/.688/.915/.821/.586/.209/.519/.927/.922, while splitting the adapter arm only gives .971/.954/.896/.592/.701/.971, matching nothing). And the disattenuated cells follow arithmetically from the paper's own inputs: .9744/sqrt(.9448*.8884) = 1.0635 -> 1.06 and .9654/sqrt(.9146*.8208) = 1.1142 -> 1.11. So the shared-null point is a legitimate interpretive criticism of why this one row exceeds 1.0, but it is not an error in the reported r, which is correct as defined. Reporting a corrected r in that cell would make it inconsistent with every other cell in the table and with Table E1.

### D10 [minor/high] lens 2 — referee: CONFIRMED
- quote: "Replicate-resampling bootstrap intervals are in Appendix E"
- location: Table 2b note (~line 191); Appendix E begins at line 721
- paper says: bootstrap intervals located in Appendix E
- computed: no bootstrap interval appears anywhere in the manuscript
- method: Appendix E (lines 721-760) contains only Table E1, a 31-row per-task delta table with a Mean/SD footer row and a descriptive note; it reports no intervals of any kind. Grepping the whole manuscript case-insensitively for 'bootstrap' returns exactly one hit — the Table 2b note itself (line 191). The cross-reference has no referent, so the note's further claim that those intervals 'are biased toward zero by resampling noise and are reported as conservative bounds' describes numbers the reader cannot see.
- referee reasoning: I verified this directly against the manuscript rather than by recomputation. Case-insensitive grep for 'bootstrap' over all 791 lines returns exactly one hit: line 191, the Table 2b note that makes the cross-reference. Grep for 'resampl' returns the same single line. Appendix E occupies lines 721 through 770 (Appendix F begins at 771) and contains only the heading, a two-sentence preamble, Table E1 - a 31-row per-task delta table with a Mean/SD footer - and a descriptive note; a scan of those lines for '[' returns zero hits, so no interval of any form appears there. The only bracketed intervals anywhere in the article are the primary Welch CI (lines 115, 588), the paired CI (line 117), and the TOST interval (lines 119, 608), none of which is a replicate-resampling bootstrap and none of which is in Appendix E. The cross-reference has no referent, and the note's follow-on clause ('they are biased toward zero by resampling noise and are reported as conservative bounds') characterizes numbers that are not in the manuscript.
- CORRECTED VALUE: Either add the replicate-resampling bootstrap intervals to Appendix E, or delete the sentence 'Replicate-resampling bootstrap intervals are in Appendix E; they are biased toward zero by resampling noise and are reported as conservative bounds.' from the Table 2b note.

### D11 [minor/high] lens 3 — referee: CONFIRMED
- quote: "57 steps at a peak rate of 5 × 10⁻⁶ with six warm-up steps and a cosine schedule sum to Σlr = 1.47 × 10⁻⁴"
- location: Results, "Orthogonality Is Predicted, Not Anomalous" (line 171)
- paper says: Σlr = 1.47 × 10⁻⁴
- computed: Σlr = 1.4250 × 10⁻⁴ (exactly 28.5 × 5e-6); paper overstates by 3.2%
- method: Instantiated the real scheduler rather than reimplementing it: transformers.get_scheduler('cosine', optimizer=SGD(lr=5e-6), num_warmup_steps=TrainingArguments.get_warmup_steps(57)=ceil(57*0.1)=6, num_training_steps=57), then summed the lr actually seen by each of the 57 optimizer steps (read param_groups lr, opt.step(), sch.step()). Sequence starts 0.000e+00, 8.333e-07, ..., peaks at 5.000e-06 on step 6, ends 4.742e-09. Sum = 1.425000e-04. This is exact and indexing-independent: because lambda(0)=0 and lambda(57)=0, summing lambda(1..57) gives the identical 1.425e-4. Analytically Σlambda = W/2 + (T-W)/2 = T/2 = 28.5 for any warmup W under a half-cosine to zero, so 1.47e-4 (which implies 29.4) is not reachable for T=57; T=58 gives 1.450e-4. Script: C:\Users\TMCUZZ~1\AppData\Local\Temp\claude\C--Users-tmcuzzort\cd70aff0-aa45-4d76-84a0-f483822680c9\scratchpad\lr2.py
- referee reasoning: I re-derived this without reusing the challenger's method. I wrote the cosine-with-warmup multiplier from first principles, lambda(i)=i/W for i<W and 0.5*(1+cos(pi*(i-W)/(T-W))) after, and validated it against the RAW artifact rather than against any reimplementation: poscontrol/retrain.log logs the actual learning rate at steps 5,10,...,55 of the 57-step original-recipe run, and my formula matches all 11 logged values at relative error exactly 0 (step 5 logged 4.166666666667e-06 = 5e-6*5/6, confirming W=6; step 10 logged 4.924492340088e-06, confirming the half-cosine with 51 decay steps; step 55 logged 1.894872582016e-08). Summing over the 57 steps gives sum(lambda)=28.5 exactly, so Sigma_lr = 28.5*5e-6 = 1.4250e-04. I then stress-tested every axis the challenger could have gotten wrong: indexing (lambda(0..56) and lambda(1..57) both give 28.5, since lambda(0)=lambda(57)=0); warmup count (W=5, 6, and 7 all give 28.5, so the paper's 'six warm-up steps' is not the issue); decay shape (a linear decay also gives 28.5); and step count (T=57 -> 1.425e-4, T=58 -> 1.450e-4, T=59 -> 1.475e-4). As an independent second implementation I instantiated transformers' own get_scheduler('cosine') with TrainingArguments.get_warmup_steps(57)=6 and summed the lr actually applied at each of 57 SGD steps: 1.4250000000e-04. The paper's 1.47e-4 implies sum(lambda)=29.4, which is unreachable for T=57 under any warmup or decay variant I could construct. The paper is wrong; the error is +3.16%.
- CORRECTED VALUE: Σlr = 1.425 × 10⁻⁴ (i.e. 1.43 × 10⁻⁴ to three figures)

### D12 [minor/high] lens 3 — referee: CONFIRMED
- quote: "could have moved more than 1.6% (typical) or 5.2% (worst case) of its own magnitude"
- location: Results, "Orthogonality Is Predicted, Not Anomalous" (line 171)
- paper says: 5.2% worst case
- computed: 5.00% worst case (1.58% typical, which does round to the paper's 1.6%)
- method: Downstream of the Σlr error above. With the correct Σlr = 1.4250e-4 and element scale 1/√(3·4096) = 0.00902110: typical = 1.4250e-4/0.00902110 = 1.5796%; worst case = 3.1622776·1.4250e-4/0.00902110 = 4.9952%. The paper's own 1.47e-4 would give 1.6295% and 5.1530%, i.e. its 1.6%/5.2% are internally consistent with its (incorrect) Σlr. The direction is conservative — the true bound is tighter than stated — so the argument that A is effectively frozen is strengthened, not weakened. Fix: 'Σlr = 1.43 × 10⁻⁴ ... 1.6% (typical) or 5.0% (worst case)'.
- referee reasoning: Purely downstream of claim 1, and I recomputed it from my own Sigma_lr rather than the challenger's. Element scale 1/sqrt(3*4096) = 0.009021097956087904 (correct for PEFT's kaiming_uniform_(a=sqrt(5)) on lora_A, whose bound is sqrt(6/((1+5)*fan_in)) = 1/sqrt(fan_in), giving sd = 1/sqrt(3*fan_in)). The Adam worst-case coordinate factor is (1-beta1)/sqrt(1-beta2) = 0.1/sqrt(0.001) = 3.1622776601683795, which matches the paper's stated 3.16. With Sigma_lr = 1.4250e-4: typical = 1.4250e-4/0.0090211 = 1.5796% (rounds to the paper's 1.6%, so that figure is CORRECT and should not be changed) and worst case = 3.1622777*1.4250e-4/0.0090211 = 4.9952% -> 5.0%, not 5.2%. I verified the paper is internally consistent with its own bad input: 1.47e-4 gives 1.6295% and 5.1530%, which round to the printed 1.6% and 5.2%. Two caveats that should temper the finding but do not rescue the number: (a) the sentence is an upper bound ('could not have moved more than'), so overstating it by 0.2 pp leaves the logical claim true and the argument conservative — the true bound is tighter and the 'A is effectively frozen' conclusion is strengthened; (b) the typical figure survives rounding untouched. Only the '5.2%' is arithmetically wrong.
- CORRECTED VALUE: 1.6% (typical) or 5.0% (worst case) — only the worst-case figure changes

### D13 [minor/high] lens 3 — referee: REFUTED
- quote: "their kurtosis is 2.05 against 1.80 for a uniform and 3.00 for a Gaussian"
- location: Results, "Orthogonality Is Predicted, Not Anomalous" (line 171)
- paper says: 2.05
- computed: Per-tensor mean 2.0534 (reproduces the paper) BUT median 1.8719, and 1.8950 with a single tensor removed; entry-pooled 3.0672
- method: Loaded all 224 lora_A tensors from dpo_adapter_poscontrol/adapter_model.safetensors in float64 and computed non-excess kurtosis m4/m2². TWO aggregations: (i) unweighted mean of the 224 per-tensor kurtoses = 2.05340, which is the paper's number; (ii) pooled over all 19,922,944 entries after dividing each entry by its own tensor's init sd 1/√(3·fan_in) = 3.06719. The paper's wording ('the entries' ... kurtosis') reads as (ii), which lands on the Gaussian reference value 3.00 and would appear to contradict the 'still uniform' argument the sentence makes. Separately, (i) is not robust: the per-tensor distribution is median 1.8719, q75 1.8932, q90 1.9990, and exactly ONE of 224 tensors exceeds 3.0 — base_model.model.model.layers.1.mlp.down_proj.lora_A.weight at kurtosis 37.3657 (sd ratio 1.3310). Dropping that single tensor moves the mean to 1.8950. So '2.05, between uniform and Gaussian' is really '1.87, indistinguishable from uniform, plus one anomalous tensor' — and that tensor is layer 1 down_proj, the same massive-activation channel the manuscript's own block-localized optimizer-state failure implicates. Scripts: scratchpad\wa2.py, wa3.py, wa4.py (same scratchpad dir as above).
- referee reasoning: The paper's number is exactly right as computed, and the challenger's 'literal reading' number is a statistical artifact of their own normalization choice. My independent load of all 224 lora_A tensors in float64 gives non-excess kurtosis m4/m2^2 with an unweighted per-tensor mean of 2.053401 — the paper's 2.05, reproduced to four figures (and identical about zero vs about the mean, 2.053373 vs 2.053401, so that axis is not in play). My sampled references confirm the paper's comparison points: uniform 1.8004, Gaussian 3.0053. The challenger's entry-pooled 3.0672 comes from dividing each entry by its tensor's INITIALIZATION sd rather than by its own sd. That does not standardize the pooled sample: the realized sd ratios range from 0.998 to 1.331 across tensors, and pooling components of unequal variance mechanically inflates kurtosis. I computed the correctly standardized entry-pooled kurtosis (each entry divided by its own tensor's sd) and got 2.336794 — still between the uniform 1.80 and the Gaussian 3.00, exactly as the sentence claims. So the pooled reading does NOT land on 3.00 and does NOT contradict the 'still uniform' argument; the challenger's 3.07 is the one aggregation out of three that happens to hit the Gaussian value, and it is the least defensible of them. I did independently confirm the challenger's outlier: exactly one of 224 tensors exceeds kurtosis 3.0 — base_model.model.model.layers.1.mlp.down_proj.lora_A.weight at 37.3657 (sd ratio 1.3310, in-box 0.914) — median is 1.871872, and the mean of the other 223 is 1.895049. That is a legitimate robustness observation worth a footnote, but it does not make 2.05 wrong, and its direction favors the paper: the typical tensor at kurtosis 1.87 is even closer to the uniform initializer than 2.05 suggests, strengthening 'A barely trains'.

### D14 [minor/high] lens 3 — referee: UNCERTAIN
- quote: "95.5% of `lora_A` entries still lie inside the kaiming-uniform initialization box |a| ≤ 1/√fan_in"
- location: Results, "Orthogonality Is Predicted, Not Anomalous" (line 171); restated in Discussion (line 475) as "95.5% of its entries remain inside their initialization box"
- paper says: 95.5% of entries
- computed: 94.671% of entries (pooled); 95.477% as an unweighted per-tensor mean
- method: For each of the 224 lora_A tensors, counted |a| ≤ 1/√fan_in (bound = 0.015625 for fan_in 4096, 0.0083517 for 14336). Entry-pooled over all 19,922,944 entries: 0.946711 = 94.671%. Unweighted mean of the 224 per-tensor fractions: 0.954767 = 95.477%. The two differ because down_proj (fan_in 14336, 92.415% in-box) carries 3.5× the entries per tensor of the other six modules (fan_in 4096, 95.987% in-box) but only 1/7 of the tensor count. The paper's phrasing quantifies over ENTRIES, which gives 94.7%, not 95.5%. Either restate as 94.7% or say 'averaged over the 224 adapted layers'. Script: scratchpad\wa2.py.
- referee reasoning: I reproduce BOTH figures from my own load, so the verdict turns entirely on the reading. Counting per tensor with bound 1/sqrt(fan_in) (0.015625 at fan_in 4096, 0.0083517 at 14336): unweighted mean over the 224 tensors = 0.954767 -> 95.5%; true entry-pooled count over all 19,922,944 entries = 0.9467107 -> 94.671%. Strict/non-strict inequality is irrelevant (identical to six decimals, no entry sits exactly on the bound). Per module: down_proj 0.924153 (fan_in 14336, 32 tensors carrying 3.5x the entries each), the six fan_in-4096 modules 0.9526-0.9640. So the two aggregations differ for exactly the reason the challenger gives. Where I part company is on which reading the manuscript is using. The same sentence reports three statistics, and only the per-layer aggregation reproduces all three simultaneously: 95.477% -> 95.5%, sd ratio 1.022719 -> 1.02, kurtosis 2.053401 -> 2.05. Under entry-pooling the sentence would have to read 94.7%, 1.03, and 2.34 — none of which are printed. The paper is therefore internally consistent in aggregating over the 224 adapted layers, and the fault is the word 'entries' rather than the number 95.5. Both readings are defensible on the text alone ('95.5% of lora_A entries' does literally quantify entries, and the Discussion at line 475 repeats 'its entries'), the gap is 0.8 pp, and nothing in the argument turns on it. Reading I used for the verdict: per-layer, because it is the only one under which the sentence's own three statistics are mutually consistent.
- CORRECTED VALUE: Optional wording fix only: '95.5% of lora_A entries, averaged over the 224 adapted layers' (the entry-pooled figure is 94.7%). No number needs changing under the per-layer reading.

### D15 [cosmetic/high] lens 3 — referee: REFUTED
- quote: "the entries' standard deviation is 1.02× that of the initial uniform distribution"
- location: Results, "Orthogonality Is Predicted, Not Anomalous" (line 171)
- paper says: 1.02×
- computed: 1.0227 per-tensor mean (matches); 1.0341 entry-pooled
- method: Same load. Per-tensor: sd(tensor)/(1/√(3·fan_in)), then unweighted mean over 224 = 1.022719 (population sd) / 1.022726 (sample sd) — rounds to the paper's 1.02. Entry-pooled after normalising each entry by its tensor's init sd = 1.034074, which rounds to 1.03. Same entries-vs-layers ambiguity as the in-box figure; per-module the ratio is 1.013–1.022 for the six fan_in=4096 modules and 1.0632 for down_proj. Cosmetic on its own, but it should be made consistent with whichever aggregation the sentence adopts for the other two statistics.
- referee reasoning: The challenger concedes this one matches, and my independent computation agrees with the paper. Loading all 224 lora_A tensors in float64 and dividing each tensor's sd by its own initialization sd 1/sqrt(3*fan_in): unweighted mean over 224 tensors = 1.022719 with population sd, 1.022726 with sample sd (ddof=1) — the ddof choice is immaterial at n>=65536 — which rounds to the printed 1.02. Median 1.016778, range 0.998046 to 1.330959. The entry-pooled alternative is 1.034074 (I get the identical value two ways: entry-weighted mean of per-tensor ratios, and the sd of all 19,922,944 entries after dividing each by its tensor's init sd), which would round to 1.03. This is the same entries-vs-layers ambiguity as the in-box figure and it resolves the same way: per-layer aggregation is the only one that reproduces all three statistics in the sentence. No computed value contradicts the paper, so there is no finding here. Per-module ratios for the record: gate 1.0125, up 1.0130, o 1.0140, v 1.0153, q 1.0195, k 1.0216, down_proj 1.0632.

### D16 [minor/medium] lens 3 — referee: PENDING
- quote: "A simulation of the model (random uniform A₁, A₂; ΔWi ∝ G AiᵀAi for a common G) returns 0.0039 ± 0.0005 and 0.0011 ± 0.0002. The measured 0.0042 ± 0.0044 is that number."
- location: Results, "Orthogonality Is Predicted, Not Anomalous" (line 171)
- paper says: measured 0.0042 ± 0.0044 identified with predicted 0.0035 (7-module average) / simulated 0.0039 ± 0.0005
- computed: The model predicts a 224-layer dispersion of ≈ 0.0011, roughly 4× narrower than the reported ±0.0044; and the predicted mean 0.00351 sits ≈ 2.4 SEM below the reported 0.0042
- method: The two simulations reproduce the per-layer MEANS exactly (0.0039 and 0.0011), so the central-tendency claim holds. But the reported ±0.0044 is an SD across 224 layers, and the model's own predicted SD across those same 224 layers is only sqrt((192/224)(0.00391−0.00351)² + (32/224)(0.00112−0.00351)² + sim_var) = sqrt(9.6e−7 + (4.4e−4)²) = 0.00106 — i.e. the observed spread is ~4× wider than 'that number' can produce. Also, with SD 0.0044 over n=224 the SEM is 0.00029, so measured 0.0042 vs predicted 0.00351 is a 2.4-SEM gap (+20% relative), not an identity. Recommend softening to 'the measured mean, 0.0042, is within 20% of the predicted 0.0035; the measured layer-to-layer dispersion is wider than the isotropic model predicts and is not explained by it.' Confidence is medium because I could not re-derive 0.0042/0.0044 from artifacts (retrain adapters absent from this machine — see blocked); the arithmetic on the paper's own stated numbers is however deterministic.

### D17 [minor/high] lens 4 — referee: CONFIRMED
- quote: "the bound is the initialization box plus 3.16·Σ*lr* = 0.0547 for that run's schedule"
- location: Results, 'An 8-Bit Optimizer Artifact Localized by a Massive-Activation Channel', line 344
- paper says: 3.16 * sum(lr) = 0.0547
- computed: 3.16 * sum(lr) = 0.054036 (sum(lr) = 0.017100 exactly)
- method: Instantiated the real scheduler the run used: transformers.get_scheduler('cosine', SGD(lr=2e-4), num_warmup_steps=18, num_training_steps=171), then summed opt.param_groups[0]['lr'] over the 171 optimizer steps (lr read BEFORE each opt.step(); scheduler stepped after, exactly as HF Trainer does). Result: sum = 0.01710 (lambda-sum exactly 85.5000 = 153/18 warmup partial sums 8.5 + 77.0 cosine), 3.16*sum = 0.0540360. Warmup=18 verified two ways: ceil(0.1*171)=18, and training_poscontrol.log's own logged learning_rate 5.555555555555556e-05 at step 5 = 2e-4*5/18 and 1.999156886888064e-04 at step 20, both reproduced bit-for-bit by my reconstruction. The sum is insensitive to off-by-one conventions: lambda(0)=0 and lambda(171)=0, so summing steps 0..170 or 1..171 gives the identical 0.01710; warmup=17 also gives 0.01710. The paper's 0.0547 requires sum(lr)=0.0173101, i.e. a lambda-sum of 86.55 rather than 85.50. Verified with sqrt(10)=3.16227 too: 0.0540749, still not 0.0547. Config confirmed from dpo_adapter_poscontrol/run_meta.json (lr 0.0002, epochs 3.0, lr_scheduler_type cosine) and train_native.py:187 (warmup_ratio=0.1).
- referee reasoning: I reproduced the challenger's number by two independent routes and could not construct any defensible schedule that yields 0.0547.

(a) Closed form, no library: for HF's cosine-with-warmup, λ(s)=s/w for s<w and 0.5(1+cos(π(s-w)/(T-w))) after, the λ-sum telescopes to exactly T/2 for ANY warmup w (warmup contributes (w-1)/2, cosine contributes (T-w)/2 + 0.5, because Σ_{k=0}^{T-w-1} cos(πk/(T-w)) = 1 by the k ↔ (T-w)-k antisymmetry). So λ-sum = 85.5 for T=171 regardless of w=17/18/19, and Σlr = 2e-4 × 85.5 = 0.017100.

(b) Real scheduler in the training venv: transformers 4.46.3 get_scheduler('cosine', SGD(lr=2e-4), 18, 171). lr applied at step 1 is 0.0; Σ of applied lr over 171 steps = 0.017100; Σ of post-step (logged) lr = 0.017100 as well, so the result is insensitive to the before/after convention. My reconstruction reproduces training_poscontrol.log bit-for-bit at three sampled points: global_step 5 → 5.555555555555556e-05, step 20 → 1.999156886888064e-04, step 170 → 2.108004964086474e-08. T=171 is confirmed by the log's own progress bar (171/171 [1:09:08]); warmup 18 = ceil(0.1·171) from train_native.py:187 warmup_ratio=0.1 and run_meta.json lr_scheduler_type=cosine, lr=2e-4, epochs=3.0.

Therefore 3.16 × 0.017100 = 0.0540360 (and √10 × 0.017100 = 0.0540749). Neither is 0.0547.

I also located the source of the error, which the challenger did not: 0.0547 requires λ-sum ∈ (86.491, 86.585], i.e. exactly 86.5 = 85.5 + 1.0. That is the warmup counted as steps 1..18 (Σk/18 = 9.5) instead of 0..17 (Σk/18 = 8.5) while leaving the cosine leg unshifted — 3.16 × 2e-4 × 86.5 = 0.0546680, which rounds to 0.0547. The same +1 slip appears at line 171 for the original recipe: 5e-6 × (57/2 + 1) = 1.475e-4 → the paper's 'Σlr = 1.47 × 10⁻⁴', whereas the correct value is 5e-6 × 28.5 = 1.425e-4. Under PyTorch LambdaLR with last_epoch=-1 the first optimizer step genuinely runs at lr = 0, so 85.5 is the correct λ-sum and the paper's is not a defensible alternative convention — it is an arithmetic slip.

Mitigation worth stating in the correction: the error inflates an UPPER bound on displacement by 1.2%, so it makes the 'moved further than exact Adam could' claim more conservative, not less. Nothing qualitative changes.
- CORRECTED VALUE: 3.16·Σlr = 0.0540 (Σlr = 0.017100 exactly); full bound for down_proj = 1/√14336 + 0.054036 = 0.06239

### D18 [minor/high] lens 4 — referee: CONFIRMED
- quote: "returns exactly one tensor: `layers.1.mlp.down_proj.lora_A`, with 169 such elements. All 169 lie in the columns [2304, 2558], spread over 93 distinct columns and all 16 rank rows"
- location: Results, 'An 8-Bit Optimizer Artifact Localized by a Massive-Activation Channel', line 344
- paper says: 169 elements, 93 distinct columns
- computed: 176 elements, 95 distinct columns (at the correct bound 0.054036); the paper's 169/93 are exactly what its own inflated bound 0.0547 produces
- method: Loaded dpo_adapter_poscontrol/adapter_model.safetensors with safetensors.safe_open (numpy), cast each of the 224 lora_A tensors to float64, and counted |a| > 1/sqrt(fan_in) + D. With D = 3.16*sum(lr) = 0.054036 (threshold 0.0623879): 176 elements, columns [2304,2558], 95 distinct columns, all 16 rows, still exactly one tensor. With the paper's D = 0.0547 (threshold 0.0630519): 169 elements, 93 distinct columns, all 16 rows. Sorting |a| descending, the 169th largest is 0.0630733 and the 170th is 0.0630144, so n=169 pins D to the interval (0.0546624, 0.0547214] - i.e. the scan itself was run at 0.0547, and only the schedule arithmetic that produced 0.0547 is wrong. Everything downstream (one tensor, [2304,2558], 16 rows, 0.175, 21x, 3x, 0.058/0.009 inside/outside, 70.4%/1.8%/0.94%) is unchanged by the correction.
- referee reasoning: I wrote my own safetensors scan (safe_open, framework='np', every tensor cast to float64; the file holds 448 tensors = 224 lora_A + 224 lora_B; lora_A shapes 32×(16,14336) and 192×(16,4096); 19,922,944 lora_A entries) and counted |a| > 1/√fan_in + D with fan_in taken per tensor.

- D = 3.16·Σlr = 0.0540360 (threshold 0.0623879 for down_proj): 176 elements, 1 tensor (layers.1.mlp.down_proj.lora_A), columns [2304, 2558], 95 distinct columns, all 16 rows.
- D = 0.0547 (the paper's stated value, threshold 0.0630519): 169 elements, 93 distinct columns, columns [2304, 2558], 16 rows — exactly the paper's figures.
- D = √10·Σlr = 0.0540749: 175 elements, 95 columns.

So the paper's 169/93 are correct *given the bound it states*, and wrong given the bound its stated formula actually produces. This is not an independent error; it is the sole downstream consequence of finding [1] and should be fixed in the same edit.

I also independently confirmed the challenger's order-statistic argument about which D the scan used. Sorting |a| descending on the artifact tensor, the 169th largest is 0.0630733 and the 170th is 0.0630144, pinning D to (0.0546624, 0.0547214] — consistent with either 0.0547 or the 3.16×2e-4×86.5 = 0.054668 identified in [1]. The alternative parse of the sentence, where 0.0547 is the whole bound rather than just the 3.16·Σlr term, is ruled out by the artifact itself: a flat threshold of 0.0547 gives 271 elements across 2 tensors and 127 columns, not 169/1/93.

Everything else in the passage survives the correction, which I verified rather than assumed: max |a| = 0.175030 (paper 0.175); 0.175030 / (1/√14336) = 20.96 → '21 times' the init bound of 0.0083519 (paper 0.0084); 0.175030/0.054036 = 3.24 → still 'three times' the worst-case Adam displacement (it was 3.20 at 0.0547); mean per-column max |a| = 0.05769 inside [2304,2559] and 0.00881 outside (paper 0.058 / 0.009).
- CORRECTED VALUE: 176 such elements, spread over 95 distinct columns (columns [2304, 2558], all 16 rank rows, still exactly one tensor)

### D19 [minor/high] lens 4 — referee: CONFIRMED
- quote: "256 is the block size bitsandbytes 0.50.1 uses to quantize optimizer state (`functional.py:940`, `optimizer.py:524`"
- location: Results, 'An 8-Bit Optimizer Artifact Localized by a Massive-Activation Channel', line 346
- paper says: functional.py:940 cited as evidence for the optimizer-state block size
- computed: functional.py:940 is weight quantization, not optimizer state; only optimizer.py:524 supports the claim
- method: Read the installed files. .venv-train/Lib/site-packages/bitsandbytes/functional.py line 940 is `        qabsmax, state2 = quantize_blockwise(_absmax - offset, blocksize=256)`, and the enclosing definition (awk over lines <=940 matching '^def ') is `def quantize_4bit(` at line 884 - this is the compress_statistics / double-quantization path for the 4-bit WEIGHT absmax values, reached from Linear4bit, never from the optimizer. The genuine optimizer-state line is .venv-train/Lib/site-packages/bitsandbytes/optim/optimizer.py:524 `            blocksize = 256`, inside `def init_state` at line 492 of `class Optimizer2State(Optimizer8bit)` at line 403 (the class AdamW8bit/adamw_bnb_8bit uses), with an identical line at :691 inside Optimizer1State.init_state. So one of the two citations names the wrong subsystem; the claim itself is correct on the strength of optimizer.py:524 alone.
- referee reasoning: I read the installed package directly. bitsandbytes/__init__.py gives __version__ = '0.50.1' (dist-info confirms bitsandbytes-0.50.1), so the version in the citation is right.

functional.py:940 is `        qabsmax, state2 = quantize_blockwise(_absmax - offset, blocksize=256)`. Walking backward for the nearest top-level `def`/`class`, the enclosing definition is `def quantize_4bit(` at line 884, and the line sits inside the `if compress_statistics:` branch that double-quantizes the 4-bit WEIGHT absmax vector and stores it as QuantState.state2. That path is reached from Params4bit/Linear4bit, never from an optimizer. It is a real 256-element block size, but of the wrong subsystem.

optim/optimizer.py:524 is `            blocksize = 256`, inside `def init_state(self, group, p, gindex, pindex)` at line 492 of `class Optimizer2State(Optimizer8bit)` at line 403, immediately followed by `n = p.numel(); blocks = (n // blocksize) + bool(n % blocksize)` and the allocation of state['absmax1']/['absmax2'] — unambiguously the optimizer-state blocking. optim/adamw.py:62 shows `class AdamW8bit(Optimizer2State)`, which is what optim='adamw_bnb_8bit' resolves to, so this is the line that governed the run. An identical `blocksize = 256` sits at optimizer.py:691 in Optimizer1State.init_state.

So the substantive claim — 256-element blocks for 8-bit optimizer state, which is what the block-alignment argument needs — is fully supported, but one of the two file citations names the wrong subsystem and should be removed or relabeled.
- CORRECTED VALUE: Cite `optim/optimizer.py:524` (and `:691`) only; drop `functional.py:940`, which is the 4-bit weight double-quantization path, not optimizer state

### D20 [cosmetic/high] lens 4 — referee: CONFIRMED
- quote: "The window is the ninth 256-element block of the row-major tensor"
- location: Results, 'An 8-Bit Optimizer Artifact Localized by a Massive-Activation Channel', line 346
- paper says: ninth 256-element block
- computed: block index 9 zero-indexed = the TENTH block counting from one; the ninth block counting from one is columns [2048, 2303], which contains no violating element
- method: 2304 = 9*256, so the window [2304, 2559] is block index 9 under zero-indexing. Verified directly on the flattened tensor: np.nonzero(mask.ravel())[0]//256 yields exactly 16 distinct flat block ids {9, 65, 121, 177, 233, 289, 345, 401, 457, 513, 569, 625, 681, 737, 793, 849}, all congruent to 9 mod 56 (14336/256 = 56 blocks per row) - one block per rank row. Counting blocks from one, [0,255] is the first, so [2048,2303] is the ninth and [2304,2559] is the tenth. Purely an ordinal/indexing wording issue; the alignment finding itself is exact and strongly confirmed.
- referee reasoning: Cosmetic ordinal error, but real. 2304 = 9 × 256 exactly, so [2304, 2559] is block index 9 zero-based, which is the tenth block counting from one; the ninth block counting from one is [2048, 2303].

I verified this two ways on the tensor rather than by arithmetic alone. (i) On the flattened (16, 14336) tensor, np.nonzero(mask.ravel())[0] // 256 gives exactly 16 distinct block ids {9, 65, 121, 177, 233, 289, 345, 401, 457, 513, 569, 625, 681, 737, 793, 849}, all ≡ 9 (mod 56) with 14336/256 = 56 blocks per row — one block per rank row. Identical block set at D = 0.0547 (169 elements) and at D = 0.054036 (176 elements), so the alignment is robust to finding [1]. (ii) Columns [2048, 2303] — the ninth block one-indexed — have max |a| = 0.013972 and zero elements over either threshold, so the sentence as written points at a block with no violations in it.

The alignment finding itself is exact and is strengthened, not weakened, by this check. Only the ordinal word needs changing.
- CORRECTED VALUE: the tenth 256-element block (block index 9, zero-based) of the row-major tensor

### D21 [minor/medium] lens 4 — referee: PENDING
- quote: "No other tensor in the adapter has a single such element"
- location: Results, 'An 8-Bit Optimizer Artifact Localized by a Massive-Activation Channel', line 344
- paper says: no other tensor in the adapter has one
- computed: true for the 224 lora_A tensors, but one lora_B tensor also exceeds the same bound: layers.31.mlp.up_proj.lora_B has a single element at |b| = 0.055690 > 0.0547 (and > 0.054036)
- method: Scanned all 224 lora_B tensors from the same safetensors file in float64. Since PEFT initializes B to exactly zero, its displacement bound is 0 + D. At D = 0.0547 exactly one lora_B tensor has an element over bound: base_model.model.model.layers.31.mlp.up_proj.lora_B.weight, one element, max|b| = 0.0556898. Same result at D = 0.054036. Next-largest max|B| anywhere in the adapter is 0.00976 (layers.30.mlp.up_proj.lora_B), so this is an isolated outlier and not a second block artifact. The sentence is defensible if 'tensor' is scoped to the 224 lora_A tensors named in the preceding sentence, but as written ('no other tensor in the adapter') plus the following clause about B, it reads as covering all 448 tensors, and under that reading it is false.

### D22 [minor/medium] lens 4 — referee: UNCERTAIN
- quote: "95.5% of `lora_A` entries still lie inside the kaiming-uniform initialization box |*a*| ≤ 1/√fan_in"
- location: Results, 'Orthogonality Is Predicted, Not Anomalous', line 171 (adjacent section, same adapter and same tensors as my assignment)
- paper says: 95.5%
- computed: 94.671% (18,861,265 of 19,922,944 entries)
- method: Over all 224 lora_A tensors of dpo_adapter_poscontrol/adapter_model.safetensors in float64, counted |a| <= 1/sqrt(fan_in) with fan_in taken per tensor (4096 for q/k/v/o/gate/up, 14336 for down_proj): 94.671%. Strict '<' gives the same to 3 decimals. Alternative readings do not land on 95.5% either: using a single 1/sqrt(4096) box for every tensor gives 97.453%; restricting to the non-down_proj tensors gives 95.987%; down_proj only gives 92.415%; excluding the artifact tensor layers.1.mlp.down_proj entirely gives 94.709%. Flagged as medium confidence because the paper does not state which fan_in convention it used, but no convention I tried reproduces 95.5%.
- referee reasoning: I reproduce BOTH figures from my own load, so the verdict turns entirely on the reading. Counting per tensor with bound 1/sqrt(fan_in) (0.015625 at fan_in 4096, 0.0083517 at 14336): unweighted mean over the 224 tensors = 0.954767 -> 95.5%; true entry-pooled count over all 19,922,944 entries = 0.9467107 -> 94.671%. Strict/non-strict inequality is irrelevant (identical to six decimals, no entry sits exactly on the bound). Per module: down_proj 0.924153 (fan_in 14336, 32 tensors carrying 3.5x the entries each), the six fan_in-4096 modules 0.9526-0.9640. So the two aggregations differ for exactly the reason the challenger gives. Where I part company is on which reading the manuscript is using. The same sentence reports three statistics, and only the per-layer aggregation reproduces all three simultaneously: 95.477% -> 95.5%, sd ratio 1.022719 -> 1.02, kurtosis 2.053401 -> 2.05. Under entry-pooling the sentence would have to read 94.7%, 1.03, and 2.34 — none of which are printed. The paper is therefore internally consistent in aggregating over the 224 adapted layers, and the fault is the word 'entries' rather than the number 95.5. Both readings are defensible on the text alone ('95.5% of lora_A entries' does literally quantify entries, and the Discussion at line 475 repeats 'its entries'), the gap is 0.8 pp, and nothing in the argument turns on it. Reading I used for the verdict: per-layer, because it is the only one under which the sentence's own three statistics are mutually consistent.
- CORRECTED VALUE: Optional wording fix only: '95.5% of lora_A entries, averaged over the 224 adapted layers' (the entry-pooled figure is 94.7%). No number needs changing under the per-layer reading.

### D23 [major/high] lens 5 — referee: CONFIRMED
- quote: "Across the 918 pairs, 866 rejected halves fail on a wrong answer, 17 on a `NameError` (none involving a `typing` name), and the rest on type, index, syntax, or timeout errors"
- location: Results, 'The Typing-Import Decomposition', line 155
- paper says: 866 wrong-answer / 17 NameError / 35 other
- computed: 773 wrong-answer (AssertionError) / 17 NameError / 128 other
- method: Script over data/dpo_pairs_capped.jsonl: for each row take meta.rejected_error, take the last non-blank line, and read the exception name off it (or classify as TIMEOUT when the string is 'timeout >8s'). Result: AssertionError 773, TypeError 56, IndexError 41, NameError 17, SyntaxError 8, timeout 6, ValueError 6, UnboundLocalError 3, ZeroDivisionError 3, AttributeError 3, RecursionError 1, KeyError 1 = 918. The paper's 866 is reproduced EXACTLY, and only, by the rule: count rows where the substring 'assert' appears in rejected_error AND the substring 'NameError' does not (866; 'assert' alone gives 883, 883-17=866). That rule over-counts, because every traceback quotes the failing `assert` line from the test harness even when the exception is a crash inside the candidate: 110 rows contain 'assert' but do not end in AssertionError (TypeError 47, IndexError 34, ValueError 5, ZeroDivisionError 3, AttributeError 3, KeyError 1, plus the 17 NameErrors). Example: a spiral_order rejected half whose traceback ends 'TypeError: '<' not supported between instances of 'list' and 'int'' is counted as a wrong answer. On the PRIOR LEAD - no other file yields 866: dpo_pairs.jsonl (1,244) gives 1,058 AssertionError, dpo_pairs.raw.jsonl (1,235) gives 1,053, repair_pairs.jsonl (38) has no rejected_error field at all. The paper cites the RIGHT file (the 918-pair capped corpus); the classifier behind the number is wrong. Secondary: 'the rest on type, index, syntax, or timeout errors' also under-enumerates - 17 of the true 128 (ValueError 6, UnboundLocalError 3, ZeroDivisionError 3, AttributeError 3, RecursionError 1, KeyError 1) fall outside the four named categories.
- referee reasoning: My own classifier (last column-0 line of meta.rejected_error, exception name read off it; literal 'timeout >8s' -> TIMEOUT) over all 918 rows of data/dpo_pairs_capped.jsonl: AssertionError 773, TypeError 56, IndexError 41, NameError 17, SyntaxError 8, timeout 6, ValueError 6, UnboundLocalError 3, ZeroDivisionError 3, AttributeError 3, RecursionError 1, KeyError 1 = 918. I independently reproduced 866 by exactly one rule: rows containing the substring 'assert' (883) minus rows containing 'NameError' (17). That rule over-counts by 110: those rows contain the harness's quoted assert line but end in a different exception. I tested the strongest alternative readings that would rescue 866 and none does. Counting a row as 'wrong answer' when the exception was raised at the assert line itself (deepest traceback frame == run_tests, e.g. a TypeError inside sorted() on a mistyped return) gives 786, not 866; including <module> frames still gives 786. 97 of the 110 disputed rows have their deepest frame inside the candidate's own function (longest_palindrome, spiral_order, my_atoi, ...), i.e. an unambiguous crash. The paper's own arithmetic is also internally impossible: 866+17 leaves 35 for 'the rest', but TypeError alone is 56. Zero of the 17 NameErrors name a typing generic, so that parenthetical is correct. On the prior lead: dpo_pairs.jsonl (1,244) gives AssertionError 1,058 / assert-minus-NameError 1,173; dpo_pairs.raw.jsonl (1,235) gives 1,053 / 1,168; repair_pairs.jsonl (38) carries no rejected_error field at all. No file yields 866 by any rule but the flawed one. Separately verified in passing and correct: 0 of 1,836 halves contain an import statement.
- CORRECTED VALUE: 773 wrong-answer / 17 NameError / 128 other

### D24 [major/high] lens 5 — referee: CONFIRMED
- quote: "This defect has now been fully resolved by introducing a `.gitattributes` file enforcing `eol=lf` across the repository, ensuring the dataset gate remains robust regardless of the host operating system."
- location: Results, Measurement-Integrity Failure 4, end of line 282
- paper says: fully resolved; .gitattributes enforces eol=lf across the repository
- computed: Not implemented. The repository-root .gitattributes is 26 bytes and contains exactly one line: 'data/*.jsonl merge=union'. No 'eol=lf' and no 'text=auto' anywhere in it. core.autocrlf is still 'true'. The only file in the tree containing '* text=auto eol=lf' is localllm/.gitattributes, a vendored subdirectory that cannot govern task_bank.py at the root.
- method: cat /c/Users/tmcuzzort/srlm-forge/.gitattributes; grep -rn 'eol=lf|text=auto' over the tree (excluding .venv-train/.git); git config --get core.autocrlf. The repository's own residuals note, poscontrol/RESIDUALS-2026-08-04.md, still reads under the heading 'Why deferred': 'The fix (`* text=auto eol=lf`, or `* -text`) ... belongs in its own reviewed PR, not inside a measurement run' and states a 'Discharge condition' (a normalization lands, the receipt is regenerated once under it, and a probe shows autocrlf=false and =true give the same fingerprint) that is not met. The manuscript contradicts itself on the same point: Limitation 7 says 'its fingerprint is line-ending dependent (Failure 4) ... it produces false refusals across clones with differing line-ending configuration.' Both cannot be true. This is the same failure mode - a claimed fix asserted without reading the bytes - that the paper withdraws in the Failure 1 CORRECTION and holds up as its central lesson.
- referee reasoning: I read the bytes. C:\Users\tmcuzzort\srlm-forge\.gitattributes is 26 bytes and `cat -A` shows exactly one line: `data/*.jsonl merge=union^M$` — CRLF-terminated, which is itself the hazard. No 'eol=lf', no 'text=auto', no '* -text'. A recursive grep for `eol=lf|text=auto|-text` over the whole tree (excluding .git and .venv-train) returns only localllm/.gitattributes (`* text=auto eol=lf`), a vendored subdirectory that cannot govern root files, plus two prose mentions in poscontrol/*.md and one false hit inside a tokenizer.json. `git config --get core.autocrlf` returns `true` (from the system gitconfig). The repository's own residuals file poscontrol/RESIDUALS-2026-08-04.md, R-1, still reads under 'Why deferred': 'The fix (`* text=auto eol=lf`, or `* -text`) ... belongs in its own reviewed PR, not inside a measurement run', and its stated discharge condition (normalization lands, receipt regenerated once under it, and a probe shows autocrlf=false and =true give the same fingerprint) is unmet. The manuscript also contradicts itself: Limitation 7 (line 457) says the receipt's 'fingerprint is line-ending dependent (Failure 4) ... it produces false refusals across clones with differing line-ending configuration.' Both cannot be true.
- CORRECTED VALUE: The defect is not resolved. The repository-root .gitattributes contains only `data/*.jsonl merge=union`; core.autocrlf is still true; the fix remains open as residual R-1.

### D25 [major/high] lens 5 — referee: CONFIRMED
- quote: "Three adapters retrained from the same code, corpus, and configuration beat the null by +9.7 to +11.0 pp, each at *p* < 10⁻⁹"
- location: Discussion, 'The First Reading - That DPO Does Not Help Here - Is Contradicted', line 402
- paper says: +9.7 to +11.0 pp; every p < 1e-9
- computed: +8.77 to +10.90 pp; p = 2.7e-7, 3.4e-8, 1.4e-6 (no p is below 1e-9; the largest is 1.4e-6, three orders of magnitude above the stated bound)
- method: Read Table 2 (lines 139-141) and recomputed the deltas from data/ruler_noise.jsonl: rep1 .6032 - ctl-null .5071 = +9.61; rep2 .6161 - .5071 = +10.90; rep3 .6060 - ctl-null3 .5183 = +8.77. The Abstract ('+8.8 to +10.9 pp'), the Conclusion ('+8.8 to +10.9 pp') and Table 2 all agree with each other and disagree with the Discussion. The contradiction is sharpened by Table 2's own note, which criticises an earlier paraphrase - '+9.6 to +10.9pp... p<1e-6' - as one that 'understates the range and overstates the third p-value'; the Discussion's surviving paraphrase understates the range further (drops +8.77) and overstates the p-values by three more orders of magnitude.
- referee reasoning: I recomputed run-level pass@1 per replicate from data/ruler_noise.jsonl and ran my own Welch t-test (incomplete-beta implementation, sanity-checked at t=2.0/df=10 -> p=.0734). Against each arm's same-session null, exactly as Table 2 specifies: retrain 1 (llama3-forged-rep1, n=40, M=.6032) vs ctl-null (n=15, M=.5071) = +9.61 pp, t=7.19, df=22.8, p=2.67e-7; retrain 2 (M=.6161) vs ctl-null = +10.90 pp, t=8.18, df=22.6, p=3.35e-8; retrain 3 (llama3-forged-rep3, n=15, M=.6060) vs ctl-null3 (n=15, M=.5183) = +8.77 pp, t=6.16, df=27.0, p=1.37e-6. Table 2 as printed is exactly right; the Discussion is inconsistent with it, with the Abstract ('+8.8 to +10.9 pp') and with the Conclusion ('+8.8 to +10.9 pp'). PARTIAL REFUTATION of the challenger, which they should be told: the Discussion's figures are not arbitrary. Pooling the three null arms (llama3-forged-null-rep + ctl-null + ctl-null3, 70 replicates, rate .5066) gives deltas of +9.66, +10.95 and +9.94 pp — precisely '+9.7 to +11.0' — with Welch p = 1.06e-19, 5.68e-23 and 1.40e-9, and Student pooled-variance p = 2.70e-21, 6.03e-25 and 3.33e-13. So the challenger's 'no p is below 1e-9 ... three orders of magnitude above the stated bound' is wrong under the very analysis that generates the quoted range: two of three are ~1e-19/1e-23 under Welch and all three are below 1e-9 under Student's t. The paper is nonetheless wrong, on two narrower grounds: (a) the pooled-null convention contradicts Table 2's same-session convention (adopted, the paper says, for 'avoiding instrument drift') and cross-session pooling of a reference arm is the exact practice Failure 5 condemns; and (b) under Welch, the test the paper declares it uses throughout, retrain 3 is p = 1.40e-9 even pooled, so 'each at p < 10⁻⁹' is false under every pooling I tried.
- CORRECTED VALUE: +8.8 to +10.9 pp, with p-values from 3.4 × 10⁻⁸ to 1.4 × 10⁻⁶ (matching Table 2, the Abstract, and the Conclusion)

### D26 [major/high] lens 5 — referee: CONFIRMED
- quote: "Generated from `data/ruler_noise.jsonl` rather than transcribed."
- location: Appendix C headnote (line 654); same claim implied by Failure 3, 'From the retained 200 draws per task per arm' (line 270), and Appendix B's artifact list (line 644)
- paper says: Appendix C's Trained and Null columns, 200 draws per task per arm, generated from data/ruler_noise.jsonl
- computed: Only the Trained column can be generated from that file. The primary comparison's null arm has 5 retained replicates, not 40 - 775 draws, i.e. 25 draws per task, not 200.
- method: Grouped all 334 rows of data/ruler_noise.jsonl by model. The preregistration (data/prereg_track_a_run1.json) fixes arms trained='llama3-forged', null='llama3-forged-null'. 'llama3-forged' has 40 rows and its per-task correct counts reproduce Appendix C's Trained column exactly (97, 97, 104, ... 78; sum 3085/6200 = .4976, SD .0415 = Table 1). 'llama3-forged-null' has 5 rows (replicate ids 1-5), 395/775 = .5097 - it cannot produce Appendix C's Null column (3051/6200 = .4921, SD .0327). No other arm in the file matches: llama3-forged-null-rep is the replication null (3113/6200 = .5021). I checked the git HEAD version of the file (316 rows) - same 5 null rows - and every other .jsonl under data/ (eval_history.jsonl and heldin_history.jsonl hold 5 and 6 'llama3-forged-null' rows respectively, with no per-task correct counts). The numbers themselves are internally coherent (I reproduced the chi-square p-values .00057 and .00118 from the printed counts), so the rows existed at analysis time; they are not in the artifact the paper points at. This also makes Failure 3's 'From the retained 200 draws per task per arm' false for the null arm, i.e. the responsiveness claim rests on data not retained.
- referee reasoning: data/ruler_noise.jsonl has 334 rows. Grouped by model: llama3-forged 40, llama3-forged-rep 41, llama3-forged-null-rep 40, llama3-forged-rep1 40, llama3-forged-rep2 40, base 50, ctl-his 15, ctl-null 15, ctl-null3 15, llama3-forged-rep3 15, poscontrol 9, poscontrol-null 9, and llama3-forged-null 5. data/prereg_track_a_run1.json fixes arms trained='llama3-forged', null='llama3-forged-null'. I verified the two columns that DO reproduce, per task, not just in aggregate: llama3-forged's 40 replicates match Appendix C's Trained column on 31/31 tasks (3085/6200 = .4976, run-level SD .0415 = Table 1); and the 40 base rows carrying a `verifier` field, summed over their `sampled` lists only (greedy-free), match the Base-matched column on 31/31 tasks (2433/4960 = .4905). The Null column (3051/6200, .4921, SD .0327) matches no arm: llama3-forged-null's 5 rows total 395/775 = .5097 (SD .0303) and match the Null column on 0/31 tasks; the best any other arm does is 2/31. I also swept every arm's first-k replicates for a per-task match and found only llama3-forged/40 for Trained, nothing for Null. I then checked history: at commit 3aa02f3 the file had 95 rows with the same 5 llama3-forged-null rows, and at HEAD 316 rows with the same 5. A recursive scan of every .json/.jsonl in the repository found llama3-forged-null 31-task rows only in ruler_noise.jsonl (5 rows, 395 correct); eval_history.jsonl's and heldin_history.jsonl's llama3-forged-null rows are from the superseded 10-task and 13-task banks. Failure 3's 'From the retained 200 draws per task per arm' is therefore false for the null arm.
- CORRECTED VALUE: Only the Trained and Base-matched columns are generated from that file. The primary comparison's null arm (`llama3-forged-null`) has 5 retained replicates, 775 draws, 25 per task — not 40 replicates and 200 draws.

### D27 [minor/high] lens 5 — referee: CONFIRMED
- quote: "The base reference rows have been moved to Failure 5."
- location: Table 1 Note (line 113), against Failure 5 (line 298)
- paper says: Table 1 contains no base row (two rows: Trained, Null)
- computed: Contradicted by Failure 5's closing sentence: 'That is a reason not to treat the base arm as a clean reference even after matching, and it is why the base row appears in Table 1 as a reference rather than as a comparator.' Table 1 as printed has exactly two rows and no base row.
- method: Read Table 1 (lines 108-113) and Failure 5 (line 298). The two statements cannot both be true. Failure 5 is one of the sections the revision did not rewrite; the base row was evidently removed from Table 1 and the sentence justifying its presence there was left behind.
- referee reasoning: Table 1 as printed (lines 108-111) has exactly two data rows, Trained and Null, and its Note (line 113) says the base rows were moved out. Failure 5's closing sentence (line 298) reads 'That is a reason not to treat the base arm as a clean reference even after matching, and it is why the base row appears in Table 1 as a reference rather than as a comparator.' These are mutually exclusive statements about the same table in the same document. No artifact computation can reconcile them; it is a leftover from the revision that removed the base row.
- CORRECTED VALUE: Failure 5's closing sentence must be revised; it still says the base row 'appears in Table 1 as a reference rather than as a comparator', which Table 1 no longer does.

### D28 [minor/high] lens 5 — referee: CONFIRMED
- quote: "pooled over the three null arms (12,090 draws)"
- location: Appendix E headnote (line 725)
- paper says: 12,090 draws
- computed: 10,850 draws (70 replicates x 155)
- method: Searched every subset of the null arms in data/ruler_noise.jsonl for the pooling that reproduces Table E1's Typing column. The unique match is llama3-forged-null-rep (40) + ctl-null (15) + ctl-null3 (15): it reproduces all twelve stated rates exactly (52.9, 54.0, 40.3, 22.0, 20.3, 11.1, 3.4, 2.9, 2.9, 2.6, 1.7, 1.7). Those three arms hold 70 replicates x 155 generations = 10,850 draws, and my direct sum over their per_task 'n' fields is 10,850. 12,090 would require 78 replicates; no combination of null arms in the file gives 78 (adding the 9-replicate positive-control null gives 79 arms/12,245 draws and breaks the rate match). The rates are right; the denominator is not.
- referee reasoning: I searched all 31 subsets of the five null arms in data/ruler_noise.jsonl for the pooling that reproduces Table E1's printed Typing column, comparing each pooled per-task rate (sum of typing_nameerror_fails / sum of n) to the printed value at one decimal on all 31 tasks. Exactly one subset matches on all 31 with zero mismatches: llama3-forged-null-rep + ctl-null + ctl-null3. Summing those arms' per_task 'n' fields directly gives 10,850 draws (6,200 + 2,325 + 2,325 = 70 replicates × 155). 12,090 would require 78 replicates and no subset of the null arms has 78 (70, 75, 79 and 84 are the reachable totals; adding the 9-replicate poscontrol null gives 12,245 and breaks the rate match). The rates themselves are all correct — I reproduced every one of the twelve non-zero values (52.9, 54.0, 40.3, 22.0, 20.3, 11.1, 3.4, 2.9, 2.9, 2.6, 1.7, 1.7) exactly; only the denominator is wrong.
- CORRECTED VALUE: 10,850 draws (70 replicates × 155 generations: llama3-forged-null-rep 40 + ctl-null 15 + ctl-null3 15)

### D29 [minor/high] lens 5 — referee: CONFIRMED
- quote: "The full table is Appendix C (Table C1)."
- location: Failure 3, responsiveness paragraph (line 270), against the Results table labelled 'Table C1' (line 370)
- paper says: Table C1 = the per-task pass-rate table in Appendix C
- computed: 'Table C1' is used for two different tables. The label 'Table C1' is printed in the Results section, over the 'Concentration of the 918-Pair Training Bank' table under 'Training-Data Concentration' (line 370-380). The actual table in Appendix C (lines 656-691) carries no number at all.
- method: grep -on 'Table [0-9A-Za-z]*' over the manuscript: 'Table C1' occurs at line 270 (the cross-reference) and line 370 (the concentration table's own label). Reading Appendix C confirms its table is unlabelled. A reader following the Failure-3 cross-reference lands on a table of cluster counts, not of pass rates. Related: the concentration table is numbered as an appendix table but sits in the Results.
- referee reasoning: A grep for 'Table [0-9A-Za-z]*' over the whole manuscript returns 'Table C1' at exactly two places: line 270 (the Failure-3 cross-reference) and line 370, where it is the bolded label immediately above the Results table titled 'Concentration of the 918-Pair Training Bank'. Appendix C (lines 650-691) has a heading, a headnote and a table with no bold label and no number at all — I read the whole appendix. A reader following the Failure-3 pointer to 'Table C1' lands on a five-row table of cluster counts in the Results, not on the 31-row per-task pass-rate table. The label collision is real and so is the oddity of an appendix-style number sitting in the Results.
- CORRECTED VALUE: Either number Appendix C's table 'Table C1' and renumber the Results concentration table, or drop the cross-reference's table number.

### D30 [minor/high] lens 5 — referee: CONFIRMED
- quote: "Replicate-resampling bootstrap intervals are in Appendix E; they are biased toward zero by resampling noise and are reported as conservative bounds."
- location: Table 2b Note (line 191)
- paper says: bootstrap intervals reported in Appendix E
- computed: Appendix E contains no bootstrap intervals. It consists of a headnote, Table E1 (per-task deltas by arm and metric) and a note; no interval, bound, or resampling result appears anywhere in it.
- method: Read Appendix E in full (lines 721-767) and grepped the whole manuscript for 'bootstrap' - it occurs only in this Table 2b note. A promised analysis is cross-referenced to a section that does not contain it.
- referee reasoning: A grep for 'bootstrap' (case-insensitive) over the whole manuscript returns exactly one hit: line 191, this Table 2b note. I read Appendix E in full (lines 721-770, bounded by the '## Appendix D' heading at 693 and '## Appendix F' at 771). It consists of a section heading, a headnote, Table E1 (31 per-task delta rows plus a Mean/SD row, columns Typing / R1-R3 raw / R1-R3 forgiven / Orig fgv / PosCtl raw / PosCtl fgv), and a closing note about which four tasks carry the typing artifact. No interval, no bound, no resampling result, no confidence limits of any kind appear in it. The promised analysis is cross-referenced to a section that does not contain it.
- CORRECTED VALUE: Appendix E contains no bootstrap intervals; either add them or delete the cross-reference.

### D31 [minor/high] lens 5 — referee: CONFIRMED
- quote: "Any of the first four would have yielded a publishable-looking number with no content; the fifth did, in a previous draft of this article, for several weeks; the sixth's *fix* did, in the form of an unverified claim that a container already existed"
- location: Discussion, 'On the Value of the Null' (line 418)
- paper says: the fifth item = the one that reached print; the sixth item's fix = the container claim
- computed: Both ordinals point at the wrong item. The list as written runs: (1) verdict channel [Failure 1], (2) residual in that channel [Failure 6], (3) verifier depending on its caller [Failure 2], (4) benchmark at its ceiling [Failure 3], (5) gate certifying unparsable data [Failure 4], (6) analysis path pooling a reference arm [Failure 5], (7) training-configuration record [Failure 7]. The item that reached print in a previous draft is Failure 5 ('This one reached print', line 286) = the SIXTH in the list, not the fifth. The unverified container claim is Failure 1's fix (line 250) = the FIRST in the list, not the sixth.
- method: Enumerated the seven items in the sentence at line 418 in order and mapped each to its numbered Failure section by its wording. The mismatch is the arithmetic consequence of inserting Failure 6 at position 2 of a list whose ordinals were written for the pre-revision six-item version; this section is one the revision did not rewrite.
- referee reasoning: I enumerated the seven clauses in the sentence at line 418 in order and mapped each to a numbered Failure by wording: (1) 'a verdict channel the candidate could write' = Failure 1; (2) 'a residual in that same channel that survived the first fix and was only demonstrated as exploitable in a later revision' = Failure 6 (heading at line 300, 'A Statement-Level Residual in the Verdict Channel, Demonstrated and Closed'); (3) 'a verifier whose ground truth depended on its caller' = Failure 2; (4) 'a benchmark at its ceiling' = Failure 3; (5) 'a gate certifying data it could no longer parse' = Failure 4; (6) 'an analysis path that pooled a reference arm across two instruments' = Failure 5; (7) 'a training-configuration record whose seed, checkpointing flag, warm-up count, and optimizer name did not describe the run' = Failure 7 (heading at 334). The item that reached print is Failure 5 — its section opens 'This one reached print' at line 286 — which is sixth in this list, not fifth. The unverified container claim is Failure 1's fix, withdrawn in the CORRECTION at line 250, which is first in this list, not sixth. The off-by-one is exactly what inserting Failure 6 at position 2 does to ordinals written for the six-item pre-revision list.
- CORRECTED VALUE: 'Any of the first five would have yielded a publishable-looking number with no content; the sixth did ...; the first's fix did ...'

### D32 [minor/high] lens 5 — referee: CONFIRMED
- quote: "the sixth's *fix* did, in the form of an unverified claim that a container already existed, for every draft before this revision's predecessor"
- location: Discussion, 'On the Value of the Null' (line 418), against Failure 1's CORRECTION (line 250) and Limitation 12 (line 462)
- paper says: the container claim was removed one revision before this one
- computed: The container claim was withdrawn in THIS revision, not its predecessor. Line 250 is headed 'CORRECTION (this revision)' and says 'Every earlier draft of this article continued the preceding paragraph with a claim that the verifier was re-architected into a locked-down Alpine Linux container'; Limitation 12 says 'The container claim withdrawn from Failure 1 in this revision'.
- method: Direct comparison of the three passages. 'every draft before this revision's predecessor' excludes the predecessor draft, which line 250 explicitly includes among the drafts carrying the claim.
- referee reasoning: Three passages in the same manuscript. Line 250 is headed '**CORRECTION (this revision).**' and states 'Every earlier draft of this article continued the preceding paragraph with a claim that the verifier "was re-architected into a locked-down Alpine Linux container" ... **That claim is withdrawn.**' Limitation 12 (line 462) states 'The container claim withdrawn from Failure 1 in this revision was identified by the authors themselves, on a re-read of the code prompted by preparing this revision.' The Discussion's 'for every draft before this revision's predecessor' excludes the predecessor draft, which the other two passages explicitly include among the drafts that carried the claim. The three cannot all be true.
- CORRECTED VALUE: '... for every draft before this one' — the claim stood in every earlier draft including this revision's immediate predecessor, and was withdrawn in this revision.

### D33 [minor/high] lens 5 — referee: CONFIRMED
- quote: "the silent-data-corruption literature (Ma et al., 2025; Altenbernd et al., 2026)"
- location: Discussion (line 412); Reference list (lines 485-569)
- paper says: Altenbernd et al. (2026) cited in the body
- computed: No reference-list entry exists for Altenbernd. The list runs Amodei -> Bouthillier -> Chen ...; an 'Altenbernd' entry would sort first and is absent.
- method: grep -n 'Altenbernd' over the manuscript returns exactly one hit, line 412 (the in-text citation). The reference list was read in full; Ma et al. (2025) is present at line 513, Altenbernd is not.
- referee reasoning: grep -n 'Altenbernd' over the manuscript returns exactly one line: 412, the in-text citation. I read the reference list in full (lines 485-569); it is alphabetical and runs Amodei (485) -> Bouthillier (487) -> Chen (489) -> Dettmers (491, 493) -> D'Amour (495) -> Dodge (497) ... An 'Altenbernd' entry would sort first, before Amodei, and there is nothing before Amodei. Ma, J., Pei, H., Lausen, L., & Karypis, G. (2025) is present at line 513, so the co-cited work is listed and this one is not.
- CORRECTED VALUE: Add an Altenbernd et al. (2026) entry to the reference list, or drop the citation.

### D34 [minor/high] lens 5 — referee: CONFIRMED
- quote: "Bouthillier, X., Delaunay, P., Bronzi, M., ... (2021). Accounting for variance in machine learning benchmarks."
- location: Reference list, lines 487, 497, 521
- paper says: Bouthillier et al. (2021), Dodge et al. (2020) and Picard (2021) listed as references
- computed: All three are uncited in the body: each name occurs exactly once in the whole manuscript, in its own reference entry. (They are cited in data/prereg_track_a_run1.json's 'what_this_CANNOT_show' field, which is quoted only in paraphrase in the paper.)
- method: grep -n over the manuscript for 'Bouthillier', 'Dodge', 'Picard' - single hits at lines 487, 497, 521 respectively, all inside the reference list. Wilson (1927, line 559) is a borderline fourth case: 'Wilson lower bound' is used at line 270 as an eponym with no year-bearing in-text citation.
- referee reasoning: grep -n over the manuscript for each surname returns a single hit apiece, all inside the reference list: Bouthillier at 487, Dodge at 497, Picard at 521. There is no in-text citation of any of the three anywhere in the Abstract, Introduction, Method, Results, Discussion, Limitations, Conclusion or appendices. The seed-variance passage that would naturally carry them (line 193) cites McCoy et al. 2020, D'Amour et al. 2022, Marx et al. 2020, Summers & Dinneen 2021 and Zhuang et al. 2022 instead, and Limitation 1's 'zero training-seed variance by construction' paraphrases the preregistration's `what_this_CANNOT_show` entry — which does cite 'Dodge et al. 2020, Bouthillier et al. 2021', as I confirmed by reading data/prereg_track_a_run1.json — without carrying the citation across. On the challenger's fourth, borderline case: line 270 uses 'Wilson lower bound' and 'Wilson upper bound' as eponyms with no year, which is defensible usage; I would not report it.
- CORRECTED VALUE: Cite Bouthillier et al. (2021), Dodge et al. (2020) and Picard (2021) in the body (the natural sites are Limitation 1 and the seed-variance discussion at line 193), or remove them from the list.

### D35 [minor/high] lens 5 — referee: CONFIRMED
- quote: "Low-rank adaptation (LoRA; Hu et al., 2021)"
- location: Method, Training Procedure (line 60), against the reference list (line 505)
- paper says: Hu et al., 2021
- computed: The reference list dates the same work 2022: 'Hu, E. J., Shen, Y., Wallis, P., ... & Chen, W. (2022). LoRA: Low-rank adaptation of large language models. Proceedings of the 10th International Conference on Learning Representations.' The body's only in-text citation of it says 2021.
- method: grep -n 'Hu et al' and 'Hu, E' over the manuscript: line 60 (body, 2021) and line 505 (list, 2022). A reader cannot resolve the in-text citation against the list.
- referee reasoning: grep over the manuscript returns two hits. Line 60 (Method, Training Procedure): '(LoRA; Hu et al., 2021)'. Line 505 (reference list): 'Hu, E. J., Shen, Y., Wallis, P., Allen-Zhu, Z., Li, Y., Wang, S., Wang, L., & Chen, W. (2022). LoRA: Low-rank adaptation of large language models. Proceedings of the 10th International Conference on Learning Representations.' That is the manuscript's only in-text citation of the work and its only list entry, and the years disagree, so the citation cannot be resolved against the list. (Both years are defensible for this work — arXiv 2021, ICLR 2022 — but the paper must pick one; since the list entry gives the ICLR venue, 2022 is the consistent choice.)
- CORRECTED VALUE: Hu et al., 2022 — to match the reference-list entry, which dates the ICLR paper 2022

### D36 [minor/high] lens 5 — referee: CONFIRMED
- quote: "The construct-valid gain is concentrated on two tasks with no typing failures at all — `ace_oss_17851` (+44/+44/+39 pp) and `ace_oss_24748` (+45/+44/+33 pp)"
- location: Results, 'The Typing-Import Decomposition' (line 153), against Table E1 (line 739)
- paper says: ace_oss_17851 has no typing failures at all
- computed: Table E1 gives ace_oss_17851 a null typing-failure rate of 1.7% of draws, not zero; and its raw deltas (+45.3/+45.3/+40.0) differ from its forgiven deltas (+44.0/+44.0/+38.7), which can only happen if typing failures exist on that task. Only ace_oss_24748 is at 0.0%.
- method: Read Table E1's row for ace_oss_17851 and confirmed the 1.7% figure independently by pooling the three null arms in data/ruler_noise.jsonl (typing_nameerror_fails / n on that task = 1.7%). Appendix E's own note states the weaker and correct version - 'neither of which has a material typing rate' - so the Results overstate what the appendix supports.
- referee reasoning: Computed directly from data/ruler_noise.jsonl, pooling the three null arms that Table E1 in fact uses (llama3-forged-null-rep + ctl-null + ctl-null3): ace_oss_17851 has 6 typing_nameerror_fails in 350 draws = 1.71%, which is the 1.7% Table E1 prints; ace_oss_24748 has 0/350 = 0.00%. The per-arm split for 17851 is 4/200 (null-rep), 1/75 (ctl-null), 1/75 (ctl-null3) — the failures are present in all three nulls, not a rounding artifact. The internal tell is decisive and I reproduced it: for ace_oss_17851 the raw and forgiven deltas differ (+45.3/+45.3/+40.0 raw vs +44.0/+44.0/+38.7 forgiven), which is arithmetically impossible if the task had no typing failures, whereas for ace_oss_24748 raw and forgiven are identical (+45.0/+45.0, +44.0/+44.0, +33.3/+33.3). The parenthetical figures the Results quotes for 17851 (+44/+44/+39) are themselves the forgiven column, so the sentence quotes typing-forgiven numbers for a task it simultaneously says has no typing failures. Appendix E's note states the correct, weaker version.
- CORRECTED VALUE: Two tasks with negligible typing failures — ace_oss_24748 (0.0% of null draws) and ace_oss_17851 (1.7%) — i.e., Appendix E's own wording, 'neither of which has a material typing rate'

### D37 [minor/high] lens 5 — referee: UNCERTAIN
- quote: "The null arms fail on an un-imported annotation 10.2–11.1 times per 155-generation replicate, and the original adapter fails 11.1 times — identical to null, in both sessions."
- location: Results, 'The Typing-Import Decomposition' (line 153), against Table 2 (lines 137-138) and 'The Arms Were Different Models' (lines 358, 364)
- paper says: the original adapter is identical to null on the typing counter in both sessions
- computed: Not identical in either place the paper reports it. Session B: ctl-his 11.07 vs its null ctl-null 10.20 - a 0.87/replicate excess, which is exactly why Table 2's session-B row has a forgiven delta (+0.52 pp) larger than its raw delta (-0.04 pp). And in the original primary comparison the same adapter differs from its null significantly on this counter in the opposite direction: 463/6,200 (11.6/replicate) vs 515/6,200 (12.9/replicate), t(77.9) = -2.74, p = .0077, of which the paper says 'The entire +0.55 pp raw difference is carried by the trained arm making fewer unimported-typing errors.'
- method: Computed typing_nameerror_fails_total per replicate for every arm in data/ruler_noise.jsonl: llama3-forged 11.57, llama3-forged-rep 11.12, llama3-forged-null-rep 11.10, ctl-his 11.07, ctl-null 10.20, ctl-null3 10.53. Session A is genuinely identical (11.12 vs 11.10); session B is not (11.07 vs 10.20), and the primary comparison's own arms are significantly different by the paper's own test. The revision's new decomposition section and the un-rewritten 'Arms Were Different Models' section make opposite claims about the same counter on the same adapter.
- referee reasoning: Reading used: 'identical to null' = equal to that session's own null on the typing counter, the same-session convention Table 2 declares. Computed typing_nameerror_fails_total per replicate for every arm in data/ruler_noise.jsonl: llama3-forged 11.57 (463 total, which reproduces the 463/6,200 = 7.47% at line 358), llama3-forged-rep 11.12, llama3-forged-null-rep 11.10, ctl-his 11.07, ctl-null 10.20, ctl-null3 10.53, poscontrol 0.11, poscontrol-null 14.67, llama3-forged-null 14.00 (n=5). So the printed null range 10.2-11.1 is right, and 11.1 for the original adapter in both sessions is right. Session A is genuinely identical (11.12 vs 11.10). Session B is not (11.07 vs 10.20), and the challenger is right that this is what makes Table 2's session-B forgiven delta (+0.52 pp) exceed its raw delta (-0.04 pp). But I could not reproduce the force they attribute to it: Welch on the 15 vs 15 per-replicate counts gives t = 0.75, df = 27.0, p = .46 — not a difference the instrument resolves — so 'identical' is defensible as 'statistically indistinguishable', and the substantive point (the original adapter sits at the null's ~11 while the retrains sit at 2.1) holds in both sessions. Their second leg fails outright: the primary comparison's 'null 515/6,200, t(77.9) = -2.74, p = .0077' cannot be re-derived, because the primary null arm (llama3-forged-null) has only 5 replicates in the artifact, totalling 70 typing failures over 775 draws (14.00/replicate); testing what does exist gives t = -2.11, df = 4.8, p = .09. And that comparison is out of the sentence's scope anyway: 'in both sessions' refers to Table 2's session A and session B rows, neither of which is the primary run. Both readings are defensible; the wording, not the arithmetic, is what is loose.
- CORRECTED VALUE: If the strict same-session reading is intended: '... 11.1 times — at the null level in both sessions (session A's null 11.1, session B's 10.2).'

### D38 [minor/high] lens 5 — referee: CONFIRMED
- quote: "a verdict that could be forged by the candidate, a verifier whose answer depended on which script invoked it, a benchmark returning its ceiling regardless of input, a gate certifying a dataset it could no longer parse, an analysis script pooling a reference arm across two instruments, and — as of this revision — a claimed fix that was never actually implemented."
- location: Introduction, 'The Distinction Between an Objective Reward and a Trustworthy One' (line 36)
- paper says: six items, enumerated as 'the failures reported in the Results section'
- computed: The Results report seven numbered failures. This list omits Failure 6 (the statement-level residual in the verdict channel) and Failure 7 (the recorded configuration that did not govern the run) while including the Failure-1 container withdrawal, which is not one of the numbered failures. The Discussion (line 418) and the Conclusion (line 479) both say seven.
- method: Mapped each clause in the line-36 list to a numbered failure heading and compared against the Results' seven headings (Failures 1-7) and against 'Seven components of that apparatus were found to be reporting confidently while not measuring' (line 418) and 'seven independent mechanisms' (line 479).
- referee reasoning: The Results carry seven numbered failure headings, which I enumerated by grep: Failure 1 A Forgeable Verdict (221), Failure 2 Ground Truth Dependent on the Interpreter (252), Failure 3 A Saturated Benchmark (262), Failure 4 A Gate That Had Stopped Verifying (274), Failure 5 A Reference Arm Measured on a Superseded Instrument (284), Failure 6 A Statement-Level Residual in the Verdict Channel (300), Failure 7 A Recorded Configuration That Did Not Govern the Run (334). The line-36 list, introduced as 'The failures reported in the Results section', has six clauses mapping to Failures 1, 2, 3, 4, 5 and then 'a claimed fix that was never actually implemented' — the Alpine-container withdrawal, which lives inside Failure 1's CORRECTION at line 250 and which the Discussion at line 418 itself describes as a *fix* rather than as one of the seven. Failure 6 (which line 302 describes as the nonce fix's residual, demonstrated by a two-line `print('__PASS__' + _NONCE); raise SystemExit(0)` candidate) and Failure 7 are absent from the list. The Discussion at 418 says 'Seven components of that apparatus were found to be reporting confidently while not measuring' and the Conclusion at 479 says 'seven independent mechanisms'; Failure 7's own opening sentence at 336 says 'The six failures above'. The Introduction is the only place that counts six.
- CORRECTED VALUE: Seven items: add the statement-level residual in the verdict channel (Failure 6) and the training-configuration record that did not govern the run (Failure 7), and mark the container claim as Failure 1's withdrawn fix rather than as a failure in its own right.

### D39 [minor/high] lens 5 — referee: REFUTED
- quote: "a timed-out candidate never becomes a `rejected` half, a timeout being an absence of information rather than a wrong answer"
- location: Results, 'Information Discarded by Pair-Primary Pipelines' (line 392), against line 155
- paper says: no timed-out candidate is a rejected half
- computed: 6 of the 918 rejected halves in the training corpus carry 'timeout >8s' as their entire error record (7 of the 1,244 in the uncapped bank). Line 155 concedes this ('and the rest on type, index, syntax, or timeout errors').
- method: Counted rows in data/dpo_pairs_capped.jsonl whose meta.rejected_error is the literal 'timeout >8s': 6. The property is stated as one the restructured pipeline's unit tests fix, but the paper never says the corpus actually trained on violates it - and the sentence as written ('never becomes') reads as a property of the data, which it is not.
- referee reasoning: I reproduce the challenger's counts exactly — 6 rows in data/dpo_pairs_capped.jsonl whose meta.rejected_error is the literal string 'timeout >8s', and 7 in the uncapped data/dpo_pairs.jsonl (1,244 rows) and in data/dpo_pairs.raw.jsonl (1,235 rows) — but they are counts of a claim the paper does not make. The sentence's own subject is the restructured pipeline, not the corpus: the clause is the third item in a list governed by 'Unit tests fix the property:' ('four passing candidates yield 0 pairs and 4 supervised rows; four failing candidates yield 4 labeled negatives; and a timed-out candidate never becomes a `rejected` half'), inside a paragraph whose first sentence is 'The pipeline was accordingly restructured so that an immutable per-candidate trace is primary'. Read in place it is a statement about what the new tests enforce going forward, not about what the legacy bank contains — and the paper does not conceal the legacy state, since line 155 lists timeout among the rejected halves' failure modes. The challenger's own framing concedes this ('The property is stated as one the restructured pipeline's unit tests fix'). The paper is not wrong here; at most the tense invites a careless reading, which a 'now' or 'going forward' would remove.

### D40 [cosmetic/high] lens 5 — referee: CONFIRMED
- quote: "fall outside by 4 pp or less, i.e., by 1 to 8 draws out of 200"
- location: Failure 3, responsiveness paragraph (line 270)
- paper says: 1 to 8 draws
- computed: 2 to 8 draws
- method: From Appendix C: ace_oss_16070 trained .8400 is 4 pp above .80 = 8 draws; ace_oss_16070 null .8100 = 2 draws; ace_oss_24748 trained .1900 is 1 pp below .20 = 2 draws; ace_oss_27102 null .1900 = 2 draws. All rates are multiples of 1/200 = 0.5 pp, and the smallest excursion is 1 pp = 2 draws; a 1-draw excursion (0.5 pp) does not occur.
- referee reasoning: Every rate in Appendix C is a multiple of 1/200 = 0.5 pp, so a whole number of draws maps to an even number of tenths of a percentage point. The four out-of-band arm-task cells are: ace_oss_16070 trained .8400 (4.0 pp above .80 = 8 draws) and null .8100 (1.0 pp = 2 draws); ace_oss_24748 trained .1900 (1.0 pp below .20 = 2 draws); ace_oss_27102 null .1900 (1.0 pp = 2 draws). Minimum excursion 2 draws, maximum 8. A 1-draw excursion would be 0.5 pp and no cell shows one. I verified the two trained-arm cells straight from the artifact rather than from the printed table: llama3-forged's pooled per-task counts are 168/200 on ace_oss_16070 and 38/200 on ace_oss_24748, and its whole 31-task column reproduces Appendix C exactly. So even restricting to the arm whose data survives, the smallest excursion is 2 draws. The '4 pp or less' half of the sentence is correct; only the draw count's lower bound is off by one.
- CORRECTED VALUE: by 2 to 8 draws out of 200

### D41 [cosmetic/high] lens 5 — referee: CONFIRMED
- quote: "767 distinct contrast structures across 918 pairs"
- location: Results, 'Training-Data Concentration' (line 384)
- paper says: 767 clusters over 918 pairs
- computed: 767 clusters over 910 pairs (8 rejected halves are syntactically invalid and are excluded from the contrast row, as they are from the rejected-structural row)
- method: Re-implemented measure_bank_concentration.py's canonicalisation and keyed on (structural_key(chosen), structural_key(rejected)). 8 rejected halves fail ast.parse (the 8 SyntaxError rows), so the contrast and rejected-structural rows have denominator 910, not 918; the printed largest share 1.4% is 13/910. Table C1 itself gives no denominators, so a reader takes 918 from this sentence.
- referee reasoning: I re-implemented measure_bank_concentration.py's canonicalisation independently (forge.extract_code's fence stripping, then the _Canon identifier-renaming AST transformer, docstring drop, annotation and return-annotation stripping, sha256 over ast.dump) and validated it against the row the paper says is a control: my chosen-structure row returns clusters 273, effective 90.41, largest share 9.8% — matching the printed table and data/bank_concentration.json exactly. On that validated implementation, 0 chosen halves fail ast.parse and 8 rejected halves do (the 8 SyntaxError rows of claim 1), leaving 910 pairs where both halves parse. Keying on (structural_key(chosen), structural_key(rejected)) over those 910 gives clusters 767, effective 666.41, largest cluster 13 = 1.4% — the paper's exact printed row. Over all 918 with the unparsable halves given their own key it gives 774 clusters (effective 673.08), not 767. The rejected-structural row settles the denominator independently: clusters 632, effective 441.59, largest 38, and 38/910 = 4.2% (the printed value) while 38/918 = 4.1%. So the paper's tabulated numbers are correct and computed over 910; only the prose sentence's 'across 918 pairs' misstates the denominator. Minor, but it is the only place a reader is given one, since Table C1 prints no denominators.
- CORRECTED VALUE: 767 distinct contrast structures across the 910 pairs whose halves both parse (8 rejected halves are syntactically invalid and are excluded, as they are from the rejected-structural row)

### D42 [cosmetic/high] lens 5 — referee: CONFIRMED
- quote: "Rahman et al.'s work on AI-assisted, Dafny-based formally verified code generation (2026)"
- location: Discussion, 'Relation to Security-Smell and Verified-Generation Research' (line 434), against the reference list (lines 537, 539)
- paper says: Rahman et al., 2026
- computed: The list has no bare '2026' entry: it has Rahman et al. (2026a) (the Dafny paper) and Rahman et al. (2026b) (the malicious-package paper). The body cites '2026b' correctly at line 436 but drops the letter here, which under APA is ambiguous between the two.
- method: grep -n over the manuscript for 'Rahman et al., 2026' and reading the reference entries at lines 537 and 539.
- referee reasoning: The reference list has two 2026 Rahman entries: 'Rahman, M. R., et al. (2026a). From natural language to verified code: Toward AI-assisted problem-to-code generation with Dafny-based formal verification' (line 537) and 'Rahman, M. R., et al. (2026b). Mind the gap: Evaluating LLMs for high-level malicious package detection vs. fine-grained indicator identification' (line 539). A grep for parenthesised years over the body returns '(2026)' at line 434 and '(2026b)' at line 436, so the manuscript uses the letter suffix correctly two lines later and drops it here. Under APA, same-author same-year works must carry the suffix in every in-text citation, so the bare '(2026)' is an error. I would soften the challenger's characterisation on one point: it is not genuinely ambiguous to a reader, because the sentence names the Dafny paper explicitly ('AI-assisted, Dafny-based formally verified code generation'), and the reference list's own closing note (line 571) already flags the Rahman entries as working citations to be reformatted before submission. This is a style fix, not a substantive one.
- CORRECTED VALUE: Rahman et al. (2026a)


## SYNTHESIS REPORT (ordered must-fix list; authoritative where it and a D# overlap)

# VERIFICATION REPORT — "progress so far v3.md" (791 lines)
Five lenses (typing-decomposition, profiles, corpus-and-consistency, weights-and-seed, optimizer-artifact) re-derived the manuscript's numbers from the raw artifacts; an adversarial referee attempted to refute every discrepancy. What follows is what survived.

---

## 1. VERDICT ON THE MANUSCRIPT

**Not safe to circulate as-is.** The empirical core is unusually solid — 107 independently re-derived claims matched, including every cell of Table 2, every cell of Table E1, every correlation and disattenuated *r* in Table 2b, the entire corpus-concentration table, the Welch and TOST arithmetic in Appendix A, the LoRA-init and *r*/*d*ᵢₙ derivations, and the block-alignment result at the heart of the optimizer-artifact section. But four errors survived adversarial refutation at **major** severity, and each is of a kind a hostile reader will find first: a corpus tally at line 155 that is off by 93 pairs and is arithmetically impossible against its own remainder ("the rest" gets 35 when TypeError alone is 56); a claim at line 282 that a line-ending defect "has now been fully resolved" when the repository's `.gitattributes` contains only `data/*.jsonl merge=union` (CRLF-terminated) and `core.autocrlf` is still `true`; a Discussion headline at line 402 ("+9.7 to +11.0 pp, each at *p* < 10⁻⁹") that contradicts the Abstract, Table 2, and the Conclusion and is unreachable under the paper's own declared same-session, two-sided-Welch convention; and a provenance claim at lines 270/644/654 that Appendix C was "Generated from `data/ruler_noise.jsonl`" when the primary null arm has 5 retained replicates (775 draws), not 40 (6,200 draws) — meaning Table 1's null row, the headline null result's own comparator, cannot currently be reproduced from any retained artifact. That last one is the serious one: it is a reproducibility gap in a paper whose entire argument is that unverifiable claims must not ship. In a paper that indicts itself for shipping "an unverified claim that a container already existed," shipping a second unverified fix claim and an unreproducible primary arm would be fatal to its credibility. Fix the four majors and the twenty-two smaller survivors below and it is ready.

---

## 2. MUST-FIX BEFORE CIRCULATION

All four survivors are **major**; none is fatal. Ordered by earliest edit site.

### M1 — Corpus error tally is wrong by 93 pairs and internally impossible
- **Quote:** "Across the 918 pairs, 866 rejected halves fail on a wrong answer, 17 on a `NameError` (none involving a `typing` name), and the rest on type, index, syntax, or timeout errors"
- **Where:** Results, "The Typing-Import Decomposition: Half the Gain Is the Instrument," line 155
- **Says now:** 866 wrong-answer / 17 NameError / 35 "the rest"
- **Must say:** 773 wrong-answer / 17 NameError / 128 rest. Full classification of `data/dpo_pairs_capped.jsonl`: AssertionError 773, TypeError 56, IndexError 41, NameError 17, SyntaxError 8, timeout 6, ValueError 6, UnboundLocalError 3, ZeroDivisionError 3, AttributeError 3, RecursionError 1, KeyError 1 = 918.
- **Why:** 866 is reproducible by exactly one rule — rows containing `assert` (883) minus rows containing `NameError` (17) — which over-counts by 110 rows that merely quote the harness's assert line before dying elsewhere (97 of them crash inside the candidate's own function); no alternative reading reaches 866, and 866 + 17 leaves 35 for a remainder in which TypeError alone is 56.
- *Unaffected:* the parenthetical "(none involving a `typing` name)" is correct (the 17 undefined names are `expand_around_center` 10, `lru_cache` 4, `stack` 2, `itertools` 1), and the bolded corpus claim in the same sentence — zero imports, zero annotated signatures across all 1,836 halves — is confirmed exactly.

### M2 — Appendix C's null column and "200 draws per task per arm" cannot be generated from the cited artifact
- **Quote:** "Generated from `data/ruler_noise.jsonl` rather than transcribed." (line 654) and "From the retained 200 draws per task per arm, that is now closed" (line 270); "Trained and null are 200 draws per task (40 replicates × 5 samples)" (line 654); the artifact list at line 644.
- **Where:** Failure 3 responsiveness paragraph (line 270), Appendix B artifact list (line 644), Appendix C headnote (line 654) — and by consequence Table 1's Null row (line 111).
- **Says now:** both Appendix C columns, at 200 draws per task per arm, derive from the retained per-replicate file.
- **Must say:** only the Trained and Base-matched columns derive from it. `data/ruler_noise.jsonl` (334 rows, at HEAD and in the working tree, and at commit 3aa02f3) contains **5** rows for model `llama3-forged-null` — the arm `data/prereg_track_a_run1.json` fixes as the null — totalling 775 draws, 25 per task, M = .5097, SD = .0303. Either state plainly that the null arm's per-replicate rows were not retained and mark Table 1's null row (M = .4921, SD = .0327), the 515/6,200 typing count, the null typing-forgiven mean .5752 and Appendix C's Null column as transcribed from a superseded record, or restore the 40 null replicates to the artifact.
- **Why:** the two columns that do reproduce were verified per task, not merely in aggregate (`llama3-forged`'s 40 replicates match Trained on 31/31 tasks, 3085/6200 = .4976, SD .0415 = Table 1; the 40 verifier-bearing `base` rows summed greedy-free match Base-matched on 31/31, 2433/4960 = .4905), while the printed Null column (3051/6200) matches **no arm on any task** — `llama3-forged-null` matches it on 0/31, the best any other arm reaches is 2/31, and a repository-wide scan of every `.json`/`.jsonl` found no other 31-task `llama3-forged-null` rows.
- *Note:* this is the one finding that touches the paper's headline null. Every other Failure-3 figure survives (no task at 0.00 or 1.00; 28/31 inside [0.20, 0.80]; the Wilson bounds .1417 and .8843 recompute exactly).

### M3 — A fix is claimed as complete that was never implemented
- **Quote:** "This defect has now been fully resolved by introducing a `.gitattributes` file enforcing `eol=lf` across the repository, ensuring the dataset gate remains robust regardless of the host operating system."
- **Where:** Results, Measurement-Integrity Failure 4, end of line 282
- **Says now:** the CRLF fingerprint defect is closed by a repository-wide `eol=lf` normalization.
- **Must say:** the defect remains open. `C:\Users\tmcuzzort\srlm-forge\.gitattributes` is 26 bytes and holds exactly one CRLF-terminated line, `data/*.jsonl merge=union` — no `eol=lf`, no `text=auto`, no `* -text`; `git config --get core.autocrlf` returns `true`; the only `* text=auto eol=lf` in the tree is in the vendored `localllm/`, which cannot govern root files. It should be reported as residual R-1 of `poscontrol/RESIDUALS-2026-08-04.md`, whose discharge condition (normalization lands, receipt regenerated under it, probe shows `autocrlf=false` and `=true` yield the same fingerprint) is unmet.
- **Why:** this is the same failure the paper itself names at line 250 — a claimed fix never checked against the bytes it describes — and Limitation 7 (line 457) already contradicts it: "its fingerprint is line-ending dependent (Failure 4) … it produces false refusals across clones with differing line-ending configuration." Both cannot be true.

### M4 — Discussion headline contradicts the Abstract, Table 2, and the Conclusion
- **Quote:** "Three adapters retrained from the same code, corpus, and configuration beat the null by +9.7 to +11.0 pp, each at *p* < 10⁻⁹, with instrument drift excluded by a same-session control."
- **Where:** Discussion, "The First Reading — That DPO Does Not Help Here — Is Contradicted," line 402
- **Says now:** +9.7 to +11.0 pp, every *p* below 10⁻⁹.
- **Must say:** "+8.8 to +10.9 pp, at *p* = 3.4 × 10⁻⁸, 2.7 × 10⁻⁷, and 1.4 × 10⁻⁶" — i.e., Table 2's own values (+9.61 pp, *t* = 7.19, df 22.8; +10.90 pp, *t* = 8.18, df 22.6; +8.77 pp, *t* = 6.16, df 27.0).
- **Why:** the printed range is internally inconsistent with the Abstract ("+8.8 to +10.9 pp"), the Conclusion, and Table 2 in the same document.
- **Important nuance the referee established, which the author needs:** the +9.7/+11.0 figures are *not* arbitrary — they are exactly what pooling all three null arms (70 replicates, rate .5066) produces (+9.66, +10.95, +9.94 pp), with Welch *p* = 1.06e-19, 5.68e-23, **1.40e-9**. So the fix is not "the challenger caught a fabrication"; it is that (a) cross-session pooling of a reference arm contradicts the same-session convention Table 2 adopts and is the exact practice Failure 5 condemns, and (b) even under that pooling, retrain 3's Welch *p* = 1.40 × 10⁻⁹ is not below 10⁻⁹, so "each at *p* < 10⁻⁹" is false under every pooling tried. Retreat to Table 2's numbers; do not simply re-round the pooled ones.

---

## 3. SHOULD-FIX

**Minor** (by section order):

- **L36** — "a verdict that could be forged by the candidate, a verifier whose answer depended on which script invoked it, a benchmark returning its ceiling regardless of input, a gate certifying a dataset it could no longer parse, an analysis script pooling a reference arm across two instruments, and — as of this revision — a claimed fix that was never actually implemented." Six items for what the Results number as seven Failures. Add Failure 6 (the statement-level residual) and Failure 7 (the configuration record), and recast the container claim as Failure 1's withdrawn *fix*, not a failure. Line 418 says "Seven components," line 479 says "seven independent mechanisms," line 336 says "The six failures above" — the Introduction is the only place that counts six.
- **L60** — "(LoRA; Hu et al., 2021)" → "(LoRA; Hu et al., 2022)". The list entry at line 505 dates the ICLR paper 2022; the citation cannot currently be resolved against the list.
- **L153** — "two tasks with no typing failures at all — `ace_oss_17851` (+44/+44/+39 pp) and `ace_oss_24748` (+45/+44/+33 pp)" → Appendix E's own wording, "neither of which has a material typing rate": `ace_oss_24748` is 0/350 pooled null draws (0.0%), `ace_oss_17851` is 6/350 (1.7%) — 4/200 in `llama3-forged-null-rep`, 1/75 in `ctl-null`, 1/75 in `ctl-null3`. Table E1 itself prints 1.7% for that task, and the +45.3/+44.0 raw-vs-forgiven gap (= 1/75 = 1.333 pp) is arithmetically impossible if the rate were zero. The scientific point is untouched: 1.7% cannot manufacture a +44 pp gain.
- **L171** — "sum to Σ*lr* = 1.47 × 10⁻⁴" → "Σ*lr* = 1.43 × 10⁻⁴" (exactly 1.4250 × 10⁻⁴). The cosine-with-warmup λ-sum telescopes to T/2 = 28.5 for T = 57 under any warmup count (5, 6, or 7) and either decay shape; transformers' own `get_scheduler('cosine')` returns 1.4250e-4, and `poscontrol/retrain.log`'s 11 logged learning rates match the reconstruction at zero relative error. 1.47e-4 requires λ-sum 29.4, unreachable at T = 57.
- **L171** — "could have moved more than 1.6% (typical) or 5.2% (worst case) of its own magnitude" → "1.6% (typical) or 5.0% (worst case)". Downstream of the above: 3.1622777 × 1.4250e-4 / 0.0090211 = 4.995%. **The 1.6% figure is correct and must not be changed** (1.5796%). The sentence is an upper bound, so the error made the argument *less* conservative than it could be; correcting it strengthens "A is effectively frozen."
- **L191** — "Permutation *p* from 20,000 shuffles of task labels." → "…, one-sided (upper tail)." Every published cell reproduces as a one-sided upper-tail *p* and none of the non-degenerate ones reproduces two-sided; the decisive tell is the OrigA v R1 row, which carries .66 at *r* = −.074 and .32 at *r* = +.081. The Preregistration section fixes "two-sided" and Tables 1 and 2 are two-sided throughout, so an undeclared convention change is live. No conclusion flips — "significant (*p* = .016)" at line 197 is .031 two-sided.
- **L191** — "Replicate-resampling bootstrap intervals are in Appendix E; they are biased toward zero by resampling noise and are reported as conservative bounds." Delete, or add the intervals. Case-insensitive grep for "bootstrap" and for "resampl" over all 791 lines returns this line only; Appendix E (721–770) holds a heading, a headnote, Table E1 and a note, with zero `[` characters — no interval of any kind.
- **L270** — "The full table is Appendix C (Table C1)." Appendix C's table carries no number; "Table C1" is the bolded label at line 370, above the five-row concentration table in the Results. Renumber one of them or drop the number from the cross-reference.
- **L298** — "it is why the base row appears in Table 1 as a reference rather than as a comparator" contradicts Table 1's own Note at line 113, "The base reference rows have been moved to Failure 5." Table 1 as printed has two data rows. Revise the Failure-5 sentence; it is a leftover from the revision that removed the row.
- **L344** — "the bound is the initialization box plus 3.16·Σ*lr* = 0.0547 for that run's schedule" → "= 0.0540" (Σ*lr* = 0.017100 exactly; full down_proj bound 1/√14336 + 0.054036 = 0.06239). Same +1 warmup slip as L171: 0.0547 requires λ-sum 86.5 = 85.5 + 1, i.e. warmup summed over steps 1..18 instead of 0..17 with the cosine leg unshifted. Under LambdaLR with `last_epoch=-1` the first step genuinely runs at lr = 0, so 85.5 is correct, not an alternative convention.
- **L344** — "with 169 such elements. All 169 lie in the columns [2304, 2558], spread over 93 distinct columns" → "176 such elements … over 95 distinct columns." Purely downstream of the bound above (169/93 is exactly what the paper's stated 0.0547 yields; 176/95 is what its stated *formula* yields); fix in the same edit. Everything else in the passage survives: still exactly one tensor, still columns [2304, 2558], still all 16 rank rows, max |*a*| = 0.175030 = 20.96× the 0.0083519 init bound, and 0.175030/0.054036 = 3.24, still "three times."
- **L346** — "(`functional.py:940`, `optimizer.py:524`; Dettmers et al., 2022)" → cite `optim/optimizer.py:524` (and `:691`) only. `functional.py:940` is `qabsmax, state2 = quantize_blockwise(_absmax - offset, blocksize=256)` inside `def quantize_4bit(` — the 4-bit *weight* double-quantization path, reachable only from `Params4bit`/`Linear4bit`, never from an optimizer. `optimizer.py:524` is the correct citation (`Optimizer2State.init_state`; `AdamW8bit(Optimizer2State)` at `optim/adamw.py:62`), and the substantive 256-block claim is fully supported.
- **L412** — "(Ma et al., 2025; Altenbernd et al., 2026)". Add an Altenbernd et al. (2026) entry or drop the citation; the alphabetical list (485–569) begins at Amodei, and an Altenbernd entry would sort before it. Ma et al. (2025) is present at line 513.
- **L418** — "Any of the first four would have yielded a publishable-looking number with no content; the fifth did, in a previous draft of this article, for several weeks; the sixth's *fix* did" → "Any of the first five … the sixth did … the first's fix did". The sentence's own seven clauses map to Failures 1, 6, 2, 3, 4, 5, 7 in that order; the item that reached print is Failure 5 (its section opens "This one reached print," line 286), which is *sixth* in that list, and the container claim is Failure 1's fix, which is *first*. The off-by-one is exactly what inserting Failure 6 at position 2 does to ordinals written for the pre-revision six-item list.
- **L418** — "for every draft before this revision's predecessor" → "for every draft before this one". Line 250 ("Every earlier draft of this article continued the preceding paragraph with a claim…" / "**That claim is withdrawn.**") and Limitation 12 (line 462, "withdrawn from Failure 1 in this revision") both include the predecessor draft among those that carried it. All three cannot be true.
- **L487, L497, L521** — Bouthillier et al. (2021), Dodge et al. (2020) and Picard (2021) appear only in the reference list; grep returns one hit each, all inside it. Cite them in the body (natural sites: Limitation 1's "zero training-seed variance by construction," which paraphrases the preregistration's `what_this_CANNOT_show` entry that *does* cite Dodge and Bouthillier, and the seed-variance passage at line 193) or remove them.
- **L725** — "pooled over the three null arms (12,090 draws)" → "(10,850 draws)". Confirmed by three lenses independently, two of them by exhaustive subset search: exactly one subset of arms reproduces Table E1's printed Typing column on all 31 tasks — `ctl-null` 15 + `ctl-null3` 15 + `llama3-forged-null-rep` 40 = 70 replicates × 155 generations = 10,850, agreeing under all three counting conventions (`len(sampled)`, `per_task['n']`, `per_task['requested']`). 12,090 implies 390 draws/task, under which no integer numerator yields the printed 52.9% (206/390 = 52.8%, 207/390 = 53.1%). Every percentage in the column is correct; only the denominator is wrong.

**Cosmetic:**

- **L18 and L157** — "+3.7 to +5.7 pp" → "+3.7 to +5.6 pp". Exact endpoints are R3 = +3.6567 and R2 = +5.6468; "5.7" is a double-rounding of the correctly-printed table value +5.65. Every alternative construction of the upper endpoint also rounds to 5.6 (per-task mean 5.6452; against session A's null 5.5650). 0.053 pp; no conclusion affected; the lower endpoint is correct.
- **L270** — "fall outside by 4 pp or less, i.e., by 1 to 8 draws out of 200" → "by 2 to 8 draws out of 200". Every rate is a multiple of 1/200 = 0.5 pp; the four out-of-band cells are 8, 2, 2, and 2 draws. A 1-draw excursion would be 0.5 pp and no cell shows one. The "4 pp or less" half is correct.
- **L346** — "The window is the ninth 256-element block of the row-major tensor" → "the tenth 256-element block (block index 9, zero-based)". 2304 = 9 × 256; the ninth block one-indexed is [2048, 2303], which has max |*a*| = 0.013972 and zero over-bound elements — as written the sentence points at a block with no violations in it. The alignment finding itself is exact and robust: the violating elements occupy exactly 16 flat blocks, ids {9, 65, 121, …, 849}, all ≡ 9 (mod 56), one per rank row, identical at both candidate thresholds.
- **L384** — "767 distinct contrast structures across 918 pairs" → "across the 910 pairs whose halves both parse". 8 rejected halves are syntactically invalid and are excluded, as they are from the rejected-structural row; the printed row (767 clusters, effective 666.41, largest 1.4%) is computed over 910 — over all 918 it would be 774/673.08 — and the rejected-structural row settles the denominator independently (38/910 = 4.2% printed; 38/918 = 4.1%). Only the prose misstates it; every tabulated number is right.
- **L434** — "Rahman et al.'s work on AI-assisted, Dafny-based formally verified code generation (2026)" → "(2026a)". The list carries 2026a and 2026b, and line 436 uses "(2026b)" correctly two lines later. Not ambiguous to a reader, and line 571 already flags the Rahman entries for reformatting; a style fix.

---

## 4. AMBIGUOUS WORDING (UNCERTAIN verdicts)

Four places where the referee found both readings defensible and declined to call the paper wrong. In each, the numbers are correct and only the wording admits two parses.

1. **L153** — "the original adapter fails 11.1 times — **identical to null**, in both sessions." Reading A (same-session): false in session B, where the null is 10.200 against the adapter's 11.067. Reading B (identical to the 10.2–11.1 band just defined in the same clause): true, and exactly true in session A (11.122 vs 11.100). Both lenses that raised it, and the referee, note the difference is not resolvable by the instrument (Welch *t* = 0.749, df 27.0, *p* = .4605, 95% CI [−1.508, +3.242]). **Disambiguating edit:** "…and the original adapter fails 11.1 times — statistically indistinguishable from null in both sessions (session A's null 11.1, session B's 10.2)." This also removes the reader's puzzle about why session B's forgiven delta (+0.52 pp) exceeds its raw delta (−0.04 pp).
2. **L171** — "95.5% of `lora_A` entries still lie inside the kaiming-uniform initialization box." Per-tensor averaging over the 224 adapted layers gives 95.477%; true entry-pooling over all 19,922,944 entries gives 94.671%. The referee kept the per-layer reading because it is the only one under which all three statistics in the sentence reproduce simultaneously (95.5%, sd ratio 1.0227 → 1.02, kurtosis 2.0534 → 2.05); entry-pooled they would read 94.7%, 1.03, 2.34. **Disambiguating edit:** "95.5% of `lora_A` entries, averaged over the 224 adapted layers, …". No number changes. (The Discussion at line 475 repeats "its entries," so check that too.)
3. **L201** — "with swings of +73, +64, and +62 pp on three tasks and −60, −38, and −31 pp on three others." The negative triple *is* the true bottom three (−60.000 `ace_oss_17880`, −37.778 `ace_oss_32606`, −31.111 `ace_oss_34760`); the positive triple is ranks 1, 3-or-4, and 5, skipping +68.889 (`ace_oss_23069`). All three quoted swings genuinely exist in Table E1, so nothing false is asserted under an illustrative reading — but the parallel exhaustive negative list implies an extremes reading, and no alternative selection rule justifies the published triple (it is not the top-three forgiven swings, and not "swings on non-typing tasks": the included `ace_oss_31439` is typing-driven, forgiven only +8.9, while the skipped `ace_oss_11739` has 0.0% typing). **Disambiguating edit:** either "+73, +69, and +64 pp" (extremes) or "swings of, among others, +73, +64, and +62 pp" (illustration).
4. **L344** — "**No other tensor in the adapter** has a single such element, and the layer's *B* factor stays well inside its own bound." Under the scoped reading set by the paragraph's opening ("Scanning all 224 `lora_A` tensors…") this is true at both candidate thresholds. Under the literal all-448-tensor reading it is false: `layers.31.mlp.up_proj.lora_B`, row 9559, col 12, holds +0.05568980, above the bound (B initializes to zero, so its box is 0). It is a single isolated element — one over-bound entry in its 256-block, next-largest |B| anywhere is 0.0097557, 5.7× smaller, no block structure. The trailing *B* clause pulls toward the expansive reading. **Disambiguating edit:** "No other `lora_A` tensor in the adapter has a single such element," plus a footnote recording the one `lora_B` outlier. The localization claim is untouched either way.

---

## 5. WHAT WAS VERIFIED CLEAN

107 distinct claims re-derived from raw artifacts and matched. Grouped by lens.

**Typing-decomposition (25 clean).** The lens implemented the *t*-distribution from scratch (regularized incomplete beta, Lentz continued fraction), self-checked against *t*₍.₉₇₅₎(10) = 2.2281 and *t*₍.₉₇₅₎(30) = 2.0423. Every Table 2 row reproduces: session A (*n* = 41, *M* = .5018, SD = .0401) raw −0.0283 pp, *t*(78.99) = −0.032, *p* = .9746, CI [−1.789, +1.733]; session B (*n* = 15, *M* = .5067, SD = .0325) raw −0.0440 pp, forgiven +0.5173 pp, *p* = .7141; Retrain 1 (*n* = 40, *M* = .6032, SD = .0405) +9.6116 pp, *t*(22.82) = 7.1875, *p* = 2.679e-7, CI [+6.84, +12.38], forgiven +4.4048 pp, *p* = .002254; Retrain 2 (*M* = .6161, SD = .0400) +10.9028 pp, *t*(22.61) = 8.1751, *p* = 3.353e-8, forgiven +5.6468 pp, *p* = 1.918e-4; Retrain 3 (*n* = 15, *M* = .6060, SD = .0351) +8.7733 pp, *t*(27.01) = 6.1627, *p* = 1.377e-6, forgiven +3.6567 pp, *p* = .01003. Typing-counter Welch tests *t*(16.2) = −8.695, *t*(16.0) = −8.797, *t*(18.4) = −13.807, all *p* below the claimed 2 × 10⁻⁷. Artifact shares 54.17/48.21/58.32% → the paper's "54%, 48%, and 58%" and the Abstract's "48–58%." The decomposition is null-invariant: against the *n* = 40 same-hardware overnight null the shares are 57.2% and 51.2%, still inside the band. Positive control +11.3978 pp raw, *t*(15.89) = 6.7554, forgiven +2.0078 pp, *p* = .2275, typing 0.111 vs 14.667/replicate. Pairwise retrain differences −1.291/−0.280/+1.012 pp, all non-significant. All 31 × 9 spot-recomputed Table E1 cells and the entire Mean/SD footer reproduce. **The decisive internal check:** across all 334 rows, `aggregate_if_typing_imported['pass@1'] − aggregate['pass@1']` equals `typing_nameerror_fails_total/155` to within 1e-4 — the forgiven metric is exactly the raw metric plus the typing counter. **The fingerprint claim holds:** all 221 rows across the eight Table 2 arms carry a byte-identical four-file verifier record (`forge.py 13d52d9c…`, `screen_tasks.py 3dfc4e9c…`, `task_bank.py 15770d4d…`, `data/screen_results.jsonl cd83ba8a…`, python 3.11.9) and identical sampler records (5 draws, no greedy anchor, temp 0.8); the positive-control pair differs (`forge.py f22eede7…`, python 3.12.10) exactly as the amendment discloses.

**Profiles (14 clean).** Every correlation in Table 2b reproduces on both metrics: primary .974/.801/.793 (retrain pairs), .171, −.074, .467, .382/.381/.561; forgiven .965/.703/.683, .391, .081, .453, .268/.267/.494. Every split-half reliability reproduces under Spearman-Brown splitting **both** the arm and its null by file-order parity — .9448/.9146, .8884/.8208, .6878/.5856, .2092, .5188, .9268/.9220 — which also pins down the method: subtracting the full null from both halves instead gives .971/.954/.896/.701/.536/.971, not the paper's numbers. Every disattenuated *r* reproduces (1.064/1.114, 0.994/0.961, 1.015/0.986, 0.244/0.565, 0.408/0.292, 0.420/0.306, 0.703/0.672), and OrigA's negative forgiven reliability (−0.118) makes its disattenuated *r* undefined, consistent with the printed em-dash. Permutation *p* at 20,000 shuffles reproduces every cell one-sided. Counts 15 up / 3 down (primary) and 12 up / 3 down (forgiven) confirmed. Per-task delta SDs 17.76/16.46/15.12 pp ("15–18 pp") and 33.36 pp ("33 pp"). The positive control's three negative swings (−60.0, −37.8, −31.1 on `ace_oss_17880`, `ace_oss_32606`, `ace_oss_34760`) are exactly the three most extreme. The whole of Table E1 (31 × 10) reproduces cell-for-cell. The arm→null mapping was recovered independently by model name and timestamp and matches the paper's stated pairings.

**Corpus-and-consistency (30 clean).** `data/dpo_pairs_capped.jsonl` is exactly 918 lines, sha256 `85fc0bdc23bf36701db851dd01dcd4b1d5f02e610ccdfc668908cc32b4655ae6` — matching both the paper and `data/dataset_verification.json`. 918 × 2 = 1,836 halves visited. **The strong corpus claim is confirmed in full:** across all 1,836 halves the substring `import` occurs 0 times, 0 lines begin with `import ` or `from … import` at any indentation, 0 halves contain `->`, and the 2,033 `def` lines resolve to 47 distinct signatures with no annotated parameter or return. `__PASS__` occurs 0 times in the 2,200 banked rows. 1,244 + 918 + 38 = 2,200 rows, 1,279 unique pairs, violations 0. **Table C1 reproduces exactly, all five rows**, by re-implementing the AST identifier canonicalization: 13/9.93/13.1%, 407/165.64/8.5%, 273/90.41/9.8%, 632/441.59/4.2%, 767/666.41/1.4% — the last two rows being novel to the paper. Failure 5 reproduces end to end: base pre-pin *n* = 10, *M* = .5910; post-pin *n* = 40, *M* = .5150; diff +7.60 pp, *t* = 8.22, df 24.1; greedy .6323 (980/1550) vs sampled .5047 (3129/6200) = +12.76 pp; and the −2.55 / −1.42 pp decomposition of the .5302 → .4905 gap is exact. Noise floor SD 0.0283 (first 10 pinned base replicates) and 0.0476 (40 matched rows) confirmed; sizing 0.0476 × √(2/40) = 1.064 pp SE, 2.8 × 1.064 = 2.98 pp MDE, 2.8 × 0.836 = 2.34 pp achieved sensitivity. Appendix A's Welch arithmetic is exact: SE = .0083599, *t* = 0.6558, df = 73.96, CI [−1.118, +2.214] pp, *F*(39,39) = 1.61. **Failure 4's two hashes verified against the live bytes:** `task_bank.py` is 1,485 bytes with CRLF and hashes to `15770d4d6583…`, LF-normalized to `b28a47cb95f1…`. Chi-square values in "The Arms Were Different Models" confirmed (67/200 vs 101/200 → χ² = 11.86, *p* = .00057; 123/200 vs 153/200 → χ² = 10.52, *p* = .00118). Limitation 10 confirmed exactly (41 rows / 35 ids; 40 rows / 35 ids). The independent-counter arithmetic closes: 463/6200 = 7.47%, 515/6200 = 8.31%, and the 0.84 pp gap equals raw (+0.55) minus forgiven (−0.29) exactly.

**Weights-and-seed (21 clean).** The seed-ordering claim is verified line by line in the installed packages: `trl/trainer/dpo_trainer.py:376` is `model = get_peft_model(model, peft_config)` and `:640` is `super().__init__(` — both inside `DPOTrainer.__init__` (def at :217, next def at :696), so the PEFT wrap **provably** precedes the Trainer constructor, and `transformers/trainer.py:424` is the `set_seed` call preceded at :423 by the comment "Seed must be set before instantiating the model when using model." `peft/tuners/lora/layer.py:334` supplies only `a=math.sqrt(5)` to `kaiming_uniform_`, leaving `generator=None` — the draw comes from the default global generator. `grep -n -i 'seed'` over `train_native.py` and `config.py` returns **zero** matches. The init-box math is exact: for `a=√5`, gain = √(2/6), bound = 1/√fan_in exactly, sd = 1/√(3·4096) = 0.00902110. 16/4096 = 0.0039, 16/14336 = 0.0011, seven-module average 0.0035. Two independent simulations (dense matmul and a trace-identity formulation, 200 trials each) return 0.003904/0.003851 and 0.001134/0.001105, reproducing "0.0039 ± 0.0005 and 0.0011 ± 0.0002"; the *r*/*d*ᵢₙ result is robust to true rank-16 orthogonal projectors (0.003953), to *d*_out (0.00386/0.00390/0.00403), and to uniform-vs-Gaussian *A*. Positive-control ‖Δ*W*‖_F mean 0.5275 over 224 layers → the paper's 0.53.

**Optimizer-artifact (17 clean).** bitsandbytes 0.50.1 confirmed; `optim/optimizer.py:524` is verbatim `blocksize = 256` inside `Optimizer2State.init_state`, which is what `adamw_bnb_8bit` resolves to. **At the paper's own stated bound (0.00835191 + 0.0547 = 0.0630519) the scan returns exactly 169 elements, columns [2304, 2558], 93 distinct columns, all 16 rank rows — all four figures reproduce exactly**, and the next-closest tensor reaches only 0.85× its threshold, so the one-tensor result is robust. Max |*a*| = 0.175030 (row 9, col 2474) = 20.96× the init bound. The block alignment is exact and threshold-invariant: 16 distinct flat blocks, ids ≡ 9 (mod 56), one per rank row. **The massive activation reproduces to the digit:** hooking the input to `model.model.layers[1].mlp.down_proj` of the cached `unsloth/llama-3-8b-Instruct-bnb-4bit`, channel 2427 carries |activation| = 386.0 and the next-largest channel (198) carries 46.25, invariant across all five prompts tried; layers 0 and 2 have no comparable channel (max 6.53 and 1.07), and channel 2427 ranks 13,422nd in layer 0 and 8,976th in layer 2. Materiality confirmed: columns [2304,2559] hold 70.415% of the layer's ‖Δ*W*‖²_F against a 1.786% uniform share, and 0.9367% of the whole adapter's 75.483. Adapter delta = 0.58% of the base layer output. Mean per-column max |*a*| 0.05769 inside vs 0.00881 outside. The 18-step warmup is confirmed against the run's own log (`5.555555555555556e-05` at step 5 = 2e-4 × 5/18; `1.999156886888064e-04` at step 20), reproduced bit-for-bit.

**One flagged-in-advance issue that turned out absent:** the column-range endpoint inconsistency ([2304, 2559] vs [2304, 2558]) reported in an earlier brief is **not present in v3** — both line 344 and line 781 write [2304, 2558], and "2559" occurs zero times in this version.

---

## 6. STILL UNVERIFIED

1. **Table 1's null arm — the primary comparison's comparator.** Null *M* = .4921, SD = .0327 (*n* = 40), the 515/6,200 typing count, the null typing-forgiven mean .5752, and Appendix C's entire Null column could not be checked against any retained row. `data/ruler_noise.jsonl` holds 5 `llama3-forged-null` replicates (775 draws, *M* = .5097). *To close:* restore the 40 null replicates' per-replicate rows to `data/ruler_noise.jsonl` (or produce whatever record they were transcribed from). Absent that, the paper must say they were not retained — see M2.
2. **All weight-level claims about the original and retrain adapters.** `dpo_adapter_native/` is listed in Appendix B's artifact list but is not present in this working copy; retrains 1–3 are absent too. Unverifiable as a result: the measured cross-run cosine "0.0042 ± 0.0044, minimum −0.0087," the `lora_A` mean cosine −0.0005, "0 of 224 bitwise identical," and the retrains' 0.02 Frobenius norm per layer (the 0.53 side of "0.53 versus 0.02" was re-derived; the 0.02 side was not). Only `dpo_adapter_poscontrol/` is on this machine. *To close:* place the four adapter directories on the verification machine, or publish per-layer cosine and Frobenius tables alongside them.
3. **PEFT 0.14.0.** The original adapter's model card records PEFT 0.14.0; only 0.20.0 is installed, so the `lora/layer.py:334` line-number citation is verified for the replication/positive-control environment only. The manuscript does not claim otherwise, but the citation is environment-specific. *To close:* install 0.14.0 and re-cite, or state the version the line number refers to.
4. **The GRPOTrainer generality claim.** `grpo_trainer.py` does not exist in the installed trl 0.12.2 and no newer TRL is present. `SFTTrainer` shows the same wrap-before-construct order (`sft_trainer.py:283/285` vs `:408`) and `DPOTrainer` was verified. *To close:* install a current TRL and check `GRPOTrainer.__init__`, or narrow the claim to DPO and SFT in 0.12.2.
5. **The "median of 0.013" activation figure.** Confirmed in magnitude only. The statistic is prompt-length dependent — 0.0120 (19 tokens), 0.0152 (35 tokens), 0.0189 (64 tokens) — and 0.013 sits inside the reproducible range, but the exact prompt could not be identified. The 386 and 46 figures are prompt-invariant and match exactly. *To close:* record the prompt, or restate as "a median of order 10⁻²."
6. **The optimizer ablation (`adamw_torch` vs `adamw_bnb_8bit`).** Not run — the paper itself lists it as F3 in *Planned Experiments* and describes the mechanism as inferred, not demonstrated. Nothing to verify; noted for completeness.
7. **A methodological ambiguity, not a blocker.** "Odd versus even replicate index" is under-specified for session A: `llama3-forged-rep` has duplicated replicate numbers (26, 28, 31, 34, 37, 40) and `llama3-forged-null-rep` has duplicates (26, 29, 32, 35, 38) from a resumed run. Parity on the recorded `replicate` field gives .2376; parity on file order gives .2092, the paper's printed .209. The choice moves session A's reliability by ~.03 and no other arm at all. *To close:* one clause in the Table 2b note saying the split is by file order.