RED WITNESS D - the replicate pooling key ignores the platform it records
Captured 2026-08-25T16:26:37Z on ubuntu-box, Linux 6.8.0-134-generic
Repo HEAD: 0122af4  branch: tool/test-run-dashboard

CLAIM: ruler_noise.py pools replicates on the verifier VERSION STRING alone, at
  :281 (banking)  and  :411-417 (analyze). forge.py:138 records an executable
  field alongside it, and the pooling key does not read it.

$ awk NR==281 ruler_noise.py
            if (r.get("verifier") or {}).get("version") == fp["version"]:
$ awk NR==411,NR==417 ruler_noise.py
    vers = {(r.get("verifier") or {}).get("version", "pre-pin/unrecorded")
            for r in runs}
    if len(vers) > 1:
        want = args.verifier or forge.verifier_interpreter()["version"]
        kept = [r for r in runs
                if (r.get("verifier") or {}).get("version",
                                                 "pre-pin/unrecorded") == want]

CONSEQUENCE, measured on the 334 banked rows:
  version bucket (the CURRENT key):
    '3.11.9'                   306
    '3.12.10'                  18
    'pre-pin/unrecorded'       10

  the 3.12.10 bucket, split by the executable field that IS recorded but NOT keyed on:
     18  C:\Users\user\srlm-forge\.venv-train\Scripts\python.exe

  this host would record executable:
    /home/user/srlm-forge/.venv-train/bin/python   version 3.12.10

  => a row scored HERE and a row scored on the WINDOWS host compare EQUAL under
     the current key, and would be pooled into one standard deviation.
