#!/bin/bash
# OVERRIDE ch09/04-symlinks: every block on this page is a DIAGNOSTIC example
# (cat a rules file, udevadm test /sys/block/hdd, a video4linux query) aimed
# at a human investigating device naming. Nothing here configures the system.
echo "ch09/04-symlinks: diagnostic examples only — skipped by override"
