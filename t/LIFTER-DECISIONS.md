# Lifter decisions

The four designs in `t/lifter-design/` (DPN: dafny-print-normalised, RD:
recursive-descent, TSG: two-stage-generic, VL: verified-lift) each end with an
"open decisions" section. Read together they name the same questions, and
each states what reversing its default costs. This file settles them so the
implementers have one answer per question. Row 1 was decided by the operator on
2026-09-05; the rest are the majority default across the four designs, with
the ROADMAP.md "assumed" list (WS-16 notes) breaking ties, approved with the
implementation plan on 2026-09-05. A row is reversed by editing this file and
re-running the corpus; the sidecar records which rule fired, so every count
can be re-derived under the other choice.

Every rewrite is recorded in the task's `.lift.json` sidecar under the name in
the "chosen" column, so the disagreement table can say which rule a row rests
on.

| # | Decision | DPN | RD | TSG | VL | Chosen default | Cost of reversing |
|---|---|---|---|---|---|---|---|
| 1 | Read-only `array<int>` parameter | refuse `array` | refuse `array` | lift to seq | refuse `array` | Lift to `seq`, `a.Length` as `len`, `a[i]` as `at`; condition: no `modifies`, no element assignment, no `new`, no call passing the array, anywhere in the method's closure; recorded `array-readonly-as-seq`. the operator, 2026-09-05; ROADMAP assumed the same. | Refuse `array`: the 36 sole-gap programs stay refused and the table loses its largest class of rows. |
| 2 | `x in s`, `x !in s` on a `seq<int>` | desugar | desugar | desugar | desugar | Bounded `exists` over `[0, len(s))` with a fresh binder, `not` of it for `!in`; recorded `in-desugared`. The contract lemma verifies it per program. | Refuse `seq-membership`: 0 of the 77, 6 of the 49 samples; a lifter-introduced quantifier never reaches a kernel. |
| 3 | `s != []`, `s == []` | refuse `seq-literal` | not raised | not raised | not raised | Refuse `seq-literal` in v1 (a display is a display). | Exact emptiness desugaring to `len(s) != 0`; maximum.dfy lifts. Revisit from the 785 table. |
| 4 | nat return | add | add | add, first | add | Add `ensures r >= 0` as the first ensures clause; recorded `nat-return-ensures`. The source's typing is part of its theorem. | The kernels prove a weaker theorem than the source's on the 25 nat-returning methods of the 77, and a twin returning a negative can pass. |
| 5 | nat local, or nat return assigned in a loop | append | append | append, dedup | add | Append `v >= 0` to every loop that assigns `v`, after the author's invariants, deduplicated; recorded `nat-invariant-added`. This is the per-assignment obligation Dafny proved, relocated to the loop head. | Omit: potencia-shaped loops fail in every kernel (exit `e <= 0` does not give `e == 0`). Refuse `nat-local` instead: 7 of the 77. Prepending instead of appending shifts the author's INVARIANT-DROP site indices. |
| 6 | Function `requires`, and nat parameters (a nat parameter is a requires) | totalise | widen | totalise | totalise | Wrap the body in an `ite` on the source's domain guard with default 0 for int and false for bool; recorded `spec-fun-totalised`. Any constant agrees on the domain; the default is invisible where the source verified. | Refuse `function-requires`: 8 to 12 of the 77 (the designs count differently), every nat-domain recursive function among them. |
| 7 | `assume` in a method body | not raised | refuse | refuse | not raised | Refuse `assume-in-body`. ROADMAP assumed the same. | Drop with provenance: the lifted theorem is stronger than the one the source proved. |
| 8 | Hints and unreachable declarations: `assert`, `calc`, lemma calls, function `ensures`, unused functions and lemmas, `Main` | drop, count | drop, count | drop, record | drop, log | Drop, each counted in the sidecar: `assert-dropped` with count, `lemma-call-dropped`, `function-ensures-dropped`, `unused-function-dropped`, `main-dropped`. A kernel that needed the hint reads UNPROVED, attributed by the sidecar. | Refuse `assert-in-body`: 8 of the 77. Refuse `function-ensures`: 1 of the 77 (`fact` in dafny-programs factorial). |
| 9 | Files with several gradable methods | one task per method | refuse when two carry an ensures | one task per method | one candidate per method | One task per gradable method (a `method` with at least one `ensures`, not `Main`); the table reports both the program count (in fragment iff every task lifts) and the method count, and every number says which unit it uses. | Refuse `multi-method` as the census does: 125 programs stay out on packaging alone (t/COVERAGE-dafnybench.md, current table). |
| 10 | Top-level `&&` inside one clause | split all | never | split requires and ensures only | never | Split in `requires` and `ensures` (`split-conjuncts`; identical theorem, finer certificate conjuncts). Never split an `invariant`: the author's invariant list is the twin's exact instrument. | Meaning is unchanged either way. Splitting invariants changes the selected twin on 27 programs (VL) and gives INVARIANT-DROP more sites (DPN). |
| 11 | Tuple `decreases` (stated, or inferred by rprint for `&&` guards and multi-parameter functions) | refuse unless a Dafny-checked candidate flag is on | sum of guarded parameters with a local check | candidate ladder with a Dafny probe | project, else `guess:sum` | Project to the one component that changes when the others are fixed (`decreases-tuple-reduced`); else the sum of components, recorded `guess:sum`. No Dafny probe in v1: a wrong measure fails the real cell visibly and cannot corrupt a count. When no candidate exists (a loop with a `\|\|`, `!b` or bool guard and no stated measure; `decreases *`), refuse `uninferable-decreases`. | Refuse `lexicographic-decreases`: SlowMax and the two gcd programs are lost. |
| 12 | Source that dafny 4.11.0 does not verify | not raised | carry the exit code | lift, tag | not raised | Lift, tag `source-unverified`, and carry the source's `dafny verify` exit code on every row. None of the 77 today. | Refuse: a Dafny-version artefact then counts as a refusal. |
| 13 | Uninitialised local read before assignment | default-init | fold | not raised | 2 in the 77, folded | Default-init 0 for int and false for bool; recorded `default-init`. | Refuse `uninitialised-local`: 2 of the 77. |
| 14 | `module` wrapper; `ghost var` local; `function ... by method`; `modifies a` with no write; `string` seen only through `\|s\|` and `seq<nat>` elements; top-level early `return` before a loop | refuse | refuse | unwrap; ordinary local; drop the method body; accept; refuse; not raised | not raised | Refuse, with the census name where one exists: `module`, `ghost-local`, `function-method`, `array-mutation` (the frame permits a write), `string-char`, `nat-seq-elements`, `early-exit`. Conservative for v1. | Each is an exact rewrite the inventory identified (TSG and DPN name the files). Revisit from the 785 table. |
| 15 | `for` loops | not raised | not raised | desugar | row in section 4 | Desugar per LIFTER-DESIGN.md section 4 with the implicit range invariant and the `hi - k` measure; recorded `for-desugared`. Zero of the 77; untested until the 785. | Refuse `for-loop`. |
| 16 | `requires true`; the `"t"` version field | not raised | not raised | not raised | mirror; always 1 | Mirror the literal as `[{"bool": true}]`; emit `"t": 1` always. | Nothing measurable; v0-shaped tasks become byte-comparable to the committed ones. |
| 17 | A task that lifts but t's own instruments refuse (`no-operator`, `interp-no-input`, a preservation-witness twin that dafny reads UNPROVED) | own column | separate rows | tag | distinct column | Two columns, lifted and counted. These are t verdicts on lifted tasks, not lift failures, and coverage of the fragment and coverage of the measurement are two numbers. | One column hides which instrument refused. |
| 18 | Retry a `lift-check-failed` row with mechanical hints (`{:induction}`, calling every generated lemma) | not raised | not raised | not raised | never | Never. The checker never argues for the lifter; the row stays refused. | A proof the lifter wrote about itself. |
| 19 | The differential run's input domain | interp's | interp's | interp's, both arms | interp's | `interp.domain` of the lifted task, so the twin, the fidelity check and the body oracle share one domain. | A larger sampled domain costs about 0.1 ms per point after the compile and finds only what interp's misses. |
| 20 | `x / y`, `x % y` on `int` | n/a (t had no division at design time) | n/a | n/a | n/a | Lift one to one to `div`/`mod`, no refusal and no rewrite beyond the op name: SPEC.md "Division and modulo (v1)" (2026-09-08) states Euclidean semantics, and Dafny's own `/` and `%` on `int` are Euclidean too, measured on dafny 4.11.0 (`-7/2 == -4`, `-7%2 == 1`, `7/-2 == -3`, `7%-2 == 1`, `-7/-2 == 4`, `-7%-2 == 1`), so the two sides compare like with like and no domain restriction or totalising wrapper is needed, only `y == 0` stays undefined on both. `lift_check.py` prints `div`/`mod` back as `/`/`%` at the same precedence as `*` for the spec-equivalence lemmas and the differential harness. Decided 2026-09-08, once SPEC.md and interp.py opened the gate. | Refuse `div-mod` as before: 163 of the 785 census programs carry `/` or `%` and would stay out of the fragment on that gap alone. |

| 21 | Mid-body (non-tail) `return` in a method body | n/a (t had no early exit at design time) | n/a | n/a | n/a | Lift to t's `{"return": [ret, e]}` statement (SPEC.md "Early exit (v1)", 2026-09-08): a non-tail `return e;`/`return r;` lifts `e`; a bare `return;` (the out-parameter already assigned on that path, confirmed by the existing definite-assignment check, section 4.7) reads back as the return variable itself; recorded `early-exit-return`. A TAIL return is unaffected, still desugared away as `tail-return`. `break`/`continue` stay refused `early-exit`: the census `early-exit` gap now names them alone, and a non-tail return is the new `early-return` burden. `lift_check.py` compares a `return` statement alpha-equivalently to `assign` (same shape: a name and a value); `lower_dafny.lower` already prints it as `r := e; return;` (Dafny's `return` takes no expression once the method has an out-parameter), so the section-9 lemmas and the section-10 differential harness need no lowering change. Decided 2026-09-08, once SPEC.md and interp.py opened the gate. | Refuse `early-exit` for any non-tail return as before: 4 of the census's 785 have `early-exit` as their sole gap and 5 more pair it with `div-mod`; both classes stay out of the fragment on that gap alone. |

| 22 | Array mutation and allocation: `a[i] := e`, `new int[n]`, a method that `modifies` an array, a method returning one it allocated | n/a (t had no seq mutation at design time) | n/a | n/a | n/a | An `array<int>`/`array<nat>` parameter or local is a `seq` value (SPEC.md "Sequences as values (v1)", 2026-09-09); `a[i] := e` becomes `a := a[i := e]` (`{"op": "update", ...}`); `new int[n]`/`new nat[n]` becomes `seq(n, 0)` (`{"op": "fill", "args": [n, {"int": 0}]}`); `a.Length` is `len(a)`, unchanged from decision 1. Two shapes map, at most one mutated/allocated array per method: (a) **modifies-param** -- a method with ZERO Dafny-declared returns that `modifies` exactly one array parameter and writes `a[i] := e` somewhere becomes a task with `a` as a `seq` parameter and a FRESH `seq` return (named `<a>_out`, collision-checked by the same renamer as every other name); the body is primed `<ret> := a;` so every later `a[i] := e` becomes an update of the return; `old(a[k])` and `old(a[..])` (whole-array, unbounded) read as the PARAMETER's own value, and every other (non-`old`) reference to `a` in the body or ensures reads as the return's value, matching what Dafny's own heap semantics already mean by the one identifier `a`. A method that ALREADY returns something (BubbleSort's own `n`, removeElement's own `i`) is refused instead (`modifies-with-existing-return`): it would need a SECOND return, and t has exactly one. (b) **alloc-fill** -- the return itself (`squared := new int[a.Length]`) or a local later copied to it (`var cubedArray := new int[a.Length]; ...; return cubedArray;`) becomes a seq return; `fresh(b)` on it is dropped (SPEC.md), on both sides of the checker's equivalence lemma (`_strip_fresh_conjuncts`/`_strip_fresh_src`) since an arbitrary lemma parameter is not something Dafny evaluates `fresh()` on the way it would the method that actually allocated `b`. A `seq`'s length is not automatically invariant across a loop the way a real array's `.Length` is (Dafny's own well-definedness rule -- "each ensures/invariant clause may assume only requires/earlier clauses", never what the body's loop separately proved -- SPEC.md's Definedness section, the same rule t itself states): `len(<ret>) == <the array's own fixed length>` is added as the task's own FIRST ensures clause (`array-length-return-ensures`) and PREPENDED (not appended, unlike decision 5's `nat-invariant-added`) to every loop invariant list that touches the mutated seq (`array-length-invariant-added`); the same fact, granted via `requires`, keeps the checker's `L_ens`/`L_inv`/`L_dec` lemmas provable despite this asymmetry the source never has to state. Recorded `array-mutation-as-seq`, `array-new-as-fill`, `array-elem-assign-as-update`. What stays refused, `array` (the array's own type is wrong) or `array-mutation` (the mutation shape is wrong): two-dimensional arrays (`array2`/`array3`), arrays of non-int/non-nat elements, `multiset` over a mutated array's slice (Clover_bubble_sort's `multiset(a[..])==multiset(old(a[..]))`), a bounded slice `a[lo..hi]` (still `seq-slice`; only the unbounded `a[..]` on the mutated array is accepted), more than one mutated array per method, a mutated array also passed to another call (aliasing, this shim cannot verify past it), any `modifies` naming anything but the one array, and any shape not named above. Measured first on the 785 DafnyBench programs (own regex + `coverage_census.py`, 2026-09-09): 308 use an array (census `array` boolean, pre-row-22), 152 assign an element (`array-mutation`), of which (file-level, so Main's own unrelated `new` counts against the file, not the method) 71 mutate a parameter in place under `modifies` alone, 47 allocate-and-fill alone, 34 do both somewhere in the file (usually Main plus the graded method, not one method doing both -- row 22 refuses that as `multi-array-mutation` if it ever is). Of the 785, 105 have gaps that are now entirely subsumed by this row (`array`, `array-mutation`, `seq-return`, `zero-returns`, alone or combined): 57 read-only-array-only (decision 1's own territory, 11 already lifted before this row, the rest refused for OTHER reasons this row does not touch -- `heap`/null-checks, `unbounded-quantifier`, `function-contract`), 18 `array`+`array-mutation` (this row's direct target: allocate-fill mostly, e.g. `dafny-synthesis_task_id_8` SquareElements), 28 `array`+`array-mutation`+`zero-returns` (the `modifies`-param shape's direct target, e.g. Clover_double_array_elements/replace/swap_in_array), 2 `zero-returns` alone (unrelated to arrays, untouched by this row). Verified end to end (checker lemmas AND the differential harness) on Clover_array_product (alloc-fill, two read-only params), Clover_double_array_elements and Clover_replace (`modifies`-param, zero return, point-wise `old`). `lift_check.py`'s `_t_expr` renders `update`/`fill` back as `s[i := v]`/`seq(n, _ => 0)`; `_split_array_post_state` rewrites the SOURCE ensures/invariant AST (never `_print_expr` itself) so a non-`old` occurrence of the mutated array's one Dafny identifier prints as the return while an `old`-wrapped one is left alone to print as the parameter, since Dafny repeats that one identifier for both and a flat rename dict cannot tell them apart; the differential harness compares `arr{j}[..]` (the materialised argument array, post-call) or `srcv[..]` against the lifted `seq` result, and `_POINT_RE`/`_dafny_value_matches` now parse a bracketed `[1, 2, 3]`-shaped printed value (measured: `dafny run`'s own seq print format, spaces after every comma, broke the old `\S+`-per-field parser -- the first tasks through this harness with a seq-typed RESULT rather than only seq-typed arguments). Decided 2026-09-09, once SPEC.md's "Sequences as values (v1)" opened the gate. | Refuse `array-mutation`/re-widen `array` as before: 152 of the 785 (`array-mutation` true) stay out of the fragment on that gap alone, 308 in total lose the read-only row too if `array` reverts to unconditional refusal (decision 1's own row, not this one, but the two share one census boolean's worth of history); the 46 programs whose ONLY remaining gap this row closes (18 + 28 above) stay refused. |

| 23 | `break` whose innermost loop is in tail position of the method body | n/a (t had no early exit at design time) | n/a | n/a | n/a | Dafny's `break` jumps to the point after the loop, and everything that held on the break path is known there (Boogie goto semantics), so it lifts one to one to t's early exit `return ret;` (SPEC.md "Early exit (v1)") exactly when its innermost enclosing loop L is in tail position of the method body: L is the last statement of the body, or the last statement of a then/else branch of an IfStmt itself in tail position (recursively; `BlockStmt`/`LabelStmt` transparent), judged AFTER `_desugar_returns` has run (a trailing `return;`/`return r;` has already vanished, a trailing `return e;` has already become an assignment), and allowing a straight-line CONTINUATION C after L: every statement after L in its block, then every statement after the enclosing if in its block, and so on to the end of the method body. C may hold no `while`, `for`, `break`, `continue`, or `return` (after desugar); if C is non-empty it is DUPLICATED at the break point (semantics-preserving under the goto reading), recorded `break-continuation-duplicated` alongside `break-as-return`. The break itself must not sit inside a loop nested within L (then its innermost loop is the inner one, never tail). Everything else stays refused `early-exit`, with a token naming why: `continue` (any `ContinueStmt`), `break-label` (a labeled break), `break-in-nested-loop` (the break's own innermost loop is itself nested in another loop), `break-before-loop` (the continuation holds a loop or another break/continue), `break-not-tail` (any other non-tail shape, e.g. the loop followed by a non-tail return, or sitting in a non-tail if branch). `lift_classify.scan_breaks` (with its `_find_tail_loops`/`_continuation_issue` helpers) is the one shared computation classify and `lift_rewrite._desugar_breaks` both call, on the raw AST and the `_desugar_returns`-desugared AST respectively, so the two can never disagree; `_desugar_breaks` runs immediately after `_desugar_returns` in `rewrite()`, replacing each accepted `BreakStmt` with a deep copy of its loop's continuation followed by a bare `ReturnStmt`, which `_lift_stmt`'s existing ReturnStmt branch already turns into `{"return": [ret, {"var": ret}]}` (the out-parameter's definite assignment on that path is Dafny's own guarantee, already checked by section 4.7 on the raw body). No change to `lift_check.py`: `interp.exec_body` already propagates an early return up out of a `while`'s body (needed since row 21's non-tail returns can already sit inside a loop), and `lower_dafny`'s printing of a bare early-exit return already existed from row 21, so both the differential harness and the section-9/10 lemmas needed nothing new. Measured on the 785-program DafnyBench census with the lifter's own parser, 2026-09-09: 36 gradable programs carry a break (0 a continue, 0 a labeled break); of the 30 that parse, 23 have the loop as the body's last statement (17 for-loops, 4 while-loops, 2 loops inside a tail if), 3 have a straight-line continuation (`dafny-synthesis_task_id_567`'s trailing `sorted := sorted;`, `_594`'s `diff := a[firstEven] - a[firstOdd];`, `_627`'s trailing assert only), 2 are followed by another loop, 1 breaks inside a nested loop. Running the lifter on all 36: 4 fully lift and check (`Program-Verification-Dataset_..._KatzManna` NinetyOne exercises `break-continuation-duplicated` end to end); 6 more lift (`break-as-return` recorded) but fail `lift_check` on a PRE-EXISTING, unrelated gap (`L_inv_0`'s invariant-equivalence automation limit, `L_fun_isOdd`/`L_fun_isEven`'s function-equivalence automation limit) that also fails on break-free for-loop and parity-function programs elsewhere in the same census run (e.g. `dafny-synthesis_task_id_770`, no break at all, same `L_inv_0` token); 6 files never reach a method at all (`no-method`/`heap`/`tuple` at parse); the remaining 20 files' break-carrying method is refused for a different, earlier construct (`heap`, `string-char`, `nested-seq`, `as-cast`, `unbounded-quantifier`, `multi-return`, `seq-slice`) before the break is reached, and in the 3 cases checkable by line order (`BPTree-verif_..._Utils` GetInsertIndex, `Formal-Verification-Project_..._strings3` maxCommonSubstringLength, `dafny-synthesis_task_id_784` FirstEvenOddIndices) the break's own line precedes the reported issue's, meaning row 23 passed it silently. Decided 2026-09-09, by measurement (see the task's own LIFTER-DECISIONS.md-writing instructions), once `lift_classify.py`/`lift_rewrite.py` already carried rows 21/22's early-exit and desugaring machinery to build on. | Refuse `early-exit` for every break as before: 16 gradable programs have it as their sole census gap, all 16 in the MBPP-DFY family. |
| 24 | `x != null` on a non-nullable `array<int>`/`array<nat>` parameter or return (`requires`/`ensures`/loop `invariant`) | n/a (t had no heap at design time) | n/a | n/a | n/a | Dafny 4 makes `array<int>` a NON-NULLABLE type by default (only `array?<int>` is nullable): a comparison `x != null` (either operand order) where `x` is a parameter or return whose declared type is a non-nullable `array<int>`/`array<nat>` is a tautology Dafny itself proves trivially (measured directly: `dafny verify` on the generated checker file emits its own warning on the exact clause, "the type of the other operand is a non-null type, so this comparison with 'null' will always return 'true'"), so it is DROPPED rather than refused `heap`: as a whole `requires`/`ensures`/loop `invariant` clause the clause vanishes entirely; as a TOP-LEVEL `&&` conjunct it is removed from the conjunction, the same "top-level conjunct" reading decision 22's `fresh(b)`-dropping already uses (`_strip_fresh_conjuncts`). Recorded `null-check-dropped` (a `Rewrite` with the clause's line, and a matching `ClauseDropped` entry in the sidecar, one per dropped comparison, mirroring `assert-dropped`'s per-instance accounting). `x == null` on the same non-nullable `x` is also a closed fact (definitionally false) but never appears in a verified Dafny program (it would make the precondition unsatisfiable), so it stays refused `heap` as before, never dropped: row 24 only ever touches `!=`. Any `null` on a NULLABLE type (`array?<...>`), on a `class`/`object`, or the SAME `!= null` comparison anywhere but a whole clause or a top-level `&&` conjunct (inside an `\|\|`, an `==>`, or a plain body expression) is untouched and still refuses `heap` exactly as before: `lift_classify.scan_null_checks` only ever matches those two positions, on `method.specs`'s own `RequiresClause`/`EnsuresClause` and every `WhileStmt`/`ForStmt`'s own `InvariantClause`, never inside a nested boolean connective. (Implementation note: a single relational operator, `!=` included, always parses as a length-1 `Chain`, never `Binary` -- section 3's `Binary` production is `+ - * / %` only -- so `scan_null_checks`/`_strip_null_checks` match `Chain(ops=("!=",), operands=(x, null))`, not a `Binary` node.) `scan_null_checks` is the one shared computation `classify` (exempting the matched `null` `Ident`s from the generic `heap` scan, via the same `accepted_ids` identity mechanism decision 22 already uses for `Old`/`Fresh`/`Slice`) and `lift_rewrite._strip_null_checks` both call, recomputed on the same raw `method` AST in each stage (`scan_breaks`'s pattern for row 23), so the two can never disagree. A nullable array PARAMETER stays refused `array` regardless (section 4.2's param-type loop already refuses any `array?`), independent of whether it is ever compared to `null` at all, so row 24's own `heap` carve-out is in practice only ever reachable through a non-nullable array already accepted into `array_params`. Measured on the MBPP-DFY family (given): 5 of the 16 gradable programs whose only census gap was a tail-position break (row 23) are refused by the lifter SOLELY for `requires a != null` on a non-nullable array (`dafny-synthesis_task_id_284`, `_433`, `_733`, `_751`, `_760`). Measured across the full 785-program DafnyBench corpus (raw-source regex over the `.dfy` files, not a full `lift_parse` corpus run: resolving all 785 through `dafny` for this row alone was judged not cheap enough while `out/lift-regress`'s own background sweep was already running on the same box; the regex looks for `IDENT: array<...>` with no `?` and an `IDENT != null`/`null != IDENT` occurrence anywhere in the same file, cross-checked against the 5 known programs above): 290 of the 785 declare at least one non-nullable `array<...>` parameter or return; 40 of those also write `!= null`/`null !=` on that same name somewhere in the file. Cross-referencing those 40 against the (pre-row-24) `out/lift` cache from an earlier corpus sweep: 31 of their gradable methods report `heap`/`null` as classify's FIRST (earliest-line) issue, meaning row 24 removes that block for 31 methods, though `_first` only ever surfaces the earliest issue, so a later, still-blocking gap in the same method is not ruled out by this count alone (only the 5 MBPP-DFY programs above were actually re-lifted end to end, below). Re-lifting the 5 MBPP-DFY programs with row 24 live (`lifter.py --list ... --force --out out/lift --jobs 4`): 2 of 5 (`_284` AllElementsEqual, `_433` IsGreater) lift structurally (`null-check-dropped` fires, `requires` empties out) but fail `lift_check` on the PRE-EXISTING `L_ens`/`L_inv_0` array-vs-seq forall/exists equivalence automation limit row 23 already named: confirmed directly by re-running `dafny verify` on the generated checker file, where `L_req` (the ONE lemma that actually carries the dropped `a != null` comparison) verifies immediately with Dafny's own "always return true" warning, while `L_ens`/`L_inv_0` (which carry no null-check content at all) are what fails, "a postcondition could not be proved on this return path" on the `a[..]`-vs-`a.Length` forall/exists conversion. 3 of 5 (`_733` FindFirstOccurrence, `_751` IsMinHeap, `_760` HasOnlyOneDistinctElement) refuse `unbounded-quantifier` once the null check no longer masks it (a two-variable `forall i, j`, or a `forall`/`exists` whose bound divides by 2 or mixes `&&`-joined ranges, `bound_quantifier` does not reduce): a pre-existing, unrelated gap the null check happened to sit in front of. Decided 2026-09-09, by measurement (the task's own instructions), once row 22's `_is_array_of_int`/`array_params` machinery and row 23's shared-computation pattern already existed to build on. | Refuse `heap` for every `null` comparison as before (drop `scan_null_checks` and `_strip_null_checks`): the 40 corpus programs measured above (5 of them the MBPP-DFY sole-gap set) go back to being blocked by this one clause alone, and `L_req`'s own trivial provability (confirmed above, a useful signal that the rule is sound) is lost along with them. |

| 25 | Dafny sequence display `[e1, ..., en]` and `[]` (int elements) | n/a (t had no seq literal at design time) | n/a | n/a | n/a | Lifts one to one to t's literal (SPEC.md "Sequences: literals, concatenation, slices (v1)", 2026-09-09): `lift_classify._seq_literal_issue` (built on the new shared `expr_kind`/`_build_kind_env` type inference, which also backs rows 26 and 27) accepts a `SeqDisplay` whose every element types `int` and refuses the rest by naming the offending element's own construct, `nested-seq` for a nested display or any other non-int shape and `string-char` for a literal char/string element, exactly the two reasons this construct refused under decision 3 before; `lift_rewrite._lift_expr` gained a `SeqDisplay` case emitting `{"op": "seq", "args": [...]}`, recorded `seq-literal-lifted`; `lift_check._t_expr` gained a matching `seq` case printing `[a, b]` back to Dafny for the checker lemmas. Alongside this row, the pre-existing `seq-return` refusal in `lift_classify.py` (unconditional on any `seq`-kind return since before decision 22) was narrowed: a method returning `seq<int>`/`seq<nat>` no longer refuses `seq-return` at all (t has carried plain seq returns since decision 22's "Sequences as values (v1)" opened 2026-09-09 morning; only `lift_rewrite.rewrite`'s own return-type branch, which defaulted every non-array, non-bool return to `int`, needed a matching fix to also read `seq`), keeping the refusal only for a seq of any OTHER element type (bool, char, nested); a `seq<nat>` return carries no per-element `>= 0` guarantee, the same gap decision 14 already accepts for a `seq<nat>` local/param, deliberately taken here as the weaker theorem. `coverage_census.py`: `seq-literal`, `seq-slice`, `seq-return` and `seq-update` (the functional-update EXPRESSION shape, landed as t's `update` operator by decision 22's own STATEMENT-shape rewrite) all reclassify gap to burden, keys unchanged; `seq-comprehension` (`seq(n, i => e)`, t's `fill` is constant-valued, this is not) stays a gap; a new lexical burden `seq-concat` fires on `+ [`, `] +` or `..] +` (documented as an under-count: a bare `s + t` between two seq NAMES, no bracket adjacent to the `+`, is invisible to it). Two more census refinements landed the same day, at the coordinator's request after a read-only agent's measurement: `zero-returns` narrows to the burden `zero-returns-array` (43 of the 785, "a method with no return whose effect is its one array, lifted as a seq return by row 22") wherever a zero-return method's own `modifies` names exactly one array parameter and its own body mutates only that name, `zero-returns` staying a gap (185 remain) only for a zero-return method with no such array effect; `multi-method` narrows to the burden `multi-method-independent` (86 of the 785) wherever no graded method's body calls another declared method by name, `multi-method` staying a gap (125 remain) only when a cross-call is lexically found. Measured (2026-09-09): `census.json`'s current 643 gradable programs give 51 (not the day's first-pass 50; the discrepancy is unresolved) whose only gaps are a non-empty subset of `{seq-literal, seq-return, seq-slice, seq-comprehension, seq-update}` (file list banked at the task's own scratch path). Re-lifting all 51 with rows 25-27 and the `seq-return` narrowing live (`lifter.py --list ... --force --out out/lift --jobs 4`, then a `--jobs 1` recheck of 3 of them): 5 lift and check clean (`dafny-duck_..._p3` max, `dafny-language-server_..._maximum` Maximum, `dafny-synthesis_task_id_257` Swap, `_261` ElementWiseDivision, `_586` SplitAndAppend); 13 lift but fail `lift_check`: 6 on `L_inv_0` (`_106` AppendArrayToSeq, `_576` IsSublist, `_587` ArrayToSeq, `_618` ElementWiseDivide, `_728` AddLists, `llm-verified-eval_..._dafny_5` intersperse), 2 on `L_req` (`_307` DeepCopySeq, `_743` RotateRight), 1 on `L_fun_power` (`_623` PowerOfListElements), 1 `check-wf-failed` on a pre-existing, unrelated spec_fun-decreases-not-int bug (`dafny-duck_..._p1` SumArray), and 3 `lift-diff-failed`/`timeout` (`_240` ReplaceLastElement, `_273` SubtractSequences, `_445` MultiplyElements) whose status was inconsistent across reruns under shared-box load (checked clean once, timed out twice more, including under `--jobs 1`), unresolved as measurement noise or a real cost; the remaining 33 refuse before ever reaching a task, by first-issue token: `unbounded-quantifier` 15, `function-contract` 7, `let-expression` (parse stage) 4, `seq-slice` 4, `array` 1, `calls-other-method` 1, `return-not-assigned-on-all-paths` 1. Decided 2026-09-09, by measurement, once SPEC.md's "Sequences: literals, concatenation, slices (v1)" and decision 22's seq-valued machinery opened the gate. | Refuse `seq-literal` as before (revert `_seq_literal_issue` to unconditional refusal) and re-widen `seq-return` to refuse every seq-kind return regardless of element type: 97 of the 643 gradable carry a literal (1 sole-blocker today), 66 carry a seq-typed return (0 sole-blocker today, all paired with another of rows 25-27's own gaps), both stay out of the fragment on that gap alone; the 5 checked-clean and however many of the 13 check-failed tasks the literal or the return actually touch lose their task JSON entirely, not just their check verdict. |
| 26 | `+` whose operands are both seq-typed | n/a (t had no seq concatenation at design time) | n/a | n/a | n/a | Lifts to t's own `+`, unchanged in the JSON: `lift_rewrite._lift_expr`'s `Binary` case already emitted `{"op": op, "args": [left, right]}` for every arithmetic operator regardless of operand type (t's `+` is polymorphic by operand type exactly as `==` already is, SPEC.md), so the only new work is DECIDING when a `+` counts as this row rather than plain int addition, and refusing the rest. `lift_classify.py` gained a dedicated pass (alongside rows 25 and 27, sharing `_build_kind_env`/`expr_kind`, a best-effort, non-flow-sensitive `int`/`bool`/`seq` inference over every param, return, local (declared type, or its own initialiser's kind recursively for an untyped `var`), quantifier binder and `for`-loop variable, plus every closure function's/the method's own return kind for a `+` over a self-call or a spec_fun call): both operands typing `seq` accepts the `+` and records `seq-concat-lifted` (`lift_rewrite._lift_expr` records the same rule, using its own `_rw_seq_kind` wrapper over the live `Scope` rather than `classify`'s static env, consulted only for this bookkeeping, never to accept or refuse); both typing `int` leaves the `+` untouched, exactly as before; anything else (one seq and one int, or a kind this row cannot resolve) refuses `seq-typing`. `a[..]` on a read-only array parameter (already a seq by decision 1) is the parameter itself, unchanged: `expr_kind`'s `Ident` case already reads the array param's own declared type as `seq`, so no special case was needed here beyond what row 27's slice handling already gives it. `lift_rewrite.py` also gained a fix an untyped local's own initialiser exposed: `var r := [];`/`var r := r + [x];` with NO declared type used to default to `int` unconditionally (`_t_type_of(None)`), sound only while a seq-shaped initialiser was always refused before reaching that code; it now falls back to `_rw_seq_kind` of the initialiser when no declared type is given. A first pass of this row's own type inference under-counted: `forall i :: ... ==> r[|s| + i] == a[i]`'s `|s| + i`, a quantifier binder never added to the env, was misread as an unresolvable operand and wrongly refused `seq-typing`, caught by the `AppendArrayToSeq` test case and fixed by adding quantifier binders and `for`-loop variables to `_build_kind_env`, both always `int`-kinded in this lifter's own accepted subset (section 6/decision 15). Measured together with rows 25 and 27 over the same 51 programs (see row 25's cell for the full breakdown); `SplitAndAppend` (prepend), `AppendArrayToSeq` (append-in-a-loop, array-element operand) and `IsSublist`/`ArrayToSeq` (concatenation reached through a slice) are the row's own worked cases among the 5 clean and 13 check-failed. `coverage_census.py`'s new lexical burden `seq-concat` (`+ [`, `] +`, `..] +`) counts 155 of the 785, an admitted under-count (see row 25's cell). Decided 2026-09-09, by measurement, once row 25's `expr_kind` machinery existed to build on. | Refuse `+` between two seqs as before (drop the dedicated pass and the `seq-typing` reason, back to no detection at all): every program whose `seq-concat` burden is its own path into the fragment loses that path; `seq-typing`'s own false-positive history above is the reason this row exists at all, so reversing loses that signal along with the acceptance. |
| 27 | `s[a..b]`, `s[a..]`, `s[..b]` on a seq-typed receiver | n/a (t had no seq slicing at design time) | n/a | n/a | n/a | Lifts to t's `{"op": "slice", "args": [s, a, b]}`, the two sugars expanded to the three-argument form the AST always carries (SPEC.md): `s[a..]` becomes `s[a..len(s)]`, `s[..b]` becomes `s[0..b]`. `lift_classify.py`'s dedicated pass (row 26's cell) accepts a `Slice` node when its base types `seq` via `expr_kind`, UNLESS it is decision 22's own mutated array's BOUNDED slice (`a[lo..hi]` on the ONE `modifies`-param array), which stays refused `seq-slice` exactly as row 22 already states; the `id(n) in accepted_ids` unbounded exemption decision 22 already carries for that same array is untouched. A slice with NEITHER bound given (`s[..]`) is not one of SPEC's two sugars at all and is unchanged from before: identity on the base, recorded `whole-slice-as-seq`, now reachable for any seq-typed base (a read-only array parameter included, "`a[..]` on an array parameter is the parameter itself", not only the row-22 mutated array as before); a slice with AT LEAST ONE bound given is this row's own new territory, recorded `seq-slice-lifted`. `lift_rewrite._lift_expr`'s `Slice` case, which used to raise on any bounded slice ("a lift_classify bug: only the unbounded form is ever accepted"), now lifts the base once and builds the three-argument form with `{"int": 0}`/`{"op": "len", "args": [base]}` standing in for the omitted bound. `lift_check._t_expr` gained a `slice` case printing `s[a..b]` back to Dafny for the checker lemmas and the differential run; `_POINT_RE`/`_dafny_value_matches` already parsed a bracketed seq value (decision 22). No definedness check was added anywhere: a lifted slice's `0 <= a <= b <= len(s)` obligation is t's own rule (SPEC.md's Definedness section), not the lifter's business, per the task's own instruction. Measured together with rows 25 and 26 over the same 51 programs (row 25's cell); pre-seqops, `seq-slice` was carried by 147 of the 643 gradable, 18 of them its SOLE blocker (`census.2026-09-09-pre-seqops.json`, banked before this row). Decided 2026-09-09, by measurement, once row 25's `expr_kind` machinery and row 22's mutated-array exemption already existed to build on. | Refuse `seq-slice` as before (bounded and unbounded alike, drop `expr_kind`'s role in this row and restore the unconditional refusal outside `accepted_ids`): 147 of the 643 gradable carry a slice, 18 of them lose their sole path into the fragment, and the read-only-array `a[..]` case falls back to refused too (it was never accepted before this row). |

| 28 | Dafny `string`/`char`: type, literal, `\|s\|`/index/`+`/slice/equality, char comparison, `as int`/`as char` | n/a (t had no char/string at design time) | n/a | n/a | n/a | Lifts one to one to t's existing int/seq (SPEC.md "Strings as sequences of code points (v1)"), no new AST form: `string` in a param, return or local is the same t `seq` the lifter already gives `seq<int>` (`lift_classify._type_issue`'s `char`/`string` branch, which used to refuse both unconditionally as `string-char`, now returns `None`; `seq<char>` reads the same way, `_is_seq_of_char` added alongside `_is_seq_of_int`/`_is_seq_of_nat`); `char` is a plain t int (`_declared_kind`/`expr_kind` fold it into the SAME `"int"` kind rows 25-27 already use, so `len`/`at`/`+`/slice/`==`/`!=` need no new code at all once the type checks open). A char literal `'a'` (and its escapes) is `{"int": n}`, a string literal `"abc"` the seq literal of code points, `""` the empty one: `lift_classify.decode_char_literal`/`decode_string_literal` (shared with `lift_rewrite._lift_expr`'s new `CharLit`/`StringLit` cases, imported the way `expr_kind` already is) decode `\n` `\t` `\r` `\0` `\'` `\"` `\\` (measured on dafny 4.11.0: 10 9 13 0 39 34 92) and the full-range escape `\U{H+}` (measured: the ONLY valid spelling of a non-ASCII code point on this dafny, `\u{H+}` -- lowercase u -- and `\{H+}` -- no letter at all -- both parse errors), refusing `char-literal-nonbmp` for anything else or a code point above `0xFFFF` (SPEC.md's own words, "a literal outside the Basic Multilingual Plane is refused rather than guessed" -- measured stale as a claim about THIS dafny's default, which is full Unicode `[0, 1114111]` already with no `--unicode-char` flag needed, `t8.dfy`/`t9.dfy` in the task's own scratch notes, but kept as the literal-decoding policy anyway, deliberately more conservative than the value bound below). `lift_parse.py`'s char tokenizer had a bug this row's own decoding exposed: it checked for a bare `{` after a backslash (never valid Dafny) instead of `U{` (the only real escape), silently truncating any `\U{...}` literal instead of handing classify a well-formed token to refuse by name; fixed. `c as int` is the identity, always (`_is_char_expr` recognises a char-typed operand: a `CharLit`, a name declared `char`, `old(...)` of one, an `IfExpr` of two, or a same-typed `Cast`); `n as char` is the identity ONLY when `_char_cast_safe` can see the operand is already a code point (an `IntLit` in `[0, 1114111]`, or a `Cast` to `int` of an already char-typed operand, "a char cast back"), else refused `char-cast-unbounded` -- `Cast` handling MOVED WHOLESALE out of `lift_classify._scan_node_for_issues`'s old unconditional `as-cast` into a dedicated pass (the same move rows 25-27 made for `SeqDisplay`/`Slice`/`+`), so every OTHER cast (`as nat`, `as real`, ...) is unaffected. `==`/`!=`/`<`/`<=`/`>`/`>=` between two chars are the same int comparisons rows 25-27's `_lift_chain` already prints; an ORDER comparison (`<`/`<=`/`>`/`>=`, never `==`/`!=`) where either operand types `seq` (string or plain `seq<int>` alike, `expr_kind` cannot and need not tell them apart) refuses `string-lib`, a new dedicated Chain check -- measured on dafny 4.11.0 (`t6.dfy`/`t6run.dfy`) that this is PROPER-PREFIX semantics, not the lexicographic order a first guess assumes (`"ac" < "b"` and `"b" < "ac"` both false), confirmed by `AssertivePrograming_tmp_tmpwf43uz0e_Find_Substring.dfy`'s OWN comment on the exact clause this row's run refused, "`<=` on sequences is the prefix relation" -- still not a t operator on seqs either way (SPEC.md: "Lexicographic order on strings is not an operator"). `c1 + c2`/`c1 - c2` between two char-typed operands verify on dafny 4.11.0 with an overflow/underflow proof obligation t's plain, unbounded int has no way to state (measured, `t8.dfy` "char addition might overflow", `t4.dfy` "char subtraction might underflow"); refused `char-arith` by a new Binary check ahead of rows 25-27's own `+` handling (char types `"int"` under `expr_kind` by design and would otherwise silently pass as ordinary arithmetic) -- `*`/`/`/`%` on char are Dafny TYPE ERRORS (measured, `t5.dfy`), never reachable. `s in t` (a substring test) needs no detector: measured directly (`t7.dfy`) NOT to type-check in Dafny at all when both sides are string ("expecting element type to be assignable to char (got string)"), structurally impossible in a verified corpus program. `multiset`/`set` of char stay refused by the PRE-EXISTING `multiset`/`set` type checks alone (`_type_issue`), unaffected by and unrelated to this row; a nested string (`seq<string>`, `seq<seq<char>>`) is `nested-seq`, the same bucket a nested int display already uses (`_seq_literal_issue`'s fallback: a `StringLit` element now types `"seq"` under `expr_kind`, not `"int"`, so a display of strings reads as a display of displays). Two correctness additions beyond this closed list, load-bearing rather than optional: `char-param-guard`/`char-return-guard` (a `requires`/`ensures 0 <= v <= 1114111`, one `_char_range` clause, mirroring decision 4/6's `nat-param-guard`/`nat-return-ensures`) and `string-elements-requires` (the same bound, `forall`-quantified over every element, mirroring `nat-elements-requires`) -- without them the differential harness's own conversion of an interp-sampled point back into a Dafny `char`/`string` (needed since the SOURCE parameter is genuinely char/string-typed) can pick a code point outside `[0, 1114111]` and CRASH the whole `dafny run` process outright, measured directly (`trun.dfy`: "Unhandled exception... Value does not fall within the expected range"), not merely mis-compare; no analogous bound was added for a STRING RETURN's own elements (decision 14's own weaker-theorem precedent for `seq<nat>`, deliberately reused here), since that direction (`char as int`) never needs one. `lift_check.py` gained matching machinery: `_print_expr`'s `CharLit`/`StringLit` cases were fixed (they double-wrapped the surrounding quotes, `''a''` for `'a'`, unreachable before this row since string/char always refused earlier and so untested); decision 1's own `array_view` frozenset generalised to a `views` dict (`_view_text`) also carrying `"string"`/`"char"`, substituting a bounded `seq(\|s\|, (k: int) requires 0 <= k < \|s\| => s[k] as int)` comprehension or a plain `(c as int)` wherever a checker lemma's LIFTED-side clause references a name the lemma signature declares in the SOURCE's own `string`/`char` type; a closure FUNCTION's own char/string param is a SEPARATE scope from the method's own params and needed its own `_fn_param_views`, caught directly by `dafny-synthesis_task_id_113` IsInteger's own `isDigit(c: char)` call ("incorrect argument type for function parameter 'c' (expected int, found char)" before the fix). The differential harness (`_build_differential_with_points`) gained the same conversions on the call side (a `seq(...)` cast to build a `string` argument, `as char` for a scalar one, both safe only because the guards above bound the sampled point first) and a shared `_ret_view` helper (generalising decision 22's array-specific seq-view) for printing/comparing the SOURCE's own return value, since `dafny run` prints a `string`/`char` value in ITS OWN shape (`abc`, unquoted, unbracketed) never t's `seq<int>`/int form, measured directly (`tprint.dfy`: `Id("abc")` prints `abc`, not `[97, 98, 99]`). Measured on the 17 DafnyBench programs the census blocks solely on `string-char` (`COVERAGE-dafnybench.md`'s own greedy-gate step 2, 13 of them also `COVERAGE-mbpp-dfy.md`'s own sole-blocker count for the MBPP-DFY 164; `lifter.py --list ... --force --out out/lift-strings --jobs 6`, run under `tjob`): 5 lift and pass every check (`dafny-synthesis_task_id_242` CountCharacters, `_238` CountNonEmptySubstrings, `_269` AsciiValue -- exactly `c as int`, `_396` StartAndEndWithSameChar -- `s[0] == s[\|s\|-1]`, `_79` IsLengthOdd); 4 lift but fail `lift_check`: 3 on `L_ens` (`Clover_all_digits` allDigits, `Clover_match` Match, `_759` IsDecimalWithTwoPrecision), the same PRE-EXISTING quantifier-equivalence automation limit rows 25-27 already named, an empty `{ }` lemma body Dafny's own SMT cannot discharge unprompted, and 1 on `L_fun_isDigit` (`_113` IsInteger) for a DIFFERENT pre-existing, unrelated gap this row's own `isDigit` call happens to be the first to trip: `_find_fun_calls`/`inv_hints` (the `L_inv` hint-line generator) prints a spec_fun call embedded inside a QUANTIFIED invariant (`forall k_v :: ... isDigit(s[k_v])`) using the call's own bound-variable text verbatim at the LEMMA BODY level, where that quantifier's binder is not in scope ("unresolved identifier: k_v") -- unrelated to char/string typing (the same bug would fire for any spec_fun called this way over plain ints) and not fixed here, named as a residual; 1 refuses `multi-return` (`_602` FindFirstRepeatedChar, a pre-existing, unrelated gap paired with `string-char` in the census already); 1 refuses `string-lib` (`AssertivePrograming_tmp_tmpwf43uz0e_Find_Substring`'s own `str2 <= str1[offset..]`, this row's own worked case above); 6 refuse `unbounded-quantifier`, a pre-existing, unrelated gap the census's greedy step reveals once `string-char` no longer masks it (the same "next gap" pattern rows 23/24 already documented). Ran 3 of the 5 clean tasks through 2 kernels (dafny, framac; `out/agent-strings`, `harness.run_task` with `harness.OUT` redirected, never the full sweep): CountCharacters and AsciiValue have NO twin on either kernel (straight-line, nothing to mutate, `no-operator`); StartAndEndWithSameChar COUNTS on both (real VERIFIED, `off-by-one` twin REFUTED, witness `s=[0]`). `coverage_census.py`'s `string-char` gap splits the way `t/nl_census.py` already splits its own string gap: `string-as-seq` (burden: the old regex plus the literal-seen forcing, now describing what lifts) and `string-lib` (gap: an order comparison lexically adjacent to a string literal, or `multiset`/`set` of char -- lexical and approximate, UNDER-counts an order comparison between two bare string-typed NAMES the way `seq-concat` already under-counts a bracket-free `+`, measured directly: the census's own lexical `string-lib` detector does NOT catch `AssertivePrograming`'s `str2 <= str1[offset..]` at all, no literal on either side, while the real lifter's type-aware check does). Regenerated `COVERAGE-dafnybench.md` and `COVERAGE-mbpp-dfy.md` with `coverage_census.py`'s own documented invocation (its module docstring): DafnyBench 292 of 643 gradable in fragment (45.4%, up from 277/643/43.1%, +15, the lexical sole-`string-char`/`{string-char,multi-return}` count -- 2 short of the run's own 17 since the census's lexical `string-lib` under-count leaves `AssertivePrograming` looking clean and `_602`'s `multi-return` stays open regardless); MBPP-DFY 118 of 164 gradable in fragment (72.0%, up from 105/164/64.0%, +13, matching the pure sole-blocker count exactly since none of the 13 pairs with another open gate). `COVERAGE-mbpp-dfy.md`'s format changed as a side effect: it used to be generated by `mbpp_gate_order.py` (a two-instrument report needing a completed `out/lift` run over the full 785-program corpus for its "what the lifter actually did" half); regenerated here via `coverage_census.py` directly on the 164-file MBPP-DFY subset instead, reproducing the census half faithfully but dropping the lifter half, which needs a full corpus lift this task's own scope (17 candidates, not 785) did not call for. Decided 2026-09-09, by measurement, once rows 25-27's `expr_kind`/`_build_kind_env` machinery and decision 22's array-view checker pattern already existed to build on. | Refuse `string-char` for every `string`/`char` type, literal, comparison and cast as before (revert `_type_issue`'s `char`/`string` branch, drop the dedicated Chain/Binary/Cast/CharLit/StringLit pass and the `char-param-guard`/`string-elements-requires` clauses, restore `_scan_node_for_issues`'s unconditional `as-cast` refusal for `Cast`): 94 of the 643 DafnyBench gradable carry a string or char (15 lose their sole path into the fragment, 2 more once `multi-return` is also open), 25 of the 164 MBPP-DFY (13 sole), and every one of the 5 checked-clean and 4 check-failed tasks above loses its task JSON entirely, not just its check verdict. |
| 29 | Dafny method with two return values, `returns (a: T1, b: T2)` | n/a (t had no pair type at design time) | n/a | n/a | n/a | Lifts to one pair-typed return (SPEC.md "Pairs (v1)"): `returns (a: T1, b: T2)` becomes a t task with ONE return `r: (T1, T2)`; `a` and `b` become locals (`lift_rewrite.rewrite`'s new `pair_returns` branch, named "r" via `renamer.fresh("r", ...)` since there is no single Dafny identifier for the combined pair the way a single return already has one, and `a`/`b` keep their own names, `renamer.fresh(ret_a.name, ...)`), default-initialised the way decision 13 already initialises an uninitialised local (0/false/`[]`; Dafny leaves an out-parameter unassigned, and every reachable exit already assigns both before it is read or the method returns, section 4.7's definite-assignment check re-run once per component, so the default is never actually observed); every path that reaches the end of the method combines the two locals into `r` (one `{"assign": ["r", {"op": "pair", "args": [a, b]}]}` appended after the whole body is lifted), and every explicit `return` statement (a bare `return;`, `return a, b;`, or `return e1, e2;`, anywhere in the body, tail or not) ends the same way, `{"return": ["r", {"op": "pair", ...}]}` for a non-tail one via `_lift_stmt`'s new `scope.pair_ret`-aware branch, an ordinary parallel `Assign` (decision 22's own multi-target machinery, reused unchanged) for a tail one via `_desugar_returns`'s new `ret_names` parameter. In `requires`/`ensures`, `a` becomes `r.0` and `b` becomes `r.1` (`scope.pair_view`, consulted by `_lift_expr`'s `Ident` case before `scope.renames`, cleared to `{}` on `body_scope` so it can never fire past that point); inside the body, AND in loop invariants, `a`/`b` stay the plain locals -- a deliberate reading of SPEC.md's own text, not the most literal one: `r` is only ever combined at the body's own exits, never kept in sync during a loop, so a loop invariant that projected it (`r.0`/`r.1`) would compare an UNCONSTRAINED fresh value against the loop's own running state, unsound and (measured, see below) provably false; this is also exactly what the two hand-written committed tasks (`divmod_pair`, `min_max`) already do, `min_max.json`'s own loop invariants reading `lo`/`hi` directly, never `r.0`/`r.1`. A `nat` component states its own non-negativity on its own projection (`r.0 >= 0`/`r.1 >= 0`, `_ge0_of`, decision 4's own fact relocated since a pair has no order and `r >= 0` is not well-typed); component types int/nat/bool/seq<int\|nat\|char>/string all lift (`lift_classify._pair_component_issue`, mirroring what `_type_issue`/rows 25-28 already carry as a single return), `string` folding to `seq` per row 28. Refused, by name: `multi-return-arity` (three or more returns, SPEC's own name for it); `multi-return-nested` (an arity-two method with an unsupported component -- a bare `char` (row 28 folds it into `int` only for a single return's own `t_ret`; doing the same on a pair component needs a guard on the PROJECTION, not on `r`, narrowed out of this row's own scope), `array<int>`/`array<nat>` (row 22's alloc-fill/modifies-param machinery is built around exactly one return; combining it with a second, independent component is also narrowed out), a Dafny bitvector/real/map/multiset/nested-tuple component, a nested seq, or a destructuring `a, b := M(x);` self-call assignment, ONE value for TWO targets, a shape `lift_rewrite._lift_stmt`'s general multi-target `Assign` handling would `zip` and silently truncate rather than lift correctly); and anything the existing rows already refuse for a single return (`return-not-assigned-on-all-paths`, checked once per component; every param/body-construct check unchanged). `lift_classify._build_kind_env` (rows 25-27's own `expr_kind` machinery) used to read only `method.returns[0]`'s own kind, leaving a pair method's `a`/`b` unresolved and wrongly refusing `seq-typing` on an ordinary int `+`; widened to every entry of `method.returns`. Three bugs in files this row does not otherwise touch were found and fixed along the way, none of them in `lower_dafny.py` (left alone throughout, per this task's own instruction): `lift_check._t_expr` had no `pair`/`fst`/`snd` case at all (unreachable before a pair task existed), added, printing Dafny's own native tuple literal and projection (`(a, b)`, `.0`, `.1`, independent of whatever per-kernel representation `lower_dafny.py` eventually settles on -- this function only ever feeds the source-vs-lifted equivalence lemmas, never that module); `lifter.py`'s `_write_outcome` crashed the whole `--list` run outright (`TypeError: Object of type Pair is not JSON serializable`) the first time a pair task's `interp_first_value` reached `json.dump`, fixed by routing it through `interp._j`, the same JSON-safety conversion `interp.Reference.witness` already applies to a `Pair` (a recursive 2-list); `lift_check._build_checker_parts` assumed exactly one source return everywhere it touched the return (`ret_type_src` only ever read `source.returns[0].type`, `_clause_rename` only ever mapped `source.returns[0].name` onto the task's one return name, `ret_type_clause` only ever stated one nat fact), each generalised to the pair case (a synthetic `Type(kind="tuple", args=(T1, T2))`, reusing `_print_type`'s EXISTING tuple-printing from decision 11; both out-parameter names mapped to `"{r}.0"`/`"{r}.1"` TEXT, since `_print_expr`'s `Ident` case is already a flat string substitution and `r.0`/`r.1` is already valid Dafny; the AND of each nat component's own projected fact). The DOMINANT `lift-check-failed` cause measured below (12 of 13, before this fix) was a loop that computes DIRECTLY into its own out-parameters with no separate `lo`/`hi`-style locals (`CalDiv`'s own shape: `x, y := 0, 191; while ... invariant 0 <= y && 7*x+y == 191`): the SOURCE invariant's `x`/`y` were rewritten by the SAME ensures-level `r.0`/`r.1` mapping, comparing an unconstrained lemma parameter against the lifted side's own plain locals inside `L_inv_k`, provably false; fixed by exploiting that `rewrite()` always emits the pair's own two locals as the FIRST two statements of the lifted body, so `_task_loop_scopes`'s report for every loop always starts with their task names, stripped off the front (leaving the existing from-the-end `for`-desugaring alignment heuristic, and every single-return program it already serves, untouched) and reattached explicitly to the lemma's own parameter list and to `loop_crename`. Measured on 74 DafnyBench programs coverage_census.py tags `multi-return-pair` or `multi-return-arity` (the union, `lifter.py --list ... --force --out out/lift-pairs --jobs 6`, run under `tjob`), cross-checked against an independent AST scan (`lift_resolve.resolve` + `lift_parse.parse` on each file, every `MethodDecl` with two or more returns): 77 raw multi-return methods, 4 not gradable (no ensures of their own, excluded from the population the same way decision 9 always excludes them) leaving 73 gradable, exactly SPEC.md's own independent shape measurement of 73; of the 73, 7 arity three or more refuse `multi-return-arity`, and of the 66 arity-two methods, 6 refuse `multi-return-nested` (component types array x2, `bv8` x1, `real` x1, `char` x1, one destructuring self-call assign) leaving 60 pair-supported: 30 lift and pass every check (`checked=True`, no refusal at any stage, types `(int,int)` x25, `(nat,nat)` x2, `(int,nat)` x1, `(seq,seq)` x1, `(int,bool)` x1); 1 lifts but fails the checker, `lift-check-failed:L_fun_isOdd` (`dafny-synthesis_task_id_807`), a PRE-EXISTING, unrelated automation limit row 28 already named (a spec_fun call embedded inside a quantified invariant, its own bound variable out of scope at the lemma-body level), not fixed here either, matching that row's own precedent; and 29 refuse for an earlier, unrelated gap the census's greedy order reveals now that `multi-return` no longer masks it (the "next gap" pattern rows 23-28 already document): `unbounded-quantifier` 7, `bodyless-method` 5, `function-contract` 3, `nondet` 3, `check-wf-failed` 2 (a pre-existing spec_fun-decreases-not-int bug), `return-not-assigned-on-all-paths` 2 (a genuine unassigned path, this row's own definite-assignment check working as intended), `array-mutation` 2 (`modifies-with-existing-return`, decision 22's own demotion), `string-lib` 1, `generics` 1, `seq-typing` 1, `calls-other-method` 1, `array` 1. The differential harness (`_build_differential_with_points`, `lower_dafny.lower`-dependent) reported `differential_verdict: arm-unavailable` on the one task inspected directly (`Clover_double_quadruple`, a Dafny error inside the generated `Main` harness), per this task's own caution about the other agent's concurrent pairs work on `lower_dafny.py`; NOT a refusal (arm-unavailable is recorded, not refused, `lift_check.check`'s own step-5 contract), so it never gated the "lifted, checked=True" count above, and `lower_dafny.py` itself was not investigated further or touched. `test_lift_rules.py`: 77/77 infragment and 21/21 seeds agree, unchanged, both before and after every change in this row; six new unit tests added (`(int,int)` loop-free, `(int,int)` computed in a loop with an invariant reading both locals, `(bool,int)` flag-and-value with a bare early `return;`, `(seq<int>,int)`, a non-tail `return y, x;` in the middle of the body, and the arity/nested refusals), each also checked semantically against `interp.exec_body` on hand-picked inputs, not just `check_wf`; all pass. `coverage_census.py`: the old unconditional gap `multi-return` is renamed `multi-return-arity` (three or more returns, or an arity-two method with an unsupported component, read lexically via the new `_multi_return_shapes`/`_split_top_level_commas`/`_pair_component_supported`, the same approximate, per-`returns(...)`-occurrence reading `_multi_return` always used); the new burden `multi-return-pair` fires when both components lexically read as supported. Regenerated `COVERAGE-dafnybench.md`/`COVERAGE-mbpp-dfy.md` with the census script's own documented invocation: DafnyBench 321 of 643 gradable in fragment (49.9%, up from 292/643/45.4%, +29, the census's own lexical estimate, coarser than the per-method count above since a program counts only when EVERY gradable method in it lifts); MBPP-DFY 120 of 164 gradable (73.2%, up from 118/164/72.0%, +2). `multi-return` (74 DafnyBench programs, sole blocker for 34; 5 MBPP-DFY, sole 3) splits into `multi-return-arity` (17 DafnyBench, sole 5; 2 MBPP-DFY, sole 1) and `multi-return-pair` (58 DafnyBench; 3 MBPP-DFY). Decided 2026-09-09, by measurement, once SPEC.md's "Pairs (v1)" and the core interp/check_wf/surface support (commit 5991e8b) opened the gate. | Refuse `multi-return` for every arity as before (revert `lift_classify`'s returns section to its old unconditional `elif len(method.returns) > 1: issues.append(...)`, drop `_pair_component_issue`, `Scope.pair_view`/`pair_ret`, the `_desugar_returns`/`_lift_stmt` `ret_names`/`pair_ret` branches, `lift_check`'s pair cases in `_t_expr`/`_clause_rename`/`ret_type_src`/`ret_type_clause`/`L_inv_k`, and `coverage_census.py`'s split back to one `multi-return` gap): 58 of the 785 DafnyBench programs carrying `multi-return-pair` (3 of the 164 MBPP-DFY) lose that path into the fragment, and the 30 checked-clean tasks measured above lose their task JSON entirely, not just their check verdict; the 6 `multi-return-nested` and 7 `multi-return-arity` programs were already refused, unaffected either way. |

| 30 | Dafny `seq<seq<int>>` and `seq<seq<nat>>` in a parameter, return or local | n/a (t had no nested seq at design time) | n/a | n/a | n/a | Lifts to t's own `{"seq": "seq"}` (SPEC.md "Nested sequences (v1)"): no new Expr form, every existing seq operator polymorphic at any nesting depth, exactly as SPEC states. `lift_classify._type_issue` gained a nested branch: `seq<seq<int>>` (a param, return or local) is `None` (accepted); `seq<seq<nat>>` is `nat-seq-elements` for a param/local (the same no-per-row-guard gap a flat `seq<nat>` param already has) but accepted for a RETURN, the same weaker-theorem exemption a flat `seq<nat>` return already carries, moved into `classify`'s own return-handling branch alongside the existing `_is_seq_of_int`/`_is_seq_of_nat`/`_is_seq_of_char` check (now also `_is_nested_seq_of_int`/`_is_nested_seq_of_nat`). Refused by name, distinctly: `seq-of-bool` (`seq<bool>`, one level), `nested-seq-string` (`seq<string>`, `seq<seq<char>>`), `nested-seq-deep` (three or more levels), `nested-seq-other` (anything else at two levels, `seq<seq<bool>>`/`seq<seq<real>>`/a datatype row), `array2` (`array2<int>`/`array3<int>`, a matrix with its own indexing -- a DIFFERENT Dafny type from `seq<seq<int>>`; this grammar has no dedicated array2 keyword so `array2<int>` parses as a generic `id` type, which `_type_issue` now recognises by name before it falls into the unrelated `generics` gap). `array<seq<int>>`/`seq<array<int>>`: decided by analogy to row 22's own reasoning, both stay refused, neither gets a new name. `array<seq<int>>` is already refused today: row 22's `_is_array_of_int` requires an int-like element, so an array of seq elements falls straight into the plain `array` refusal, unchanged by this row. `seq<array<int>>` is new code (`_type_issue`'s seq branch, row is `array`-kinded), also refused `array`: row 22's array-as-seq-value machinery is built around exactly ONE top-level array per method, in one of two shapes (a `modifies`-param mutation or an alloc-fill return) with the source's own single Dafny identifier standing for both the pre- and post-mutation value; an array nested inside another seq VALUE is a second, independent array position that machinery never reaches, and row 22's own residual list already refuses aliasing it cannot verify past ("more than one mutated array per method", "a mutated array also passed to another call") -- the same reasoning extends to an array living inside a seq. `lift_classify._declared_kind` (rows 25-27's `int`/`bool`/`seq` kind inference) widened to read a nested seq as `seq` too (unchanged vocabulary, `+`/slice bookkeeping needs "is it seq-typed", never how deep). `lift_classify._seq_literal_issue` (row 25) widened: a nested display, `[[1, 2], [3]]`, now lifts (`None`) when every element is itself an int-typed inner display or any other seq-typed expression (a name, a slice, a call -- this best-effort, non-flow-sensitive scan cannot see whether that row is flat or nests a level too deep, the same reading `_type_issue`'s own declared-type check settles precisely wherever the literal sits in a typed position); a string-literal row, or a further nested inner display, refuses `nested-seq-string`/`nested-seq-deep` by name instead of the old unconditional `nested-seq`. `lift_rewrite.py`: `_lift_expr`'s existing `Index`/`Cardinality`/`SeqDisplay`/`Slice`/`+` cases already recurse and already print polymorphically -- `s[i][j]` is `Index(Index(s, i), j)`, already `{"op": "at", "args": [{"op": "at", ...}, j]}` with zero new code; a nested display's elements, themselves `SeqDisplay` nodes, already recurse into nested `{"op": "seq", ...}` the same way. The one genuinely new piece: a new `_t_json_type(t)` function gives a param/return/typed-local's emitted JSON `"type"` entry the compound `{"seq": "seq"}` for a nested shape (`_t_type_of` itself stays the flat `"int"|"bool"|"seq"` string it already was, still used unchanged for the `+`/slice/default-value bookkeeping that never needs nesting depth), threaded through every site that builds that field (params, returns, both `VarDeclStmt` branches, `_lift_function`'s spec_fun params); an untyped local's own nested-ness (`var r := m;` with no declared type, for a nested `m`) is a best-effort guess from a nested LITERAL initialiser only, not from reading another name's own declared type, a known, narrow residual (SPEC's own measured shapes all give the accumulator an explicit declared type). `s[i := row]` (Dafny's functional seq-update EXPRESSION, distinct from the array element-assignment STATEMENT decision 22 already maps) was, before this row, unconditionally refused `seq-update` by `_scan_node_for_issues` with no rewrite path at all despite the census already counting it a burden; moved to the same dedicated, type-aware pass as `+`/slice (accepted when the base types `seq`, flat or nested alike, recorded `seq-update-lifted`), and `lift_rewrite._lift_expr` gained a `SeqUpdate` case emitting `{"op": "update", "args": [base, index, value]}`, the same operator decision 22's array-mutation statement rewrite already targets; `lift_check._t_expr` had no `update`/`fill` case either (unreachable before either row 22's array-mutation ensures/invariants or this row's own expression form could put one there), added, printing `s[i := v]`/`seq(n, _ => r)`. A genuine two-binder quantifier (`forall i, j`, not the nested single-binder `forall i :: forall j :: ...` form this row's own test exercises) stays refused `unbounded-quantifier`, the standing refusal, untouched by this row. Recorded `nested-seq-lifted` implicitly (no dedicated rewrite tag beyond the existing `seq-literal-lifted`/`seq-concat-lifted`/`seq-slice-lifted`/`seq-update-lifted`, since no new Expr form exists to tag). `test_lift_rules.py`: 57 unit tests plus 21/21 seed acceptance and 77/77 infragment acceptance passed before this row (measured against a git-HEAD copy of the four files this row touches, restored after, so as not to disturb the concurrent nested-lowering agent's own uncommitted `lower_*.py`/`fuzz_lower.py` work in the same tree); two PRE-EXISTING assertions had to change to match this row's own deliberate behaviour change, not a regression (`test_seq_literal_lifted`'s nested-display sub-case used to assert `nested-seq` refusal, now asserts the literal lifts, with a new three-level sub-case asserting `nested-seq-deep` instead; its display-of-strings sub-case now asserts `nested-seq-string`, not the old bare `nested-seq`; `test_char_string_refusals`'s `seq<string>` sub-case likewise now asserts `nested-seq-string`); six new tests added (a nested parameter read row by row under a single-binder `forall i` with a `|m[i]|` fact, `m[i][j]` guarded by both `0 <= i < |m|` and `0 <= j < |m[i]|` plus a two-binder `forall i, j` staying refused `unbounded-quantifier`, a nested display literal, a row update `m[i := row]`, a `seq<string>` refusal, an `array2<int>` refusal); 63 unit tests plus 21/21 seeds and 77/77 infragment pass after (current, 2026-09-10: 62 `def test_` functions in t/test_lift_rules.py). Measured on the 12 DafnyBench programs `coverage_census.py` tags `nested-seq` (10 sole blocker; a fresh run, not the prior night's count, `census.json` banked at the task's own scratch path; `lifter.py --list ... --force --out out/lift-nested --jobs 6`, run under `tjob`): 1 lifts and passes every checker lemma (`checked=True`, `dafny-synthesis_task_id_792` CountLists) though its OWN differential arm reports `arm-unavailable: TypeError: unhashable type: 'dict'` (a NEW defensive catch this row adds around `lift_check.check`'s `_build_differential_with_points` call, since `lower_dafny.TYPES` -- owned by the concurrent nested-lowering agent's own in-progress work on `lower_dafny.py`, untouched here -- has no entry yet for the compound `{"seq": "seq"}` key, an unhashable dict; recorded arm-unavailable rather than crashing the whole check, matching row 29's own precedent for an unready `lower_dafny.py` arm); 4 lift but fail the checker, 3 on `L_inv_0` (`dafny-synthesis_task_id_460` GetFirstElements, `_69` ContainsSequence, `_95` SmallestListLength) and 1 on `L_req` (`_401` IndexWiseAddition), all PRE-EXISTING automation limits this row's own type acceptance newly exposes rather than causes (the same "next gap" pattern rows 23-29 already document, reached at the lemma-body level, before the differential harness or `lower_dafny.py` are ever invoked); the remaining 7 refuse for an earlier, unrelated gap: `unbounded-quantifier` 3 (`_290` MaxLengthList, `_457` MinLengthSublist, `_70` AllSequencesEqualLength), `array` 2 (`_143` CountArrays, `_94` MinSecondValueFirst), `set` 1 (`Program-Verification-Dataset..._BoatsToSavePeople`), `zero-returns` 1 (`Clover_modify_2d_array`); none reached a Dafny-level nested-type lowering error (the one place this row's own work touches the concurrent lowering agent's territory is the differential-arm crash above, caught rather than retried since it never blocked `checked=True`). `coverage_census.py`: the `nested-seq` detector (one level, int/nat rows, array\d* combinations lexically folded in though the real classifier refuses those `array`/`array2`) reclassifies gap to burden; `seq-of-bool`/`nested-seq-string`/`nested-seq-deep`/`nested-seq-other` stay gaps, keys and detector functions unchanged. Regenerated `COVERAGE-dafnybench.md`/`COVERAGE-mbpp-dfy.md` with the census script's own documented invocation (`MBPP-DFY` run against a scratch symlink directory of the 164 `dafny-synthesis_task_id_*.dfy` files, `--name` set to match the corpus's usual title): DafnyBench 334 of 643 gradable in fragment (51.9%, up from 324/643/50.4%, +10, exactly the sole-blocker count); MBPP-DFY 131 of 164 gradable in fragment (79.9%, up from 121/164/73.8%, +10, since all 10 sole-blocker programs are `dafny-synthesis_task_id_*` files, the MBPP-DFY tier's own naming pattern). Two further residuals named rather than fixed: `lift_rewrite.py`'s multi-target parallel-assign machinery (decision 22's own `a[i], b := e1, e2;` shape) still hardcodes an INDEX target's temp as plain `int` ("v1's only array/seq element type" at the time that comment was written); a nested seq's row-typed index target would need `{"seq": "seq"}` there instead, untouched since no candidate in this row's own population exercises it; and `_lift_function`'s spec_fun closure params take a nested seq's JSON type from `_t_json_type` (fixed, one line) but this row's own test population never exercises a closure function with a nested-seq PARAMETER end to end. Decided 2026-09-10, by measurement, once SPEC.md's "Nested sequences (v1)" and the core interp/check_wf/surface support (commit 6c8061d) opened the gate. | Refuse `nested-seq` for `seq<seq<int>>`/`seq<seq<nat>>` as before (revert `_type_issue`'s seq branch to the old unconditional `return "nested-seq"` for any non-int/non-char element, drop `_is_nested_seq_of_int`/`_is_nested_seq_of_nat`, the return-handling widening, `_seq_literal_issue`'s nested-display acceptance, `_t_json_type`, the `SeqUpdate` acceptance pass and its `lift_rewrite`/`lift_check` cases, and `coverage_census.py`'s `nested-seq` burden back to a gap): 12 of the 643 DafnyBench gradable programs (10 sole) and 10 of the 164 MBPP-DFY (10 sole) lose that path into the fragment, and `dafny-synthesis_task_id_792`'s own checked task loses its task JSON entirely, not just its check verdict; the `array2`/`seq-of-bool`/`nested-seq-string`/`nested-seq-deep`/`nested-seq-other` refusals were already refused (under the coarser `generics`/`nested-seq` names respectively), unaffected either way. |

| 31 | An unbounded (refused) `forall`/`exists` whose binder IS bounded by an index range implied by the array/seq it indexes (`0 <= k < \|s\|`, `0 <= k < a.Length`), but the range is not in the one shape `bound_quantifier` already read (a single top-level `Implies`/`&&`) | n/a (v0's `bound_quantifier` covered only the literal chain-guard and single-`Implies`-body shapes) | n/a | n/a | n/a | Read the SAME bound already recognised (SPEC.md "Gate 1", decision-adjacent to the original 4.4 rules) through four more shapes dafny's own `rprint` produces and the original code never matched: (a) an unguarded forall's antecedent has a LEFTOVER conjunct beside the range chain (`1 <= d <= a && 1 <= d <= b && ... ==> P`) -- the single-binder branch used to bail outright instead of folding the leftover into the body the way the GUARDED branch already does; now it folds, matching that branch. (b) dafny DISTRIBUTES an implication over a top-level `&&` body (`range ==> (A && B)` prints as `(range ==> A) && (range ==> B)`) and a conjunction over a top-level `\|\|` body (`range && (A \|\| B)` prints as `(range && A) \|\| (range && B)`) -- `_extract_unguarded_range` detects the repeated range by structural equality (sound: every copy is the SAME source guard reprinted onto the SAME line) and reassembles the one-guard shape. (c) an UNGUARDED multi-binder quantifier never had ANY unguarded path before (the N-binder branch required `q.range` non-`None`); it now derives a synthetic guard from the body the same way, through (b), and separately (exists only) from a bare top-level `&&` with no `Implies` at all, since `exists` never wraps its range in one. (d) two binders ordered against the SAME end (`0 <= i < j < hi`), one chain over four operands, not the two independent two-operand chains the N-binder loop otherwise expects -- read `i` over `[lo, hi)` and, nested inside, `j` over `[i+1, hi)` (or `[i, hi)` for a non-strict middle relation); every `i` the tighter original guard would have excluded gets an empty inner range, contributing nothing to a forall (vacuously true) or an exists (no witness), the same widening `in-desugared`'s `[0, len(s))` outer range already relies on. A binder with NO index-implied bound anywhere in its body (an `x: int` compared only by membership or arithmetic, e.g. `forall x :: x in result <==> ...`, or a binder defined by an equality filter clause `\| _t#0 == i + 1` rather than an inequality) is UNCHANGED: still refused `unbounded-quantifier`. Measured on the 30 in-fragment dafny-synthesis programs the row named (COVERAGE-mbpp-dfy-lifter.md, ROADMAP 16.2): 22 of 30 now lift (the lifter alone, `--skip-check`); the other 8 split 2 genuinely unbounded (correctly still refused) and the `\| _t#0 == ...` filter-binder shape (6, a different construct, out of this row's scope). Recorded under the SAME rewrite names the original rules already used (`quantifier-bounded`/`in-desugared`) -- row 31 changes which shapes `bound_quantifier` reads, not what it records. | Revert `bound_quantifier` to the four original branches (drop `_extract_unguarded_range`, `_ordered_pair_bound`, the multi-binder unguarded path, and the leftover-conjunct fold in the single-binder unguarded forall branch): the 22 gained tasks (and any other in-corpus program relying on one of these four shapes) return to `unbounded-quantifier`. Every previously-lifted task is unaffected -- none of the four new paths is reached unless the OLD code would already have returned `None`, so no existing lift can change. |

| 32 | rprint's own print of a `requires`/`ensures`/`invariant`/`decreases` clause can parse to a DIFFERENT tree than the source's own text (a dafny printer defect: the parentheses around a divided operand of `*` are dropped on reprint) | n/a (v0 trusted rprint's text as the source's own, unconditionally) | n/a | n/a | n/a | Re-parse each graded method's own `requires`/`ensures` (and, best-effort, every loop's `invariant`/`decreases`) from the ORIGINAL `.dfy` file's own bytes, using the EXACT SAME grammar rprint-derived clauses already go through (`lift_parse._Parser._parse_mspec_list`/`_parse_loopspec_list`, applied to a different token stream: `lift_resolve.check_against_source`), and correct the rprint-derived method's specs IN PLACE, structurally (line-blind), wherever the two trees disagree -- called right after `lift_parse.gradable_methods`, before classify/rewrite/`lift_check.check` ever see the method, so every later stage (including the checker's own `L_req`/`L_ens`/`L_inv_k` equivalence lemmas) reads the SOURCE's own tree from that point on, never rprint's print of it compared against itself. A `requires`/`ensures`/`invariant` clause whose COUNT cannot be matched 1:1 against rprint's own refuses the method `source-unparseable` (named, with both counts, rather than guessed); a `decreases`/`modifies` count mismatch is only logged, never refused, since dafny itself INFERS a `decreases` the source never wrote (measured on 598 itself: 0 in the source, one `decreases n` in rprint). Loop-level correction is best-effort, attempted only when a lexical count of `while`/`for` tokens in the raw body text agrees with `_collect_loop_nodes`'s own structural count over the rprint-parsed body; a mismatch (a loop inside a `forall`/`assert ... by` hint block this row's own simplified scan does not exempt, or any other shape not measured here) skips loop correction with a logged warning rather than risk a misalignment. A source method not found by name, or whose text cannot be tokenised, also refuses `source-unparseable`. Scope, named rather than silently assumed complete: only the GRADED method's own clauses are corrected this way; a closure FUNCTION's own `requires` (decision 6's `spec-fun-totalised`, `L_fun_<name>`'s own signature) is not re-derived from source here and could carry the same class of printer defect, unmeasured -- a residual for a future row, not claimed closed by this one. Recorded `rprint-source-disagreement: <method> <kind>#<k>: ...` in `LiftRecord.warnings` (the sidecar's own free-text log, section 8 -- no new sidecar FIELD, since `lift_ast.py` is not this row's file to change) rather than a new rename-map-shaped field, quoting both the rprint-derived and the source-derived tree in full (`repr`, unambiguous). Measured (2026-09-12): `dafny-synthesis_task_id_598` IsArmstrong's own ensures reads `(n / 100) * (n / 100) * (n / 100) + ((n / 10) % 10) * ((n / 10) % 10) * ((n / 10) % 10) + (n % 10) * (n % 10) * (n % 10)`; `dafny resolve --rprint:-` prints `n / 100 * n / 100 * n / 100 + n / 10 % 10 * n / 10 % 10 * n / 10 % 10 + n % 10 * n % 10 * n % 10` (every parenthesised divided factor's parens dropped), which the SAME parser reads as `((n div 100) * n) div 100 + ...` -- a DIFFERENT tree from the source's `(n div 100) * (n div 100) * (n div 100) + ...`, false (`interp`, checked directly against the source's own arithmetic) at n = 370, 371, 407 where the source's theorem is true, and agreeing at n = 100, 153, 999 (both trees give the same boolean there by coincidence of the inputs, not by being the same tree). With this row: `check_against_source` finds exactly the one `ensures#0` disagreement (`requires` is unchanged, both texts agree on it byte for byte once parsed), corrects it to the source's own grouping, and `interp` now agrees with the source at every one of 100, 153, 370, 371, 407, 999; the method lifts, `L_ens`/`L_req` both verify (`python3 lifter.py --list <598-only> --out <scratch> --jobs 1 --timeout 300`), the differential harness agrees on 3 of 3 points, and `python3 grade.py --tasks <scratch-598> --kernels dafny,spark --flake 3 --jobs 2` reports FULL AGREEMENT on both kernels (`real=verified twin=refuted`, `wrong-var#3`, witness n=100). Re-lifting the 164 dafny-synthesis files with this row live (`python3 lifter.py --list <164-file-list> --corpus-dir <ground_truth> --out <scratch> --jobs 6 --timeout 300`, 245 of 245 gradable methods reached, 0 lost relative to the committed `t/out/lift`'s 237) and diffing every emitted `.json`/`.lift.json` against it: 598's own task JSON is the one designed difference (above). 3 methods that used to refuse `unbounded-quantifier` at classify now lift AND check clean end to end (`checked=True`, confirmed attributable to this row by reproducing the OLD `unbounded-quantifier` refusal on an UNMODIFIED HEAD checkout of the same three files): `_472` ContainsConsecutiveNumbers, `_567` IsSorted, `_622` FindMedian -- the same class of gain as the ensures fix itself, an EXISTENTIAL row 31 could not bound was actually dafny's own resolver-introduced desugaring artifact (a synthetic `_t#0` companion binder, LIFTER-DESIGN's own tokenizer comment names this shape), invisible in the un-desugared SOURCE text this row now reads instead. 1 method (`_751` IsMinHeap) similarly escapes `unbounded-quantifier` but still refuses, one stage later, on the SAME pre-existing `L_ens` quantifier-equivalence automation limit rows 25-30 already named repeatedly -- progress, not a new gap. 1 method (`_594` FirstEvenOddDifference, two closure functions `IsEven`/`IsOdd`) shows a cosmetic difference -- which one is named `L_fun_isEven` vs `L_fun_isOdd` in its (still-refused, `lift-check-failed`, unaffected verdict) outcome, and which of the two collides into a renamer suffix (`n` vs `n_v`) -- traced to a PRE-EXISTING, UNRELATED bug this row's own measurement exposed rather than caused: `lift_classify._closure`/`_called_names` (untouched by this row) discovers a method's closure functions by iterating a plain `set[str]`, and Python randomises string hashing per process by default, so the SAME unmodified HEAD checkout, run twice, non-deterministically returns the closure in either order (measured directly: `lift_classify._closure(module, method)` on `_594` gave `['IsEven', 'IsOdd']` one invocation and `['IsOdd', 'IsEven']` the next, zero code changed between them) -- named here as a residual for `lift_classify.py`'s own next pass, not fixed by this row. A further 20 methods differ ONLY in their `.lift.json` sidecar (never their task `.json`): a `twin_rung` or warning-count difference from the committed snapshot, independently reproduced on the SAME unmodified HEAD checkout against the same committed reference (measured on 2 of the 20 directly, `_242` CountCharacters and `_412` RemoveOddNumbers) -- pre-existing staleness/flakiness between `t/out/lift`'s last sweep and current HEAD (harness/checker behavior that has moved on since), not this row's own doing. Unit-tested dafny-free (`test_align_and_correct_unit`: a genuine disagreement corrects and logs, an agreeing pair is untouched and silent, a `requires`/`ensures` count mismatch refuses `source-unparseable` naming both counts, a `decreases` count mismatch is logged only) and end to end against the real corpus file (`test_source_disagreement_598`, `test_lift_front.py`). | Revert to trusting rprint's text alone for the source side of every clause (drop `lift_resolve.check_against_source` and its call in `lifter.py.lift_file`): 598 (and any other corpus program, present or future, a dafny release prints this same way) keeps a `checked=True` verdict that is unearned -- the printer's own defect, not the checker's automation, decided the outcome, and no amount of checker-side proof effort could ever have caught it, since both sides of the equivalence lemma were reading the SAME wrong print. |

| 33 | Row 31's own carve-out named a shape it did NOT bound: a binder introduced by an EQUALITY filter clause dafny prints as `\| _t#0 == i + 1` (a companion binder defined by an equation, not bounded by an inequality) -- dafny-synthesis 472, 567, 622, 751 (row 32's own text already names these four; row 31 counted "6" clauses across them, since 472/622/751 each carry two such quantifiers -- ensures+invariant, or one per branch -- not six distinct programs) | n/a (v0 had no equality-filter reading at all) | n/a | n/a | n/a | Read the filter as a substitution, generally, at the TOP of `bound_quantifier` before any of section 4.4's/row 31's shapes are tried: a top-level conjunct of `q.range` that is a plain equality `Chain(("==",), (A, B))` with exactly one side a bare `Ident` naming one of the quantifier's OWN binders, the other side an expression that does not mention that same binder (`_binder_defining_equality`, guards against the circular `_t#0 == _t#0 + 1` shape, which is unsatisfiable and never a real definition), eliminates that ONE binder: it is dropped from the binder list, every occurrence of it in the body AND in any other range conjuncts is substituted by the other side (`_subst_ast`, a plain AST-level walk -- distinct from `lift_rewrite._subst`, which runs later against a fresh-renamed scope and knows the `_AtHole` membership sentinel; this one runs here, at classify time, on the still-unrewritten `lift_ast.Expr` tree), and the loop repeats (an equality can define more than one binder in the same quantifier, row 751's own two-branch shape has one per branch, not two in the same clause, but the loop handles either). The REDUCED quantifier (fewer binders, the equality conjunct gone from the range, the substitution folded into the body) then falls into the existing single-/multi-binder logic completely unchanged -- row 33 only ever REMOVES a binder before that logic runs, it never adds a new bounding shape of its own, so every prior row's own behaviour is reached identically once the equality binder(s), if any, are gone. A quantifier with no equality-filter conjunct at all is untouched, byte for byte (`_binder_defining_equality` returns `None` immediately). Eliminating a binder down to ZERO remaining (the whole quantifier collapsing to a closed boolean) is left refused rather than guessed: no in-corpus program exercises it (every measured equality-filter binder has at least one companion binder left over) and t's own forall/exists node has no zero-binder shape to build. Not recorded under any NEW rewrite name: the reduced quantifier reaches the SAME `bound_quantifier` success path row 31 already reports under (`quantifier-bounded`, or `in-desugared` if the reduced form also has a membership binder) -- row 33, like row 31, changes which shapes are READ, not what gets recorded. Unit-tested directly against the exact rprint shape (`test_lift_rules.py`'s hand-rolled shim front end extended to lex `#<alnum>` identifier tails the same way `lift_parse._lex` already does, so a unit test can write `_t#0` literally): the `exists` shape verbatim from 472 (equality on the LEFT), the `forall`/`requires` shape from 622 with the equality's sides REVERSED (`i + 1 == _t#0`), two independent equality binders in one method (751's own two-branch shape), the self-reference guard (`_t#0 == _t#0 + 1` stays refused `unbounded-quantifier`), and row 31's own carve-out re-confirmed unchanged (no range, no defining equation, stays refused). 71 registered unit tests (66 before this row, 5 added) plus 21/21 seed acceptance and 77/77 infragment acceptance pass. Measured on the current 164-file dafny-synthesis corpus (`t/COVERAGE-mbpp-dfy-lifter.md`'s own 164-row list; `python3 lifter.py --list <164-file-list> --corpus-dir <ground_truth> --out <scratch> --jobs 4 --timeout 200`, twice -- once with this row, once on an UNMODIFIED HEAD checkout swapped in for `lift_classify.py` alone, both runs isolated in their OWN freshly-named scratch directory to rule out this shared box's other concurrent agents writing into a same-named one): the SAME five methods refuse `unbounded-quantifier` in BOTH runs (`_572` RemoveDuplicates, `_644` Reverse/ReverseUptoK, `_769` Difference, `_803` IsPerfectSquare -- every one a genuinely unbounded binder compared only by membership or arithmetic, correctly untouched by this row's own carve-out), and `_472`/`_567`/`_622`/`_751` read IDENTICALLY in both runs (`_472`/`_567`/`_622` lift and check clean, `_751` lifts but still refuses `lift-check-failed` on the same pre-existing `L_ens` automation limit rows 25-30 already name) -- this row makes ZERO measured difference on the current corpus, because row 32 (already committed, reading each clause from the ORIGINAL source text) already never lets the resolver-synthesised `_t#0` artifact reach classify in the first place for any of these four: the equality-filter binder is purely a printer/desugaring artifact of a chained index inside a quantifier that no Dafny AUTHOR writes by hand, so once row 32 substitutes the source's own un-desugared tree, the shape this row exists to read is already gone before `bound_quantifier` ever sees it. Row 33 is defense in depth, not a corpus gain: it protects the (documented, best-effort) case where row 32's source-loop-correction skips a method, any corpus without row 32's own source-correction step, and the general contract (any Dafny author could legally write `\| j == i + 1` by hand, not only dafny's own resolver). Diffed every emitted task `.json` against the committed, read-only `t/out/lift`: zero previously-lifted task changed or stopped lifting (only two `.check.diff.deps/runtimeconfig.json` build-cache files, not task data, are absent from the fresh scratch run, present only because the committed snapshot predates a later, unrelated tooling change). Graded `_472`/`_567`/`_622`/`_751` (dafny, lean; flake 3): `_472` and `_622` read `verified/refuted` (full agreement) on both kernels; `_567` reads `vacuous/refuted` (dafny) and `unproved/refuted` (lean); `_751` reads `verified/refuted` (dafny) and `unproved/timeout` (lean, sole blocker) -- all four consistent with the SAME pre-existing per-kernel proof-automation gaps rows 25-30 and row 32 already name, none a new gap this row introduces. Decided 2026-09-14. | Revert `bound_quantifier` to try none of row 33's equality-filter reading (drop `_binder_defining_equality`/`_subst_ast`/`_mentions_ident` and the elimination loop at the top of `bound_quantifier`): no measured change to the current 164-file or 785-lifted-task corpus (row 32 already covers every in-corpus occurrence by a different route), but any FUTURE corpus item, or hand-written `\| j == i + 1` filter, or a case where row 32's own best-effort source-loop-correction skips, would read `unbounded-quantifier` again instead of eliminating the defined binder. |

| 34 | Read-only array PARAMETER (decision 1): two bugs that FALSELY refused a method whose own array parameter genuinely satisfied decision 1's condition, found by measuring the MBPP-DFY 164's `array` census rows | n/a (bug fixes against decision 1's own stated condition, not a new construct) | n/a | n/a | n/a | (a) `array_readonly_issue`'s flat, scope-blind identifier scan flagged a bare `Ident` matching `param_name` inside a CLOSURE function that shadows the name with its own parameter (`min(s: seq<int>)` alongside the method's `array<int> s`) as the array escaping to a call; `_closure_root_shadows` now skips a closure root whose own parameter list rebinds the name before scanning it, in both `array_readonly_issue` and `_array_passed_to_call`. (b) the generic per-node scan (`_scan_node_for_issues`) flagged EVERY array-typed `VarDeclStmt` as `local array` unconditionally, even one `find_array_mutation` had already validated as decision 22's own `alloc-fill` shape (`var cubedArray := new int[a.Length];` alongside a genuinely read-only, UNRELATED array parameter `a`); `_array_mutation_accepted_ids` now also exempts that one `VarDeclStmt`. Fixing (b) exposed a THIRD, previously unreachable bug in `lift_rewrite._lift_stmt`'s own `VarDeclStmt`/`NewRhs` branch (`json_ty` read before being assigned on that branch, `UnboundLocalError`), fixed alongside it since nothing upstream could ever have reached it before. Recorded via the SAME rule names decisions 1/22 already use (`array-readonly-as-seq`, `array-mutation-as-seq`); no new rewrite name, since nothing new is being decided, only correctly recognised. | Leave the two scan bugs in place: every method matching either shape (a closure function shadowing an array parameter's name, or an alloc-fill local coexisting with an unrelated read-only array parameter) stays refused `array`, an under-count of decision 1's own stated condition rather than a true measurement of it. |
| 35 | A closure function's OWN `reads` clause naming only its OWN read-only array parameter (`predicate InArray(a: array<int>, x: int) reads a`), and passing a method's read-only array parameter to that function as a plain call argument (`InArray(a, x)`) | n/a (t had no function-level array parameter at design time; the generic `reads`-nonempty rule and the "passed to a call" escape check both predate this row and were never narrowed for this shape) | n/a | n/a | n/a | Widen decision 1 to a closure FUNCTION's own array parameter, on the same read-only condition: a Dafny function can never write through any reference at all (ghost or not, no assignment, no heap mutation, ever), so its own array parameter is read-only BY CONSTRUCTION and lifts to `seq` (`lift_rewrite._lift_function`'s param-typing now goes through the SAME `_t_type_of` decision 1's method-level params already use, rather than a hand-rolled seq/bool/else-int with no array case). A `reads` clause whose every expr is a bare `Ident` naming one of the function's own array-typed (non-nullable, int/nat-element) parameters is DROPPED, not refused (t has no heap to frame once that array is a `seq` value), recorded `function-reads-array-dropped`; a `reads` clause naming anything else (`*`, a field, a different expression) still refuses `function-contract` exactly as before. Passing the method's array parameter to that closure function as a call argument (`InArray(a, x)`) is exempted from `array_readonly_issue`'s "passed to a call" escape check -- a pure function cannot alias or retain what it is handed -- while passing it to any other call (another method, an unresolved callee) is UNCHANGED (already unreachable in practice: a call of a different method already refuses `calls-other-method` first). Closing this pair also uncovered a THIRD, previously unreachable bug this row fixes alongside it: dafny's rprint always materialises an INFERRED decreases clause for any `reads`-bearing function (a leading `SetDisplay` heap-ordering component, `decreases {a}, a, x`), self-recursive or not; `lift_rewrite._function_decreases` used to gate only on "is a decreases clause PRINTED at all" (`if not dcs: return None`), so a non-recursive `reads`-bearing function's inferred-but-unneeded decreases reached `_lift_expr` and crashed (`ValueError: no expression mapping for SetDisplay`) the first time classify ever let one through. Fixed by gating on SELF-CALLS instead (`if not calls: return None`), matching what `_decreases_issues` already assumes on the classify side. Measured on the MBPP-DFY 164's own four `function-contract` census rows (`dafny-synthesis_task_id_2/161/249/579`, all sharing the identical `InArray` predicate): all four now lift and check_wf clean; a fifth (`_579` `DissimilarElements`) still refuses, one stage earlier, on an UNRELATED pre-existing rule (decision 14's `ghost-local`, a `ghost var partialSize` local this row does not touch); three (`_2/161/249`) reach the checker and fail `L_fun_inArray` there, a genuinely NEW residual in `lift_check.py`'s spec-function-equivalence-lemma generation for a function with an array-to-seq parameter (that file is not owned by this row -- see LIFTER-DECISIONS.md's own header table and LIFTER-785-RESIDUALS.md for where residuals outside a row's own files are named rather than silently left unexplained). | Refuse every `reads`-bearing closure function outright regardless of shape (revert to the old unconditional rule): all four MBPP-DFY `function-contract` rows, and any other program pairing a read-only array parameter with a `reads`-annotated helper predicate over it, stay refused on framing syntax alone, never on the actual (nonexistent) mutation risk. |

Excluded on purpose: the ROADMAP 12.3 question, whether a task with no
refuting twin is refused, stays open and is not the lifter's to settle. The
lifter emits the task and records the twin refusal; the count treats it under
whatever 12.3 decides.

Where a design's default was reversed by this file, the design text still
argues the other way: VL section 15 items 1 and 5 (arrays, clause splitting)
and DPN section 15 items 1 and 9 are the places to read the losing argument.
