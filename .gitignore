# Publish safety is enforced by sync_public.py's whitelist, NOT by this file.
# This repo has no git remote on purpose: nothing here can be pushed anywhere.
# Do not add council/ or channel files here. They belong in this repo and are
# kept out of the public one by the whitelist, which cannot be forgotten.
.venv-train/
smoke_adapter/
smoke_adapter_*/
dpo_adapter_native/
dpo_adapter/
localllm/out_demo/
localllm/corpus.txt
*.err
*.log
**/__pycache__/
*.pyc
_locallm_publish/
