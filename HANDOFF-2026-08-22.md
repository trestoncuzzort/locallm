# HANDOFF — 2026-08-22 (read with HANDOFF-2026-08-20.md, OPEN-ITEMS.md, poscontrol/START-HERE.md)

## What happened today
- Session 2cfd3a1f: literature sweep (12 angles + 2 repo audits, 14 agents), zero-compute probes, the 8-bit-optimizer
  block finding, the massive-activation check on the base model, and a full manuscript revision (v3). A power loss at
  ~14:21 killed the gap-finder memo (16/17 agents done) and the 5-lens verification (0/5 returned).
- Session cd70aff0: restarted both from their journals; both completed. Then a correction pass produced v4.
- Antigravity (Gemini) ran in parallel on web/citation jobs. Its fetched bibliographic data was reliable (4/4 spot
  checks); its JUDGMENTS were not (one fabricated arXiv ID, one misattributed ID, six false "mismatch" flags, three
  wrong "suggested DOIs", and a TRL table that confused file line order with call order). Rule adopted: every arXiv ID
  is resolved to its abs-page title before use; every source-order claim is traced by execution, not line number.

## Where everything is
- Manuscript: `C:\Users\tmcuzzort\Downloads\progress so far v4.md` (corrected, polished); v3 frozen as
  `docs/review-2026-08-22/manuscript-v3-pre-verification.md`.
- `docs/review-2026-08-22/`: research_direction_memo.md (ranked gaps + first-lab-session plan), verification_report.md
  (ordered must-fix list), verification_dossier.md (107 clean claims, 42 discrepancies with referee verdicts),
  references_corrections.md, nikolich_2606.23740_extract.md, new_citations_resolved.md, trl_seed_order_audit.md
  (Antigravity's — WRONG on its seed-first rows), trl_seed_order_audit_partial.md (byte-level, partial), context.md,
  probe_results.md, merged_gaps.json, refutations_cached.json.

## Four major defects found by verification (all repo facts; the paper now states them honestly)
- M1 corpus tally: 773 AssertionError / 17 NameError / 128 other in data/dpo_pairs_capped.jsonl (paper said 866/17/35).
- M2 data/ruler_noise.jsonl retains only 5 of 40 `llama3-forged-null` replicates (775 draws). Table 1's null row and
  Appendix C's Null column cannot be regenerated from any retained artifact. ACTION: recover the 35 rows from the
  4080 machine's copy if it exists; otherwise the paper's "transcribed from a superseded record" wording stands.
- M3 the `.gitattributes eol=lf` fix claimed in Failure 4 was never implemented: the file holds only
  `data/*.jsonl merge=union` (CRLF) and core.autocrlf=true. Residual R-1 is OPEN. This is a red witness; fix only
  with a receipt regenerated under the normalization and the autocrlf=false/true fingerprint-equality probe.
- M4 Discussion "+9.7 to +11.0 pp, p<1e-9" contradicted Table 2 (+8.8 to +11.4, same-session Welch).

## Research direction (memo, after 12 refuter verdicts)
1. Optimizer-block artifact (8-bit blockwise Adam vs channel 2427) — first experiment: four fixed-seed --max-steps 20
   runs, ~50 min GPU. 2. Functional acceptance class + norm-matched random-adapter null. 3. Verifier-environment swap.
   "The Sketch Law" is refuted: Nikolich et al. arXiv:2606.23740 §3.4 already publishes cross-seed LoRA orthogonality
   from the unseeded A init and "different weights, same basin". The paper now cites it and narrows its claim.

## TRL seeding-order audit (in progress)
- Installed trl 0.12.2: get_peft_model at dpo_trainer.py:376 precedes super().__init__ at :640 -> set_seed at
  transformers/trainer.py:424. lora_A is unseeded even with a seed passed. Confirmed by two independent searches that
  nobody has reported this. v0.21.0 spot-checked by hand: still peft-first (helper called at 317, defined at 543).
- RESULT (byte-level, execution order inside __init__): 108/108 trainer rows (DPO/SFT/GRPO where present) across
  39 releases from v0.7.11 through v0.29.1 and v1.0.0-v1.6.0 are PEFT-FIRST; 0 seed-first; 5 independent verifier
  re-derivations agree. Table: docs/review-2026-08-22/trl_seed_order_audit_partial.md. Not yet traced: `main`.
- Byte-level audit across all minors: 45 cached results in session cd70aff0 workflow wf_d02411b3-eb8 (batched script
  at .claude/projects/C--Users-tmcuzzort-AppData-Local-Temp/cd70aff0-.../workflows/scripts/). Resume, don't rerun.

## Uncommitted as of this handoff
Everything from Aug 20 (18 poscontrol rows in data/ruler_noise.jsonl, forge.py f22eede7, harness_smells.py + tests,
poscontrol/{poscontrol_verdict,red_witness_nonce_exploit}.py, dashboard.py, data/prereg_positive_control*.json,
dpo_adapter_poscontrol/ [168 MB safetensors is unpushable — push seed + lora_B + GGUF]) plus today's docs/ and this file.

## Next session, in order
1. Commit + push the above (smoke-test push first; git ls-remote timed out once today).
2. Decide M2 (recover rows or keep the disclosure) and M3 (implement .gitattributes with receipt + probe).
3. Preregister the optimizer ablation (memo §6 step 2), add --seed/--optim/--rank/--save-steps to train_native.py
   (memo §6 step 3), validate seeding with two --max-steps 1 runs + init_vs_training.py, then run the four arms.
4. Resume the TRL audit workflow; fold the table into the paper's Initialization subsection.
